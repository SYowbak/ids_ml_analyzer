================================================================================
CODEX TEST EVALUATION RESULTS
================================================================================

--- Output 1 ---
Exit code: 0
Wall time: 3.7 seconds
Output:
model <class 'src.core.two_stage_model.TwoStageModel'> algo Random Forest
threshold_default 0.3 strict 0.65
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_DDoS_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 90.7% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_PortScan_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 95.3% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_WebAttack_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 90.7% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 97.7% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 97.7% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Neptune_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Satan_Probe_15pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Smurf_DoS_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Exploits_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Fuzzers_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Generic_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)

=== PER FILE ===
                                        file  thr  rows  attack_true  attack_pred      TPR      FPR  Precision  tp  fp  fn
          CIC-IDS2017_DDoS_50pct_anomaly.csv 0.20  1000          500          199 0.274000 0.124000   0.688442 137  62 363
          CIC-IDS2017_DDoS_50pct_anomaly.csv 0.30  1000          500            0 0.000000 0.000000   0.000000   0   0 500
          CIC-IDS2017_DDoS_50pct_anomaly.csv 0.50  1000          500            0 0.000000 0.000000   0.000000   0   0 500
          CIC-IDS2017_DDoS_50pct_anomaly.csv 0.65  1000          500            0 0.000000 0.000000   0.000000   0   0 500
      CIC-IDS2017_PortScan_30pct_anomaly.csv 0.20  1000          300           77 0.010000 0.105714   0.038961   3  74 297
      CIC-IDS2017_PortScan_30pct_anomaly.csv 0.30  1000          300            0 0.000000 0.000000   0.000000   0   0 300
      CIC-IDS2017_PortScan_30pct_anomaly.csv 0.50  1000          300            0 0.000000 0.000000   0.000000   0   0 300
      CIC-IDS2017_PortScan_30pct_anomaly.csv 0.65  1000          300            0 0.000000 0.000000   0.000000   0   0 300
     CIC-IDS2017_WebAttack_10pct_anomaly.csv 0.20  1000          100          116 0.060000 0.122222   0.051724   6 110  94
     CIC-IDS2017_WebAttack_10pct_anomaly.csv 0.30  1000          100            0 0.000000 0.000000   0.000000   0   0 100
     CIC-IDS2017_WebAttack_10pct_anomaly.csv 0.50  1000          100            0 0.000000 0.000000   0.000000   0   0 100
     CIC-IDS2017_WebAttack_10pct_anomaly.csv 0.65  1000          100            0 0.000000 0.000000   0.000000   0   0 100
CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv 0.20  1000          200          167 0.060000 0.193750   0.071856  12 155 188
CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv 0.30  1000          200            0 0.000000 0.000000   0.000000   0   0 200
CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv 0.50  1000          200            0 0.000000 0.000000   0.000000   0   0 200
CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv 0.65  1000          200            0 0.000000 0.000000   0.000000   0   0 200
CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv 0.20  1000          300          180 0.146667 0.194286   0.244444  44 136 256
CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv 0.30  1000          300            0 0.000000 0.000000   0.000000   0   0 300
CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv 0.50  1000          300            0 0.000000 0.000000   0.000000   0   0 300
CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv 0.65  1000          300            0 0.000000 0.000000   0.000000   0   0 300
   NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv 0.20   500          100            0 0.000000 0.000000   0.000000   0   0 100
   NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv 0.30   500          100            0 0.000000 0.000000   0.000000   0   0 100
   NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv 0.50   500          100            0 0.000000 0.000000   0.000000   0   0 100
   NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv 0.65   500          100            0 0.000000 0.000000   0.000000   0   0 100
       NSL-KDD_Neptune_DoS_20pct_anomaly.csv 0.20  1000          200            0 0.000000 0.000000   0.000000   0   0 200
       NSL-KDD_Neptune_DoS_20pct_anomaly.csv 0.30  1000          200            0 0.000000 0.000000   0.000000   0   0 200
       NSL-KDD_Neptune_DoS_20pct_anomaly.csv 0.50  1000          200            0 0.000000 0.000000   0.000000   0   0 200
       NSL-KDD_Neptune_DoS_20pct_anomaly.csv 0.65  1000          200            0 0.000000 0.000000   0.000000   0   0 200
       NSL-KDD_Satan_Probe_15pct_anomaly.csv 0.20  1000          150            0 0.000000 0.000000   0.000000   0   0 150
       NSL-KDD_Satan_Probe_15pct_anomaly.csv 0.30  1000          150            0 0.000000 0.000000   0.000000   0   0 150
       NSL-KDD_Satan_Probe_15pct_anomaly.csv 0.50  1000          150            0 0.000000 0.000000   0.000000   0   0 150
       NSL-KDD_Satan_Probe_15pct_anomaly.csv 0.65  1000          150            0 0.000000 0.000000   0.000000   0   0 150
         NSL-KDD_Smurf_DoS_10pct_anomaly.csv 0.20   500           50            0 0.000000 0.000000   0.000000   0   0  50
         NSL-KDD_Smurf_DoS_10pct_anomaly.csv 0.30   500           50            0 0.000000 0.000000   0.000000   0   0  50
         NSL-KDD_Smurf_DoS_10pct_anomaly.csv 0.50   500           50            0 0.000000 0.000000   0.000000   0   0  50
         NSL-KDD_Smurf_DoS_10pct_anomaly.csv 0.65   500           50            0 0.000000 0.000000   0.000000   0   0  50
             UNSW_NB15_DoS_20pct_anomaly.csv 0.20  1000          200          436 0.255000 0.481250   0.116972  51 385 149
             UNSW_NB15_DoS_20pct_anomaly.csv 0.30  1000          200            1 0.005000 0.000000   1.000000   1   0 199
             UNSW_NB15_DoS_20pct_anomaly.csv 0.50  1000          200            0 0.000000 0.000000   0.000000   0   0 200
             UNSW_NB15_DoS_20pct_anomaly.csv 0.65  1000          200            0 0.000000 0.000000   0.000000   0   0 200
        UNSW_NB15_Exploits_30pct_anomaly.csv 0.20  1000          300          537 0.660000 0.484286   0.368715 198 339 102
        UNSW_NB15_Exploits_30pct_anomaly.csv 0.30  1000          300            0 0.000000 0.000000   0.000000   0   0 300
        UNSW_NB15_Exploits_30pct_anomaly.csv 0.50  1000          300            0 0.000000 0.000000   0.000000   0   0 300
        UNSW_NB15_Exploits_30pct_anomaly.csv 0.65  1000          300            0 0.000000 0.000000   0.000000   0   0 300
         UNSW_NB15_Fuzzers_10pct_anomaly.csv 0.20  1000          100          490 0.520000 0.486667   0.106122  52 438  48
         UNSW_NB15_Fuzzers_10pct_anomaly.csv 0.30  1000          100            0 0.000000 0.000000   0.000000   0   0 100
         UNSW_NB15_Fuzzers_10pct_anomaly.csv 0.50  1000          100            0 0.000000 0.000000   0.000000   0   0 100
         UNSW_NB15_Fuzzers_10pct_anomaly.csv 0.65  1000          100            0 0.000000 0.000000   0.000000   0   0 100
         UNSW_NB15_Generic_50pct_anomaly.csv 0.20  1000          500          239 0.022000 0.456000   0.046025  11 228 489
         UNSW_NB15_Generic_50pct_anomaly.csv 0.30  1000          500            0 0.000000 0.000000   0.000000   0   0 500
         UNSW_NB15_Generic_50pct_anomaly.csv 0.50  1000          500            0 0.000000 0.000000   0.000000   0   0 500
         UNSW_NB15_Generic_50pct_anomaly.csv 0.65  1000          500            0 0.000000 0.000000   0.000000   0   0 500

=== AGG BY THRESHOLD ===
thr=0.20 weighted_TPR=0.1713 weighted_FPR=0.2141 weighted_Prec=0.2102
thr=0.30 weighted_TPR=0.0003 weighted_FPR=0.0000 weighted_Prec=0.0769
thr=0.50 weighted_TPR=0.0000 weighted_FPR=0.0000 weighted_Prec=0.0000
thr=0.65 weighted_TPR=0.0000 weighted_FPR=0.0000 weighted_Prec=0.0000
[Preprocessor] 56 columns filled with 0: ['Init_Win_bytes_forward', 'Fwd Pkts/s', 'Bwd Header Len', 'idle_max', 'Packet Length Variance']...
[Preprocessor] 56 columns filled with 0: ['Init_Win_bytes_forward', 'Fwd Pkts/s', 'Bwd Header Len', 'idle_max', 'Packet Length Variance']...
[Preprocessor] 56 columns filled with 0: ['Init_Win_bytes_forward', 'Fwd Pkts/s', 'Bwd Header Len', 'idle_max', 'Packet Length Variance']...
[Preprocessor] 56 columns filled with 0: ['Init_Win_bytes_forward', 'Fwd Pkts/s', 'Bwd Header Len', 'idle_max', 'Packet Length Variance']...
[Preprocessor] 56 columns filled with 0: ['Init_Win_bytes_forward', 'Fwd Pkts/s', 'Bwd Header Len', 'idle_max', 'Packet Length Variance']...
[Preprocessor] 92 columns filled with 0: ['idle_max', 'tcp_fin_count', 'avg_fwd_segment_size', 'bwd_iat_min', 'Init Fwd Win Byts']...
[Preprocessor] 92 columns filled with 0: ['idle_max', 'tcp_fin_count', 'avg_fwd_segment_size', 'bwd_iat_min', 'Init Fwd Win Byts']...
[Preprocessor] 92 columns filled with 0: ['idle_max', 'tcp_fin_count', 'avg_fwd_segment_size', 'bwd_iat_min', 'Init Fwd Win Byts']...
[Preprocessor] 92 columns filled with 0: ['idle_max', 'tcp_fin_count', 'avg_fwd_segment_size', 'bwd_iat_min', 'Init Fwd Win Byts']...
[Preprocessor] 82 columns filled with 0: ['idle_max', 'tcp_fin_count', 'avg_fwd_segment_size', 'bwd_iat_min', 'Init Fwd Win Byts']...
[Preprocessor] 82 columns filled with 0: ['idle_max', 'tcp_fin_count', 'avg_fwd_segment_size', 'bwd_iat_min', 'Init Fwd Win Byts']...
[Preprocessor] 82 columns filled with 0: ['idle_max', 'tcp_fin_count', 'avg_fwd_segment_size', 'bwd_iat_min', 'Init Fwd Win Byts']...
[Preprocessor] 82 columns filled with 0: ['idle_max', 'tcp_fin_count', 'avg_fwd_segment_size', 'bwd_iat_min', 'Init Fwd Win Byts']...


--- Output 2 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
                            add_log(
                                "вљ пёЏ XGBoost: reindex РєР»Р°СЃС–РІ РІРёРєРѕРЅР°РЅРѕ, Р°Р»Рµ СЃРёРЅС…СЂРѕРЅС–Р·СѓРІР°С‚Рё target_encoder "
                                "Р°РІС‚РѕРјР°С‚РёС‡РЅРѕ РЅРµ РІРґР°Р»РѕСЃСЏ."
                            )

                progress.progress(45)

                st.info(f"РўСЂРµРЅСѓРІР°РЅРЅСЏ РЅР° {len(X_train):,} Р·Р°РїРёСЃР°С…, РїРµСЂРµРІС–СЂРєР° РЅР° {len(X_test):,}...")
                progress.progress(55)

                engine = ModelEngine(models_dir=str(ROOT_DIR / 'models'))

                if two_stage_mode:
                    add_log("Р Р•Р–РРњ: TWO-STAGE DETECTION ENABLED")
                elif turbo_mode:
                    add_log("Р Р•Р–РРњ: РўРЈР Р‘Рћ")
                else:
                    add_log("Р Р•Р–РРњ: РџРћР’РќРР™")

                st.info("РћРїС‚РёРјС–Р·Р°С†С–СЏ РїР°СЂР°РјРµС‚СЂС–РІ (С†Рµ РјРѕР¶Рµ Р·Р°Р№РЅСЏС‚Рё С‡Р°СЃ)...")
                progress.progress(60)

                print(f"[LOG] Starting training with algorithm: {algorithm}, Turbo={turbo_mode}, TwoStage={two_stage_mode}")

                model = None
                search_info = {}
                two_stage_threshold_info: dict[str, Any] = {
                    'threshold': float(DEFAULT_SENSITIVITY_THRESHOLD),
                    'f1_attack': 0.0,
                    'f2_attack': 0.0,
                    'precision_attack': 0.0,
                    'recall_attack': 0.0,
                    'evaluated_points': 0,
                    'objective': 0.0,
                }

                benign_candidates: list[int] = []
                if two_stage_mode:
                    label_map = preprocessor.get_label_map() if hasattr(preprocessor, 'get_label_map') else {}
                    benign_tokens = BENIGN_LABEL_TOKENS
                    benign_candidates = [
                        class_id
                        for class_id, class_name in label_map.items()
                        if str(class_name).strip().lower() in benign_tokens
                    ]
                    if not benign_candidates:
                        st.warning(
                            "РЈ РґР°С‚Р°СЃРµС‚С– РЅРµ Р·РЅР°Р№РґРµРЅРѕ BENIGN/Normal РєР»Р°СЃ. "
                            "Two-Stage РІРёРјРєРЅРµРЅРѕ вЂ” РІРёРєРѕСЂРёСЃС‚РѕРІСѓС”С‚СЊСЃСЏ Р·РІРёС‡Р°Р№РЅР° РєР»Р°СЃРёС„С–РєР°С†С–СЏ."
                        )
                        add_log("вљ  BENIGN РєР»Р°СЃ РЅРµ Р·РЅР°Р№РґРµРЅРѕ вЂ” Two-Stage РІРёРјРєРЅРµРЅРѕ (fallback РґРѕ supervised).")
                        two_stage_mode = False

                if two_stage_mode:
                    add_log("Р†РЅС–С†С–Р°Р»С–Р·Р°С†С–СЏ РґРІРѕРµС‚Р°РїРЅРѕС— РјРѕРґРµР»С– (Binary + Multiclass)...")

                    binary_base = engine._create_base_model(algorithm)
                    multiclass_base = engine._create_base_model(algorithm)
                    model = TwoStageModel(binary_model=binary_base, multiclass_model=multiclass_base)

                    add_log("РўСЂРµРЅСѓРІР°РЅРЅСЏ Stage 1 С‚Р° Stage 2...")

                    benign_code_for_two_stage = None
                    if benign_candidates:
                        benign_code_for_two_stage = benign_candidates[0]
                        add_log(f"Р’РёР·РЅР°С‡РµРЅРѕ BENIGN РєРѕРґ РґР»СЏ Two-Stage: {benign_code_for_two_stage}")
                        model.fit(X_train, y_train, benign_code=benign_code_for_two_stage)
                    else:
                        add_log("вљ пёЏ BENIGN РєРѕРґ РЅРµ РІРёР·РЅР°С‡РµРЅРѕ РѕРґРЅРѕР·РЅР°С‡РЅРѕ; РІРёРєРѕСЂРёСЃС‚РѕРІСѓС”С‚СЊСЃСЏ РµРІСЂРёСЃС‚РёРєР° Two-Stage")
                        model.fit(X_train, y_train)

                    two_stage_threshold_info = _calibrate_two_stage_threshold(
                        model,
                        X_test,
                        y_test,
                        benign_code=benign_code_for_two_stage
                    )
                    add_log(
                        "Two-Stage Р°РІС‚Рѕ-РїРѕСЂС–Рі: "
                        f"{float(two_stage_threshold_info['threshold']):.2f} "
                        f"(F1_attack={float(two_stage_threshold_info['f1_attack']):.3f}, "
                        f"F2_attack={float(two_stage_threshold_info.get('f2_attack', 0.0)):.3f}, "
                        f"Recall_attack={float(two_stage_threshold_info['recall_attack']):.3f})"
                    )

                    engine.model = model
                    engine.algorithm_name = f"Two-Stage ({algorithm})"
                    search_info = {
                        'best_params': 'Default (Two-Stage Mode)',
                        'best_score': 'N/A',
                        'two_stage_threshold': two_stage_threshold_info
                    }

                elif is_isolation_algorithm:
                    add_log("Р Р•Р–РРњ: ANOMALY DETECTION (Isolation Forest)")
                    add_log("РќР°РІС‡Р°РЅРЅСЏ С‚С–Р»СЊРєРё РЅР° РЅРѕСЂРјР°Р»СЊРЅРѕРјСѓ С‚СЂР°С„С–РєСѓ (BENIGN/NORMAL)...")

                    label_map = preprocessor.get_label_map() if hasattr(preprocessor, 'get_label_map') else {}
                    normal_tokens = BENIGN_LABEL_TOKENS
                    normal_ids = [
                        class_id
                        for class_id, label_name in label_map.items()
                        if str(label_name).strip().lower() in normal_tokens
                    ]
                    if not normal_ids:
                        normal_ids = [0]

                    normal_mask = y_train.isin(normal_ids)
                    X_train_normal = X_train[normal_mask]

                    if len(X_train_normal) < 10:
                        st.error("РќРµРґРѕСЃС‚Р°С‚РЅСЊРѕ РЅРѕСЂРјР°Р»СЊРЅРѕРіРѕ С‚СЂР°С„С–РєСѓ РґР»СЏ РЅР°РІС‡Р°РЅРЅСЏ (РјС–РЅС–РјСѓРј 10 Р·Р°РїРёСЃС–РІ BENIGN/NORMAL)")
                        st.stop()

                    add_log(f"Р’РёРєРѕСЂРёСЃС‚Р°РЅРѕ {len(X_train_normal)} Р·Р°РїРёСЃС–РІ РЅРѕСЂРјР°Р»СЊРЅРѕРіРѕ С‚СЂР°С„С–РєСѓ")
                    if_effective_contamination = (
                        float(if_contamination) if if_manual_contamination else float(DEFAULT_IF_CONTAMINATION)
                    )
                    if if_manual_contamination:
                        add_log(f"Р СѓС‡РЅРёР№ contamination: {if_effective_contamination:.3f}")
                    else:
                        add_log(f"Р’РёРєРѕСЂРёСЃС‚Р°РЅРѕ СЂРµРєРѕРјРµРЅРґРѕРІР°РЅРёР№ contamination: {if_effective_contamination:.3f}")

                    model = engine.train(
                        X_train_normal,
                        y_train[normal_mask],
                        algorithm='Isolation Forest',
                        params={
                            'n_estimators': int(if_n_estimators),
                            'contamination': float(if_effective_contamination),
                            'random_state': 42,
                            'n_jobs': 1
                        }
                    )
                    add_log(f"IF С‚СЂРµРЅСѓРІР°РЅРЅСЏ: n_estimators={if_n_estimators}, contamination={if_effective_contamination:.4f}")

                    calibration_info = {}
                    y_attack_holdout = (~y_test.isin(normal_ids)).astype(int).to_numpy(dtype=int)
                    X_calib = X_test
                    y_attack_calib = y_attack_holdout

                    if int(np.sum(y_attack_holdout == 1)) == 0:
                        add_log("РЈ РІР°Р»С–РґР°С†С–Р№РЅС–Р№ РІРёР±С–СЂС†С– РЅРµРјР°С” Р°С‚Р°Рє. РЁСѓРєР°С”РјРѕ Р·РѕРІРЅС–С€РЅС– РґР°РЅС– РґР»СЏ Р°РІС‚Рѕ-РєР°Р»С–Р±СЂСѓРІР°РЅРЅСЏ...")
                        X_ext_calib, y_ext_attack = _load_if_external_calibration(
                            loader=loader,
                            preprocessor=preprocessor,
                            exclude_path=dataset_path if (dataset_path and isinstance(dataset_path, Path)) else None
                        )
                        if X_ext_calib is not None and y_ext_attack is not None and int(np.sum(y_ext_attack == 1)) > 0:
                            X_calib = X_ext_calib
                            y_attack_calib = y_ext_attack
                            add_log(f"Р—Р°РІР°РЅС‚Р°Р¶РµРЅРѕ Р·РѕРІРЅС–С€РЅСЋ РєР°Р»С–Р±СЂРѕРІРєСѓ: {len(X_calib)} Р·Р°РїРёСЃС–РІ")
                        else:
                            y_attack_calib = None
                            add_log("Р—РѕРІРЅС–С€РЅСЋ РєР°Р»С–Р±СЂРѕРІРєСѓ РЅРµ Р·РЅР°Р№РґРµРЅРѕ. Р’РёРєРѕСЂРёСЃС‚РѕРІСѓС”РјРѕ unsupervised РєР°Р»С–Р±СЂСѓРІР°РЅРЅСЏ.")

                    if if_auto_calibration:
                        calibration_info = engine.auto_calibrate_isolation_threshold(
                            X_calib,
                            y_attack_binary=y_attack_calib,
                            target_fp_rate=float(if_target_fp_rate)
                        )
                        add_log(
                            "IF auto-calibration: "
                            f"mode={calibration_info.get('mode')}, "
                            f"threshold={float(calibration_info.get('threshold', 0.0)):.4f}, "
                            f"anomaly_rate={float(calibration_info.get('anomaly_rate', 0.0)):.2%}"
                        )
                    else:
                        engine.if_threshold_ = 0.0
                        engine.if_threshold_mode_ = "decision_zero_manual"
                        calibration_info = {
                            'mode': engine.if_threshold_mode_,
                            'threshold': float(engine.if_threshold_),
                            'target_fp_rate': float(if_target_fp_rate),
                            'supervised_used': False
                        }
                        add_log("IF auto-calibration РІРёРјРєРЅРµРЅРѕ. Р’РёРєРѕСЂРёСЃС‚Р°РЅРѕ РєР»Р°СЃРёС‡РЅРёР№ РїРѕСЂС–Рі 0.0.")

                    scores = model.decision_function(X_train_normal)
                    print(f"[LOG] IF scores on training: min={scores.min():.4f}, max={scores.max():.4f}, mean={scores.mean():.4f}")
                    add_log(
                        f"IF training scores: min={scores.min():.4f}, max={scores.max():.4f}, "
                        f"mean={scores.mean():.4f}"
                    )

                    search_info = {
                        'best_params': {'n_estimators': int(if_n_estimators), 'contamination': float(if_effective_contamination)},
                        'best_score': 'N/A (Unsupervised)',
                        'if_calibration': calibration_info
                    }

                else:
                    model, search_info = engine.optimize_hyperparameters(
                        X_train,
                        y_train,
                        algorithm=algorithm,
                        search_type=search_type,
                        fast=turbo_mode,
                        progress_callback=add_log
                    )

                print("[LOG] Training finished.")
                progress.progress(82)

                st.info("РћС†С–РЅРєР° С‚РѕС‡РЅРѕСЃС‚С– РјРѕРґРµР»С–...")
                from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score, confusion_matrix

                label_map_eval = preprocessor.get_label_map() if hasattr(preprocessor, 'get_label_map') else {}
                normal_ids_eval = _resolve_normal_label_ids(label_map_eval)
                y_test_attack = (~y_test.isin(normal_ids_eval)).astype(int).to_numpy(dtype=int)

                if is_isolation_algorithm:
                    y_pred = engine.predict(X_test)
                    y_pred_attack = (np.asarray(y_pred).astype(int) == 1).astype(int)
                    metrics = {
                        'accuracy': accuracy_score(y_test_attack, y_pred_attack),
                        'precision': precision_score(y_test_attack, y_pred_attack, zero_division=0),
                        'recall': recall_score(y_test_attack, y_pred_attack, zero_division=0),
                        'f1': f1_score(y_test_attack, y_pred_attack, zero_division=0),
                        'confusion_matrix': confusion_matrix(y_test_attack, y_pred_attack).tolist()
                    }
                else:
                    metrics = engine.evaluate(X_test, y_test)
                    y_pred_eval = np.asarray(engine.predict(X_test))
                    y_pred_attack = (~np.isin(y_pred_eval, normal_ids_eval)).astype(int)
                    metrics['attack_precision'] = precision_score(y_test_attack, y_pred_attack, zero_division=0)
                    metrics['attack_recall'] = recall_score(y_test_attack, y_pred_attack, zero_division=0)
                    metrics['attack_f1'] = f1_score(y_test_attack, y_pred_attack, zero_division=0)
                    metrics['attack_accuracy'] = accuracy_score(y_test_attack, y_pred_attack)
                    metrics['attack_confusion_matrix'] = confusion_matrix(y_test_attack, y_pred_attack).tolist()

                metrics['attack_rate_test'] = float(np.mean(y_test_attack)) if len(y_test_attack) else 0.0
                metrics['attack_rate_pred'] = float(np.mean(y_pred_attack)) if len(y_pred_attack) else 0.0

                class_counts_train = y_train.value_counts(normalize=True) if hasattr(y_train, 'value_counts') else pd.Series(dtype=float)
                metrics['unique_classes_train'] = int(len(np.unique(y_train))) if len(y_train) else 0
                metrics['unique_classes_test'] = int(len(np.unique(y_test))) if len(y_test) else 0
                metrics['min_class_ratio_train'] = float(class_counts_train.min()) if len(class_counts_train) else 0.0

                training_files_used = (
                    list(files_for_training_used)
                    if files_for_training_used
                    else (
                        list(mega_model_files)
                        if is_mega_model
                        else (list(selected_training_files) if selected_training_files else ([dataset_path] if dataset_path else []))
                    )
                )
                trained_families_used = {
                    fam for fam in (_infer_dataset_family_name(Path(p).name) for p in training_files_used) if fam
                }
                training_distribution_profile = _build_training_distribution_profile(
                    X_train,
                    list(getattr(preprocessor, 'feature_columns', [])),
                    max_features=24
                )

                effective_is_mega = bool(is_mega_model and len(trained_families_used) >= 2)
                if is_mega_model and not effective_is_mega:
                    st.warning(
                        "Mega-Model РјС–СЃС‚РёС‚СЊ Р»РёС€Рµ РѕРґРЅРµ СЃС–РјРµР№СЃС‚РІРѕ РґР°РЅРёС…. "
                        "РњРѕРґРµР»СЊ Р±СѓРґРµ Р·Р±РµСЂРµР¶РµРЅР° СЏРє СЃРїРµС†С–Р°Р»С–СЃС‚, С‰РѕР± РЅРµ Р±Р»РѕРєСѓРІР°С‚Рё РЅР°РІС‡Р°РЅРЅСЏ."
                    )

                quality_gate = _evaluate_training_quality_gate(
                    metrics,
                    is_isolation_algorithm=is_isolation_algorithm,
                    two_stage_mode=two_stage_mode,
                    is_mega_model=effective_is_mega,
                    trained_family_count=len(trained_families_used),
                    training_file_count=len(training_files_used)
                )

                progress.progress(92)

                save_name = f"{model_name}.joblib"
                schema_mode = "unified" if align_to_schema else "family"
                model_metadata = {
                    'algorithm': algorithm,
                    'model_type': ALGORITHM_WIKI[algorithm].get('model_type', 'classification'),
                    'training_strategy': (
                        training_strategy
                        if effective_is_mega
                        else "РЎРїРµС†С–Р°Р»С–СЃС‚ (Р°РІС‚Рѕ-С„РѕР»Р±РµРє Р· Mega)"
                    ),
                    'ui_mode': training_ui_mode,
                    'two_stage_mode': two_stage_mode,
                    'turbo_mode': turbo_mode,
                    'schema_mode': schema_mode,
                    'training_files': [str(p) for p in training_files_used],
                    'trained_families': sorted(trained_families_used),
                    'training_distribution_profile': training_distribution_profile,
                    'compatible_file_types': (
                        sorted(SUPPORTED_SCAN_EXTENSIONS)
                        if is_isolation_algorithm
                        else sorted(TABULAR_EXTENSIONS)
                    ),
                    'description': f"РњРѕРґРµР»СЊ {algorithm} РґР»СЏ РІРёСЏРІР»РµРЅРЅСЏ {'Р°РЅРѕРјР°Р»С–Р№' if is_isolation_algorithm else 'Р°С‚Р°Рє'}"
                }
                if is_isolation_algorithm:
                    model_metadata['if_contamination'] = float(if_effective_contamination)
                    model_metadata['if_auto_calibration'] = bool(if_auto_calibration)
                    model_metadata['if_target_fp_rate'] = float(if_target_fp_rate)
                    model_metadata['if_threshold_mode'] = getattr(engine, 'if_threshold_mode_', 'decision_zero')
                elif two_stage_mode:
                    model_metadata['two_stage_threshold_default'] = float(two_stage_threshold_info.get('threshold', DEFAULT_SENSITIVITY_THRESHOLD))
                    # Р”РµС„РѕР»С‚РЅРёР№ РїСЂРѕС„С–Р»СЊ РјР°С” Р±СѓС‚Рё СЃС‚Р°Р±С–Р»СЊРЅРѕ "balanced":
                    # strict РєРѕСЂРёСЃС‚СѓРІР°С‡ РІРјРёРєР°С” СЃРІС–РґРѕРјРѕ РїС–Рґ СЃРІС–Р№ СЃС†РµРЅР°СЂС–Р№.
                    model_metadata['two_stage_profile_default'] = DEFAULT_TWO_STAGE_PROFILE
                    model_metadata['two_stage_threshold_strict'] = _resolve_two_stage_profile_threshold(
                        float(model_metadata['two_stage_threshold_default']),
                        "strict"
                    )
                    model_metadata['two_stage_threshold_calibration'] = {
                        'f1_attack': float(two_stage_threshold_info.get('f1_attack', 0.0)),
                        'f2_attack': float(two_stage_threshold_info.get('f2_attack', 0.0)),
                        'precision_attack': float(two_stage_threshold_info.get('precision_attack', 0.0)),
                        'recall_attack': float(two_stage_threshold_info.get('recall_attack', 0.0)),
                        'evaluated_points': int(two_stage_threshold_info.get('evaluated_points', 0)),
                        'objective': float(two_stage_threshold_info.get('objective', 0.0)),
                    }
                    if hasattr(engine.model, 'stage2_sampling_info_'):
                        model_metadata['two_stage_stage2_balancing'] = dict(getattr(engine.model, 'stage2_sampling_info_', {}))
                model_metadata['quality_gate'] = quality_gate
                model_metadata['validation_metrics'] = {
                    'accuracy': float(metrics.get('accuracy', 0.0)),
                    'precision': float(metrics.get('precision', 0.0)),
                    'recall': float(metrics.get('recall', 0.0)),
                    'f1': float(metrics.get('f1', 0.0)),
                    'attack_precision': float(metrics.get('attack_precision', metrics.get('precision', 0.0))),
                    'attack_recall': float(metrics.get('attack_recall', metrics.get('recall', 0.0))),
                    'attack_f1': float(metrics.get('attack_f1', metrics.get('f1', 0.0))),
                    'attack_rate_test': float(metrics.get('attack_rate_test', 0.0)),
                    'attack_rate_pred': float(metrics.get('attack_rate_pred', 0.0)),
                }

                if not quality_gate.get('passed', False):
                    quality_score = int(quality_gate.get('score', 0))
                    allow_override = bool(is_mega_model or quality_score >= 55)
                    if not allow_override:
                        st.error("Quality Gate РЅРµ РїСЂРѕР№РґРµРЅРѕ. РњРѕРґРµР»СЊ РЅРµ Р·Р±РµСЂРµР¶РµРЅРѕ.")
                        st.caption(
                            f"РћС†С–РЅРєР° Quality Gate: {quality_score}/100. "
                            "РЎРєРѕСЂРёРіСѓР№С‚Рµ РґР°РЅС– Р°Р±Рѕ РїР°СЂР°РјРµС‚СЂРё С‚СЂРµРЅСѓРІР°РЅРЅСЏ."
                        )


--- Output 3 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
import streamlit as st
import pandas as pd
import numpy as np
from pathlib import Path
from typing import Any

from src.core.data_loader import DataLoader
from src.core.preprocessor import Preprocessor
from src.core.two_stage_model import TwoStageModel

from src.ui.utils.model_helpers import (
    TABULAR_EXTENSIONS,
    PCAP_EXTENSIONS,
    SUPPORTED_SCAN_EXTENSIONS,
    BENIGN_LABEL_TOKENS,
    DEFAULT_SENSITIVITY_THRESHOLD,
    TWO_STAGE_THRESHOLD_MIN,
    TWO_STAGE_THRESHOLD_MAX,
    TWO_STAGE_THRESHOLD_STEP,
    _clamp_two_stage_threshold,
)

ROOT_DIR = Path(__file__).resolve().parent.parent.parent.parent

def _to_attack_binary(labels: pd.Series) -> np.ndarray:
    normalized = labels.astype(str).str.strip().str.lower()
    return (~normalized.isin(BENIGN_LABEL_TOKENS)).astype(int).to_numpy(dtype=int)


def _load_if_external_calibration(
    loader: DataLoader,
    preprocessor: Preprocessor,
    exclude_path: Path | None = None,
    max_files: int = 3,
    max_rows_per_file: int = 8000,
) -> tuple[pd.DataFrame | None, np.ndarray | None]:
    """
    Load additional labeled attack samples for IF threshold calibration.
    Used only when the primary training split has no attack labels.
    """
    ready_dir = ROOT_DIR / 'datasets' / 'Training_Ready'
    if not ready_dir.exists():
        return None, None

    candidates = [
        f for f in sorted(ready_dir.glob('*.*'))
        if f.suffix.lower() in TABULAR_EXTENSIONS
    ]
    if exclude_path is not None:
        candidates = [f for f in candidates if f.resolve() != exclude_path.resolve()]

    # Prefer files that likely contain attacks by filename heuristic.
    attack_first = sorted(
        candidates,
        key=lambda f: ('normal' in f.name.lower() or 'benign' in f.name.lower(), f.name.lower())
    )

    X_parts: list[pd.DataFrame] = []
    y_parts: list[np.ndarray] = []

    for file_path in attack_first:
        if len(X_parts) >= max_files:
            break
        try:
            df_cal = loader.load_file(str(file_path), max_rows=max_rows_per_file, multiclass=False)
            if 'label' not in df_cal.columns:
                continue
            y_attack = _to_attack_binary(df_cal['label'])
            if int(np.sum(y_attack == 1)) == 0:
                continue

            X_cal = preprocessor.transform(df_cal.drop(columns=['label']))
            if len(X_cal) == 0:
                continue

            X_parts.append(X_cal)
            y_parts.append(y_attack[:len(X_cal)])
        except Exception:
            continue

    if not X_parts:
        return None, None

    X_full = pd.concat(X_parts, ignore_index=True)
    y_full = np.concatenate(y_parts)
    if len(y_full) != len(X_full):
        n = min(len(y_full), len(X_full))
        X_full = X_full.iloc[:n].copy()
        y_full = y_full[:n]

    if len(y_full) == 0:
        return None, None

    return X_full, y_full

def _resolve_normal_label_ids(label_map: dict[int, Any]) -> list[int]:
    normal_ids = [
        class_id
        for class_id, class_name in label_map.items()
        if str(class_name).strip().lower() in BENIGN_LABEL_TOKENS
    ]
    return normal_ids or [0]


def _calibrate_two_stage_threshold(
    model: TwoStageModel,
    X_val: pd.DataFrame,
    y_val: pd.Series,
    benign_code: int | None = None
) -> dict[str, Any]:
    """
    Auto-select the best Two-Stage binary threshold on validation data.
    Objective: maximize attack F2 (recall-oriented) with precision guard.
    """
    from sklearn.metrics import f1_score, precision_score, recall_score

    fallback_threshold = _clamp_two_stage_threshold(
        float(getattr(model, 'binary_threshold', DEFAULT_SENSITIVITY_THRESHOLD))
    )
    result: dict[str, Any] = {
        'threshold': fallback_threshold,
        'f1_attack': 0.0,
        'f2_attack': 0.0,
        'precision_attack': 0.0,
        'recall_attack': 0.0,
        'evaluated_points': 0,
        'objective': 0.0,
    }

    if X_val is None or y_val is None or len(X_val) == 0 or len(y_val) == 0:
        model.binary_threshold = fallback_threshold
        result['reason'] = 'empty_validation'
        return result

    benign_code_resolved = benign_code
    if benign_code_resolved is None:
        benign_code_resolved = getattr(model, 'benign_code_', None)
    if benign_code_resolved is None:
        benign_code_resolved = 0

    y_true_attack = (np.asarray(y_val) != benign_code_resolved).astype(int)
    if len(np.unique(y_true_attack)) < 2:
        # Not enough class variance for stable threshold calibration.
        model.binary_threshold = fallback_threshold
        result['reason'] = 'single_class_validation'
        return result

    attack_rate = float(np.mean(y_true_attack))
    # Adaptive FP target and guards for low-prevalence datasets.
    if attack_rate < 0.02:
        target_fp_rate = 0.005
        precision_floor = 0.75
        max_attack_rate = min(0.08, (attack_rate * 2.5) + 0.02)
        fp_penalty_weight = 1.6
        over_pred_weight = 0.7
        precision_weight = 0.45
    elif attack_rate < 0.05:
        target_fp_rate = 0.01
        precision_floor = 0.70
        max_attack_rate = min(0.12, (attack_rate * 2.0) + 0.03)
        fp_penalty_weight = 1.2
        over_pred_weight = 0.55
        precision_weight = 0.40
    elif attack_rate < 0.10:
        target_fp_rate = 0.02
        precision_floor = 0.65
        max_attack_rate = min(0.20, (attack_rate * 1.8) + 0.04)
        fp_penalty_weight = 1.0
        over_pred_weight = 0.40
        precision_weight = 0.38
    else:
        target_fp_rate = 0.03 if attack_rate < 0.25 else 0.04
        precision_floor = 0.60
        max_attack_rate = min(0.60, (attack_rate * 1.6) + 0.05)
        fp_penalty_weight = 0.9
        over_pred_weight = 0.25
        precision_weight = 0.35
    max_attack_rate = float(np.clip(max_attack_rate, 0.01, 0.90))

    best = {
        'threshold': fallback_threshold,
        'f1_attack': -1.0,
        'f2_attack': -1.0,
        'precision_attack': 0.0,
        'recall_attack': 0.0,
        'objective': -1e9,
    }

    grid = np.arange(
        TWO_STAGE_THRESHOLD_MIN,
        TWO_STAGE_THRESHOLD_MAX + (TWO_STAGE_THRESHOLD_STEP / 2.0),
        TWO_STAGE_THRESHOLD_STEP
    )
    result['evaluated_points'] = int(len(grid))
    threshold_stats: list[dict[str, float]] = []

    for thr in grid:
        threshold = _clamp_two_stage_threshold(float(thr))
        y_pred = model.predict(X_val, threshold=threshold)
        y_pred_attack = (np.asarray(y_pred) != benign_code_resolved).astype(int)

        f1 = float(f1_score(y_true_attack, y_pred_attack, zero_division=0))
        precision = float(precision_score(y_true_attack, y_pred_attack, zero_division=0))
        recall = float(recall_score(y_true_attack, y_pred_attack, zero_division=0))
        fp_rate = 0.0
        pred_attack_rate = float(np.mean(y_pred_attack)) if len(y_pred_attack) else 0.0
        if len(y_true_attack):
            tn = int(np.sum((y_true_attack == 0) & (y_pred_attack == 0)))
            fp = int(np.sum((y_true_attack == 0) & (y_pred_attack == 1)))
            fp_rate = fp / (fp + tn) if (fp + tn) else 0.0
        denom = (4.0 * precision) + recall
        f2 = float((5.0 * precision * recall) / denom) if denom > 0 else 0.0

        # Soft guard against extremely low precision.
        precision_penalty = max(0.0, precision_floor - precision)
        fp_penalty = max(0.0, fp_rate - target_fp_rate)
        over_pred_penalty = max(0.0, pred_attack_rate - max_attack_rate)
        objective = (
            f2
            - (precision_penalty * precision_penalty * precision_weight)
            - (fp_penalty * fp_penalty_weight)
            - (over_pred_penalty * over_pred_weight)
        )
        threshold_stats.append(
            {
                'threshold': threshold,
                'f1_attack': f1,
                'f2_attack': f2,
                'precision_attack': precision,
                'recall_attack': recall,
                'fp_rate': fp_rate,
                'pred_attack_rate': pred_attack_rate,
                'objective': objective,
            }
        )

        better = False
        if objective > best['objective'] + 1e-12:
            better = True
        elif abs(objective - best['objective']) <= 1e-12 and recall > best['recall_attack'] + 1e-12:
            better = True
        elif (
            abs(objective - best['objective']) <= 1e-12
            and abs(recall - best['recall_attack']) <= 1e-12
            and abs(f1 - best['f1_attack']) <= 1e-12
            and abs(threshold - DEFAULT_SENSITIVITY_THRESHOLD) < abs(best['threshold'] - DEFAULT_SENSITIVITY_THRESHOLD)
        ):
            better = True

        if better:
            best = {
                'threshold': threshold,
                'f1_attack': f1,
                'f2_attack': f2,
                'precision_attack': precision,
                'recall_attack': recall,
                'objective': objective,
            }

    # РЎС‚Р°Р±С–Р»С–Р·Р°С†С–СЏ РїРѕСЂРѕРіСѓ:
    # СЏРєС‰Рѕ РєС–Р»СЊРєР° РїРѕСЂРѕРіС–РІ РјР°Р№Р¶Рµ СЂС–РІРЅРѕС†С–РЅРЅС– Р·Р° F2, РѕР±РёСЂР°С”РјРѕ С‚РѕР№, С‰Рѕ Р±Р»РёР¶С‡Рµ
    # РґРѕ РґРµС„РѕР»С‚РЅРѕРіРѕ С– Р·Р°Р·РІРёС‡Р°Р№ РґР°С” СЃС‚Р°Р±С–Р»СЊРЅС–С€РёР№ Р±Р°Р»Р°РЅСЃ FP/FN.
    if threshold_stats:
        f2_tolerance = 0.002
        precision_tolerance = 0.03
        stable_pool = [
            item for item in threshold_stats
            if item['f2_attack'] >= (best['f2_attack'] - f2_tolerance)
            and item['precision_attack'] >= max(0.0, best['precision_attack'] - precision_tolerance)
        ]
        if stable_pool:
            stable_choice = min(
                stable_pool,
                key=lambda item: (
                    abs(item['threshold'] - DEFAULT_SENSITIVITY_THRESHOLD),
                    -item['recall_attack'],
                    -item['f2_attack']
                )
            )
            best = stable_choice

    # Enforce FP guard using benign-only quantile if needed.
    try:
        probas = model.binary_model.predict_proba(X_val)
        attack_idx = getattr(model, 'attack_idx_', None)
        if attack_idx is None:
            attack_idx = int(np.where(model.binary_model.classes_ == 1)[0][0]) if hasattr(model.binary_model, 'classes_') else 1
        attack_probs = probas[:, attack_idx]
        benign_mask = (y_true_attack == 0)
        if benign_mask.any():
            fp_threshold = float(np.quantile(attack_probs[benign_mask], 1.0 - target_fp_rate))
            fp_threshold = _clamp_two_stage_threshold(fp_threshold)
            if fp_threshold > best['threshold']:
                best['threshold'] = fp_threshold
        # Guard predicted attack rate on validation set (use all samples).
        rate_threshold = float(np.quantile(attack_probs, 1.0 - max_attack_rate))
        rate_threshold = _clamp_two_stage_threshold(rate_threshold)
        if rate_threshold > best['threshold']:
            best['threshold'] = rate_threshold
    except Exception:
        pass

    model.binary_threshold = float(best['threshold'])
    # Refresh metrics for the final threshold.
    y_pred_final = model.predict(X_val, threshold=model.binary_threshold)
    y_pred_attack_final = (np.asarray(y_pred_final) != benign_code_resolved).astype(int)
    result.update({
        'threshold': float(model.binary_threshold),
        'f1_attack': float(f1_score(y_true_attack, y_pred_attack_final, zero_division=0)),
        'f2_attack': float(best.get('f2_attack', 0.0)),
        'precision_attack': float(precision_score(y_true_attack, y_pred_attack_final, zero_division=0)),
        'recall_attack': float(recall_score(y_true_attack, y_pred_attack_final, zero_division=0)),
        'evaluated_points': int(result.get('evaluated_points', 0)),
        'objective': float(best.get('objective', 0.0)),
        'target_fp_rate': float(target_fp_rate),
        'max_attack_rate': float(max_attack_rate),
        'precision_floor': float(precision_floor),
    })
    return result


def _evaluate_training_quality_gate(
    metrics: dict[str, Any],
    *,
    is_isolation_algorithm: bool,
    two_stage_mode: bool,
    is_mega_model: bool,
    trained_family_count: int = 0,
    training_file_count: int = 1
) -> dict[str, Any]:
    """
    РЇРєС–СЃРЅРёР№ С„С–Р»СЊС‚СЂ РїРµСЂРµРґ Р·Р±РµСЂРµР¶РµРЅРЅСЏРј РјРѕРґРµР»С–.
    РћС†С–РЅСЋС” Р·РґР°С‚РЅС–СЃС‚СЊ Р·РЅР°С…РѕРґРёС‚Рё Р°С‚Р°РєРё, Р° РЅРµ Р»РёС€Рµ Р·Р°РіР°Р»СЊРЅСѓ С‚РѕС‡РЅС–СЃС‚СЊ.
    """
    def _safe(value: Any) -> float:
        try:
            v = float(value)
            if not np.isfinite(v):
                return 0.0
            return v
        except Exception:
            return 0.0

    observed = {
        'accuracy': _safe(metrics.get('accuracy', 0.0)),
        'precision': _safe(metrics.get('precision', 0.0)),
        'recall': _safe(metrics.get('recall', 0.0)),
        'f1': _safe(metrics.get('f1', 0.0)),
        'attack_precision': _safe(metrics.get('attack_precision', metrics.get('precision', 0.0))),
        'attack_recall': _safe(metrics.get('attack_recall', metrics.get('recall', 0.0))),
        'attack_f1': _safe(metrics.get('attack_f1', metrics.get('f1', 0.0))),
        'attack_rate_test': _safe(metrics.get('attack_rate_test', 0.0)),
        'attack_rate_pred': _safe(metrics.get('attack_rate_pred', 0.0)),
        'unique_classes_train': int(_safe(metrics.get('unique_classes_train', 0))),
        'unique_classes_test': int(_safe(metrics.get('unique_classes_test', 0))),
        'min_class_ratio_train': _safe(metrics.get('min_class_ratio_train', 1.0)),
    }

    attack_rate = observed['attack_rate_test']
    if is_isolation_algorithm:
        thresholds = {
            'min_accuracy': 0.50,
            'min_attack_precision': 0.08,
            'min_attack_recall': 0.20,
            'min_attack_f1': 0.15,
        }
    else:
        if attack_rate < 0.02:
            thresholds = {
                'min_accuracy': 0.60,
                'min_attack_precision': 0.15,
                'min_attack_recall': 0.25,
                'min_attack_f1': 0.20,
            }
        elif attack_rate < 0.10:
            thresholds = {
                'min_accuracy': 0.60,
                'min_attack_precision': 0.30,
                'min_attack_recall': 0.40,
                'min_attack_f1': 0.35,
            }
        else:
            thresholds = {
                'min_accuracy': 0.62,
                'min_attack_precision': 0.50,
                'min_attack_recall': 0.55,
                'min_attack_f1': 0.55,
            }

        if two_stage_mode:
            thresholds['min_attack_recall'] = min(0.95, thresholds['min_attack_recall'] + 0.05)
        if is_mega_model:
            thresholds['min_attack_precision'] = max(0.20, thresholds['min_attack_precision'] - 0.10)
            thresholds['min_attack_recall'] = max(0.35, thresholds['min_attack_recall'] - 0.10)
            thresholds['min_attack_f1'] = max(0.40, thresholds['min_attack_f1'] - 0.10)

    failures: list[str] = []

    if not is_isolation_algorithm and observed['unique_classes_train'] < 2:
        failures.append("РЈ С‚СЂРµРЅСѓРІР°РЅРЅС– РјРµРЅС€Рµ 2 РєР»Р°СЃС–РІ. РљР»Р°СЃРёС„С–РєР°С‚РѕСЂ РЅРµРІР°Р»С–РґРЅРёР№.")
    if not is_isolation_algorithm and observed['unique_classes_test'] < 2:
        failures.append("РЈ РІР°Р»С–РґР°С†С–С— РјРµРЅС€Рµ 2 РєР»Р°СЃС–РІ. РћС†С–РЅРєР° РјРѕРґРµР»С– РЅРµРЅР°РґС–Р№РЅР°.")
    if two_stage_mode and observed['unique_classes_train'] < 3:
        failures.append("Р”Р»СЏ Two-Stage РїРѕС‚СЂС–Р±РЅРѕ С‰РѕРЅР°Р№РјРµРЅС€Рµ 3 РєР»Р°СЃРё (BENIGN + 2 С‚РёРїРё Р°С‚Р°Рє).")
    if is_mega_model and trained_family_count < 2:
        failures.append("Mega-Model РїРѕРІРёРЅРµРЅ РјС–СЃС‚РёС‚Рё С‰РѕРЅР°Р№РјРµРЅС€Рµ 2 СЂС–Р·РЅС– СЃС–РјРµР№СЃС‚РІР° РґР°РЅРёС….")
    if is_mega_model and training_file_count < 2:
        failures.append("Р”Р»СЏ Mega-Model Р·Р°РјР°Р»Рѕ С„Р°Р№Р»С–РІ. Р”РѕРґР°Р№С‚Рµ Р±С–Р»СЊС€Рµ РЅР°РІС‡Р°Р»СЊРЅРёС… РІРёР±С–СЂРѕРє.")
    if is_mega_model and trained_family_count >= 2 and observed['attack_recall'] < 0.40:
        failures.append("Р”Р»СЏ РєСЂРѕСЃ-РґРѕРјРµРЅРЅРѕРіРѕ Mega-Model РїРѕРІРЅРѕС‚Р° РїРѕ Р°С‚Р°РєР°С… РјР°С” Р±СѓС‚Рё РЅРµ РЅРёР¶С‡Рµ 0.40.")

    if observed['accuracy'] < thresholds['min_accuracy']:
        failures.append(
            f"РўРѕС‡РЅС–СЃС‚СЊ (accuracy) {observed['accuracy']:.3f} < {thresholds['min_accuracy']:.3f}"
        )
    if observed['attack_precision'] < thresholds['min_attack_precision']:
        failures.append(
            f"РўРѕС‡РЅС–СЃС‚СЊ РїРѕ Р°С‚Р°РєР°С… {observed['attack_precision']:.3f} < {thresholds['min_attack_precision']:.3f}"
        )
    if observed['attack_recall'] < thresholds['min_attack_recall']:


--- Output 4 ---
Exit code: 0
Wall time: 0.5 seconds
Output:
        failures.append(
            f"РџРѕРІРЅРѕС‚Р° РїРѕ Р°С‚Р°РєР°С… {observed['attack_recall']:.3f} < {thresholds['min_attack_recall']:.3f}"
        )
    if observed['attack_f1'] < thresholds['min_attack_f1']:
        failures.append(
            f"F1 РїРѕ Р°С‚Р°РєР°С… {observed['attack_f1']:.3f} < {thresholds['min_attack_f1']:.3f}"
        )

    # Р—Р°С…РёСЃС‚ РІС–Рґ РїР°С‚РѕР»РѕРіС–С‡РЅРѕС— РїРѕРІРµРґС–РЅРєРё: РјРѕРґРµР»СЊ РІС–РґРјС–С‡Р°С” РјР°Р№Р¶Рµ РІСЃРµ Р°Р±Рѕ РјР°Р№Р¶Рµ РЅС–С‡РѕРіРѕ.
    if observed['attack_rate_test'] > 0.0:
        pred_rate = observed['attack_rate_pred']
        if pred_rate <= 0.0001:
            failures.append("РњРѕРґРµР»СЊ РјР°Р№Р¶Рµ РЅРµ РїРѕР·РЅР°С‡Р°С” Р°С‚Р°РєРё РЅР° РІР°Р»С–РґР°С†С–С—.")
        elif pred_rate >= 0.995:
            failures.append("РњРѕРґРµР»СЊ РїРѕР·РЅР°С‡Р°С” РјР°Р№Р¶Рµ РІСЃС– Р·Р°РїРёСЃРё СЏРє Р°С‚Р°РєРё РЅР° РІР°Р»С–РґР°С†С–С—.")
        else:
            attack_rate = observed['attack_rate_test']
            if attack_rate < 0.02:
                low_ratio, high_ratio = 0.10, 8.0
            elif attack_rate < 0.10:
                low_ratio, high_ratio = 0.15, 6.0
            else:
                low_ratio, high_ratio = 0.25, 4.0
            ratio = pred_rate / max(attack_rate, 1e-9)
            if ratio < low_ratio or ratio > high_ratio:
                failures.append(
                    "РџРµСЂРµРѕС†С–РЅРєР°/РЅРµРґРѕРѕС†С–РЅРєР° Р°С‚Р°Рє: "
                    f"РїСЂРѕРіРЅРѕР·РѕРІР°РЅР° С‡Р°СЃС‚РєР° {pred_rate:.2%} vs С„Р°РєС‚РёС‡РЅР° {attack_rate:.2%}."
                )

    if observed['min_class_ratio_train'] < 0.001 and not is_isolation_algorithm:
        failures.append("Р”СѓР¶Рµ СЂС–РґРєС–СЃРЅС– РєР»Р°СЃРё Сѓ train (<0.1%). РџРѕС‚СЂС–Р±РЅР° СЂРµР±Р°Р»Р°РЅСЃРёСЂРѕРІРєР° Р°Р±Рѕ Р±С–Р»СЊС€Рµ РґР°РЅРёС….")

    score = 100
    for key, observed_key in (
        ('min_accuracy', 'accuracy'),
        ('min_attack_precision', 'attack_precision'),
        ('min_attack_recall', 'attack_recall'),
        ('min_attack_f1', 'attack_f1'),
    ):
        gap = thresholds[key] - observed[observed_key]
        if gap > 0:
            score -= int(np.ceil(gap * 100))

    # Р”РѕРґР°С‚РєРѕРІС– С€С‚СЂР°С„Рё Р·Р° СЃС‚СЂСѓРєС‚СѓСЂРЅС– СЂРёР·РёРєРё.
    if not is_isolation_algorithm and observed['unique_classes_train'] < 2:
        score -= 35
    if not is_isolation_algorithm and observed['unique_classes_test'] < 2:
        score -= 30
    if two_stage_mode and observed['unique_classes_train'] < 3:
        score -= 25
    if is_mega_model and trained_family_count < 2:
        score -= 20
    if is_mega_model and training_file_count < 2:
        score -= 10
    if is_mega_model and trained_family_count >= 2 and observed['attack_recall'] < 0.40:
        score -= 12
    if observed['min_class_ratio_train'] < 0.001 and not is_isolation_algorithm:
        score -= 12
    if observed['attack_rate_test'] > 0:
        pred_rate = observed['attack_rate_pred']
        attack_rate = observed['attack_rate_test']
        ratio = pred_rate / max(attack_rate, 1e-9)
        if ratio < 0.15 or ratio > 6.0:
            score -= 10

    score = int(np.clip(score, 0, 100))

    return {
        'passed': len(failures) == 0,
        'score': score,
        'thresholds': thresholds,
        'observed': observed,
        'failures': failures,
    }


def _filename_looks_normal(name: str) -> bool:
    lowered = name.lower()
    normal_tokens = ["normal", "benign", "С‡РёСЃС‚", "РЅРѕСЂРјР°Р»СЊ", "safe"]
    return any(token in lowered for token in normal_tokens)


def _find_training_ready_files() -> tuple[list[Path], list[Path], list[Path], list[Path]]:
    ready_dir = ROOT_DIR / 'datasets' / 'Training_Ready'
    if not ready_dir.exists():
        return [], [], [], []

    all_files = [
        f for f in sorted(ready_dir.glob('*.*'))
        if f.suffix.lower() in TABULAR_EXTENSIONS or f.suffix.lower() in PCAP_EXTENSIONS
    ]
    tabular_files = [f for f in all_files if f.suffix.lower() in TABULAR_EXTENSIONS]
    pcap_files = [f for f in all_files if f.suffix.lower() in PCAP_EXTENSIONS]
    normal_files = [f for f in tabular_files if _filename_looks_normal(f.name)]
    attack_files = [f for f in tabular_files if f not in normal_files]
    return tabular_files, normal_files, attack_files, pcap_files


@st.cache_data(show_spinner=False)
def assess_training_file_compatibility(
    file_path: str,
    file_mtime: float,
    file_size: int
) -> dict[str, Any]:
    """
    Check whether a training file can be processed by our pipeline.
    Also reports mode constraints (supervised vs IF-only).
    """
    path = Path(file_path)
    ext = path.suffix.lower()
    result: dict[str, Any] = {
        'file': path.name,
        'path': file_path,
        'ext': ext,
        'compatible': False,
        'reason': '',
        'is_pcap': ext in PCAP_EXTENSIONS,
        'has_label': False,
        'allowed_modes': []
    }

    if ext not in SUPPORTED_SCAN_EXTENSIONS:
        result['reason'] = f"РќРµРїС–РґС‚СЂРёРјСѓРІР°РЅРёР№ С„РѕСЂРјР°С‚ `{ext}`."
        return result

    if ext in PCAP_EXTENSIONS:
        result['compatible'] = True
        result['allowed_modes'] = ['if_unsupervised']
        result['reason'] = "PCAP СЃСѓРјС–СЃРЅРёР№ С‚С–Р»СЊРєРё Р· Isolation Forest."
        return result

    # Tabular formats (CSV/NF/NFDUMP): РјР°РєСЃРёРјР°Р»СЊРЅРѕ Р»РµРіРєРёР№ preview Р±РµР· Р·Р°РїСѓСЃРєСѓ РїРѕРІРЅРѕРіРѕ РїР°Р№РїР»Р°Р№РЅР°.
    preview = None
    read_attempts = [
        {"encoding": "utf-8", "engine": "c"},
        {"encoding": "utf-8", "engine": "python", "on_bad_lines": "skip"},
        {"encoding": "cp1251", "engine": "python", "on_bad_lines": "skip"},
        {"encoding": "latin1", "engine": "python", "on_bad_lines": "skip"},
    ]
    for attempt in read_attempts:
        try:
            kwargs = {
                "nrows": 1200,
                "low_memory": False,
                "skipinitialspace": True,
                **attempt,
            }
            preview = pd.read_csv(file_path, **kwargs)
            if preview is not None and len(preview) > 0:
                break
        except Exception:
            preview = None

    if preview is None:
        try:
            loader = DataLoader()
            preview = loader.load_file(
                file_path,
                max_rows=300,
                multiclass=False,
                align_to_schema=False,
                preserve_context=False
            )
        except Exception as exc:
            result['reason'] = f"Р¤Р°Р№Р» РЅРµ РІРґР°Р»РѕСЃСЏ РѕР±СЂРѕР±РёС‚Рё: {exc}"
            return result

    if preview is None or len(preview) == 0:
        result['reason'] = "Р¤Р°Р№Р» РїРѕСЂРѕР¶РЅС–Р№ Р°Р±Рѕ РЅРµ РјС–СЃС‚РёС‚СЊ РІР°Р»С–РґРЅРёС… Р·Р°РїРёСЃС–РІ."
        return result

    columns_lower = {str(col).strip().lower() for col in preview.columns}
    label_candidates = {
        'label', 'labels', 'class', 'target', 'y',
        'attack_cat', 'attack_type', 'status'
    }
    has_label = bool(columns_lower.intersection(label_candidates))
    result['has_label'] = has_label

    if has_label:
        result['compatible'] = True
        result['allowed_modes'] = ['supervised', 'if_unsupervised']
        return result

    result['compatible'] = True
    result['allowed_modes'] = ['if_unsupervised']
    result['reason'] = "РЈ С„Р°Р№Р»С– РЅРµРјР°С” label вЂ” РґРѕСЃС‚СѓРїРЅРµ Р»РёС€Рµ IF (unsupervised)."
    return result


--- Output 5 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
import streamlit as st
import pandas as pd
import numpy as np
import joblib
from pathlib import Path
from typing import Any

from src.core.data_loader import DataLoader
from src.core.dataset_detector import DatasetDetector

# Core Extensions
PCAP_EXTENSIONS = {'.pcap', '.pcapng', '.cap'}
NETFLOW_EXTENSIONS = {'.nf', '.nfdump'}
TABULAR_EXTENSIONS = {'.csv'} | NETFLOW_EXTENSIONS
SUPPORTED_SCAN_EXTENSIONS = TABULAR_EXTENSIONS | PCAP_EXTENSIONS

# Thresholds & Constraints
DEFAULT_SENSITIVITY_THRESHOLD = 0.3
TWO_STAGE_THRESHOLD_MIN = 0.01
TWO_STAGE_THRESHOLD_MAX = 0.99
TWO_STAGE_THRESHOLD_STEP = 0.01
DEFAULT_SENSITIVITY_LEVEL = int(round((1.0 - DEFAULT_SENSITIVITY_THRESHOLD) * 100))
DEFAULT_IF_CONTAMINATION = 0.10
DEFAULT_IF_TARGET_FP_RATE = 0.01
BENIGN_LABEL_TOKENS = {'0', '0.0', 'benign', 'normal', 'normal.', 'РЅРѕСЂРјР°'}
DEFAULT_TWO_STAGE_PROFILE = "balanced"
TWO_STAGE_PROFILE_ORDER = ("balanced", "strict")
TWO_STAGE_PROFILE_RULES = {
    "balanced": {
        "label": "Р—Р±Р°Р»Р°РЅСЃРѕРІР°РЅРёР№",
        "description": "Р‘Р°Р·РѕРІРёР№ СЂРµР¶РёРј РґР»СЏ С‰РѕРґРµРЅРЅРѕРіРѕ СЃРєР°РЅСѓРІР°РЅРЅСЏ: Р±Р°Р»Р°РЅСЃ РјС–Р¶ FP/FN.",
    },
    "strict": {
        "label": "РЎС‚СЂРѕРіРёР№ (РјРµРЅС€Рµ FP)",
        "description": "Р—РЅРёР¶СѓС” РєС–Р»СЊРєС–СЃС‚СЊ С…РёР±РЅРёС… С‚СЂРёРІРѕРі, Р°Р»Рµ РјРѕР¶Рµ РїСЂРѕРїСѓСЃРєР°С‚Рё СЃР»Р°Р±РєС– Р°С‚Р°РєРё.",
    },
}

def _clamp_two_stage_threshold(threshold: float) -> float:
    return float(np.clip(float(threshold), TWO_STAGE_THRESHOLD_MIN, TWO_STAGE_THRESHOLD_MAX))

def _threshold_to_sensitivity_level(threshold: float) -> int:
    threshold = _clamp_two_stage_threshold(threshold)
    level = int(round((1.0 - threshold) * 100.0))
    return int(np.clip(level, 1, 99))

def _sensitivity_level_to_threshold(level: int) -> float:
    level = int(np.clip(int(level), 1, 99))
    threshold = 1.0 - (level / 100.0)
    return _clamp_two_stage_threshold(threshold)

def _normalize_two_stage_profile(profile: Any) -> str:
    profile_value = str(profile).strip().lower().replace('-', '_')
    if profile_value in TWO_STAGE_PROFILE_RULES:
        return profile_value
    return DEFAULT_TWO_STAGE_PROFILE

def _resolve_two_stage_profile_threshold(default_threshold: float, profile: str) -> float:
    base_threshold = _clamp_two_stage_threshold(default_threshold)
    profile_key = _normalize_two_stage_profile(profile)

    if profile_key == "balanced":
        return base_threshold

    strict_threshold = min(0.90, max(base_threshold + 0.20, 0.65))
    return _clamp_two_stage_threshold(strict_threshold)

def _infer_dataset_family_name(path_or_name: str) -> str:
    name = str(path_or_name).lower()
    if "unsw" in name or "nb15" in name:
        return "UNSW-NB15"
    if "nsl" in name or "kdd" in name:
        return "NSL-KDD"
    if "cic" in name or "ids2017" in name or "ids2018" in name or "iscx" in name:
        return "CIC-IDS"
    return ""

def _normalize_compatible_types(raw_types: Any) -> list[str]:
    if not isinstance(raw_types, (list, tuple, set)):
        raw_types = sorted(TABULAR_EXTENSIONS)

    normalized = []
    for raw in raw_types:
        ext = str(raw).strip().lower()
        if not ext:
            continue
        if not ext.startswith('.'):
            ext = f'.{ext}'
        normalized.append(ext)

    return normalized or sorted(TABULAR_EXTENSIONS)

@st.cache_data(show_spinner=False)
def load_model_manifest(model_path: str, mtime: float, size: int) -> dict:
    """
    Load only lightweight compatibility hints from a model file.
    mtime/size are included to invalidate cache when file changes.
    """
    manifest = {
        'algorithm': '',
        'two_stage_mode': False,
        'is_isolation_forest': False,
        'compatible_file_types': sorted(TABULAR_EXTENSIONS),
        'two_stage_threshold_default': float(DEFAULT_SENSITIVITY_THRESHOLD),
        'two_stage_sensitivity_default': int(np.clip(DEFAULT_SENSITIVITY_LEVEL, 1, 99)),
        'two_stage_profile_default': DEFAULT_TWO_STAGE_PROFILE,
        'two_stage_threshold_strict': float(np.clip(DEFAULT_SENSITIVITY_THRESHOLD, TWO_STAGE_THRESHOLD_MIN, TWO_STAGE_THRESHOLD_MAX)),
        'trained_families': [],
        'training_file_count': 0,
        'schema_mode': 'unified'
    }

    try:
        loaded = joblib.load(model_path)
    except Exception:
        return manifest

    if not isinstance(loaded, dict):
        return manifest

    metadata = loaded.get('metadata') if isinstance(loaded.get('metadata'), dict) else {}
    algorithm_meta = str(metadata.get('algorithm', '')).strip()
    algorithm_name = str(loaded.get('algorithm_name') or algorithm_meta).strip()

    two_stage_mode = bool(metadata.get('two_stage_mode', False))
    if not two_stage_mode and 'Two-Stage' in algorithm_name:
        two_stage_mode = True

    is_isolation_forest = ('Isolation Forest' in algorithm_name) or ('Isolation Forest' in algorithm_meta)

    compatible_types = _normalize_compatible_types(metadata.get('compatible_file_types'))
    if 'compatible_file_types' not in metadata:
        compatible_types = sorted(SUPPORTED_SCAN_EXTENSIONS) if is_isolation_forest else sorted(TABULAR_EXTENSIONS)

    threshold_default = float(
        metadata.get(
            'two_stage_threshold_default',
            getattr(loaded.get('model'), 'binary_threshold', DEFAULT_SENSITIVITY_THRESHOLD)
        )
    )
    threshold_default = _clamp_two_stage_threshold(threshold_default)
    sensitivity_default = _threshold_to_sensitivity_level(threshold_default)
    profile_default = _normalize_two_stage_profile(
        metadata.get('two_stage_profile_default', DEFAULT_TWO_STAGE_PROFILE)
    )
    strict_threshold = _clamp_two_stage_threshold(
        float(
            metadata.get(
                'two_stage_threshold_strict',
                _resolve_two_stage_profile_threshold(threshold_default, "strict")
            )
        )
    )
    training_files = metadata.get('training_files', []) if isinstance(metadata, dict) else []
    if not isinstance(training_files, list):
        training_files = []
    trained_families_meta = metadata.get('trained_families', []) if isinstance(metadata, dict) else []
    if not isinstance(trained_families_meta, list):
        trained_families_meta = []
    trained_families = sorted(
        set([str(f).strip() for f in trained_families_meta if str(f).strip()])
        | {
            fam for fam in (_infer_dataset_family_name(Path(p).name) for p in training_files) if fam
        }
    )

    manifest.update({
        'algorithm': algorithm_meta or algorithm_name,
        'two_stage_mode': two_stage_mode,
        'is_isolation_forest': is_isolation_forest,
        'compatible_file_types': compatible_types,
        'two_stage_threshold_default': threshold_default,
        'two_stage_sensitivity_default': sensitivity_default,
        'two_stage_profile_default': profile_default,
        'two_stage_threshold_strict': strict_threshold,
        'trained_families': trained_families,
        'training_file_count': len(training_files),
        'schema_mode': str(metadata.get('schema_mode', 'unified')).strip().lower() if isinstance(metadata, dict) else 'unified'
    })

    return manifest

@st.cache_data(show_spinner=False)
def detect_scan_file_family_info(file_path: str, file_mtime: float, file_size: int) -> dict[str, Any]:
    """
    Р’РёР·РЅР°С‡Р°С” СЃС–РјРµР№СЃС‚РІРѕ РґР°РЅРёС… РґР»СЏ С„Р°Р№Р»Сѓ СЃРєР°РЅСѓРІР°РЅРЅСЏ С‚Р° РїРѕРІРµСЂС‚Р°С” confidence.
    """
    path = Path(file_path)
    by_name_family = _infer_dataset_family_name(path.name)
    result: dict[str, Any] = {
        'family': by_name_family,
        'confidence': 0.55 if by_name_family else 0.0,
        'ambiguous': False,
        'source': 'name' if by_name_family else 'none',
    }

    if path.suffix.lower() not in TABULAR_EXTENSIONS:
        return result

    try:
        # Р’РёР·РЅР°С‡РµРЅРЅСЏ СЃС–РјРµР№СЃС‚РІР° РјР°С” Р№С‚Рё РїРѕ СЃРёСЂРёС… РєРѕР»РѕРЅРєР°С…, Р±РµР· Unified Pipeline,
        # С–РЅР°РєС€Рµ РјР°РїС–РЅРі/Р°Р»Р°Р№РЅ РјРѕР¶Рµ "Р·Р°РјР°СЃРєСѓРІР°С‚Рё" СЃРїСЂР°РІР¶РЅС” РїРѕС…РѕРґР¶РµРЅРЅСЏ РґР°С‚Р°СЃРµС‚Сѓ.
        raw_sample = pd.read_csv(path, nrows=1500, low_memory=False)
        raw_sample.columns = raw_sample.columns.astype(str).str.strip()
        details = DatasetDetector().detect_with_confidence(raw_sample)
    except Exception:
        # Fallback РґР»СЏ РЅРµСЃС‚Р°РЅРґР°СЂС‚РЅРёС… CSV/NF: РїСЂРѕР±СѓС”РјРѕ С‡РµСЂРµР· DataLoader.
        try:
            loader = DataLoader()
            sample = loader.load_file(str(path), max_rows=1200, multiclass=False)
            if sample is None or len(sample) == 0:
                return result
            if 'label' in sample.columns:
                sample = sample.drop(columns=['label'])
            details = DatasetDetector().detect_with_confidence(sample)
        except Exception:
            return result

    detected = str(details.get('dataset', 'Unknown'))
    confidence = float(details.get('confidence', 0.0))
    ambiguous = bool(details.get('ambiguous', False))

    if detected and detected not in {'Unknown', 'Generic'}:
        result.update({
            'family': detected,
            'confidence': confidence,
            'ambiguous': ambiguous,
            'source': 'columns',
        })
    else:
        result.update({
            'confidence': max(result['confidence'], confidence),
            'ambiguous': ambiguous,
            'source': 'name_fallback' if result.get('family') else 'columns',
        })
    return result


def detect_scan_file_family(file_path: str, file_mtime: float, file_size: int) -> str:
    return str(detect_scan_file_family_info(file_path, file_mtime, file_size).get('family', ''))


--- Output 6 ---
Exit code: 0
Wall time: 2.9 seconds
Output:
benign_code 0
binary_threshold 0.3
binary_classes [0 1]
attack_idx 1
classes sample [ 0  1  2  4  5  6  7  8 12 13]
stage2 map len 9
stage2_sampling_info None
binary_sampling_info {'enabled': True, 'attack_rate_raw': 0.26483791051728883, 'target_rate': 0.2, 'used_attack_samples': 34720, 'used_benign_samples': 138878, 'downsampled': True}


--- Output 7 ---
Exit code: 1
Wall time: 10.9 seconds
Output:
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[Benign_Normal_Traffic].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[DDoS].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[Port_Scan].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[BruteForce-SSH_FTP].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[Web_Attacks-Brute_XSS_SQL].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
Loaded mix 5 => (20000, 45)
2026-03-25 23:00:01.980 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
2026-03-25 23:00:01.981 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
2026-03-25 23:00:01.982 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
Traceback (most recent call last):
  File "<stdin>", line 119, in <module>
  File "<stdin>", line 52, in train_twostage
  File "E:\IDS_ML_Analyzer\.venv\Lib\site-packages\sklearn\utils\_param_validation.py", line 218, in wrapper
    return func(*args, **kwargs)
  File "E:\IDS_ML_Analyzer\.venv\Lib\site-packages\sklearn\model_selection\_split.py", line 2945, in train_test_split
    train, test = next(cv.split(X=arrays[0], y=stratify))
                  ~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "E:\IDS_ML_Analyzer\.venv\Lib\site-packages\sklearn\model_selection\_split.py", line 1928, in split
    for train, test in self._iter_indices(X, y, groups):
                       ~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^
  File "E:\IDS_ML_Analyzer\.venv\Lib\site-packages\sklearn\model_selection\_split.py", line 2346, in _iter_indices
    raise ValueError(
    ...<5 lines>...
    )
ValueError: The least populated classes in y have only 1 member, which is too few. The minimum number of groups for any class cannot be less than 2. Classes with too few members are: [6]


--- Output 8 ---
Exit code: 0
Wall time: 35.7 seconds
Output:
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[Benign_Normal_Traffic].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[DDoS].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[Port_Scan].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[BruteForce-SSH_FTP].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[Web_Attacks-Brute_XSS_SQL].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
Dropped rare classes: [6] removed 1

[CIC_SPECIALIST] train_time=6.4s thr=0.30 P=0.998 R=0.997 F1=0.998
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_DDoS_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 88.4% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_PortScan_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_WebAttack_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 88.4% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 95.3% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 95.3% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Neptune_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Satan_Probe_15pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Smurf_DoS_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Exploits_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Fuzzers_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Generic_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)

[CIC_SPECIALIST] TEST_DATA
CIC-IDS2017_DDoS_50pct_anomaly.csv            rows=1000 trueAtk= 500 predAtk= 500 TPR=1.000 FPR=0.000 P=1.000
CIC-IDS2017_PortScan_30pct_anomaly.csv        rows=1000 trueAtk= 300 predAtk=   1 TPR=0.003 FPR=0.000 P=1.000
CIC-IDS2017_WebAttack_10pct_anomaly.csv       rows=1000 trueAtk= 100 predAtk=  92 TPR=0.920 FPR=0.000 P=1.000
CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv  rows=1000 trueAtk= 200 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv  rows=1000 trueAtk= 300 predAtk= 146 TPR=0.487 FPR=0.000 P=1.000
NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv     rows= 500 trueAtk= 100 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
NSL-KDD_Neptune_DoS_20pct_anomaly.csv         rows=1000 trueAtk= 200 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
NSL-KDD_Satan_Probe_15pct_anomaly.csv         rows=1000 trueAtk= 150 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
NSL-KDD_Smurf_DoS_10pct_anomaly.csv           rows= 500 trueAtk=  50 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
UNSW_NB15_DoS_20pct_anomaly.csv               rows=1000 trueAtk= 200 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
UNSW_NB15_Exploits_30pct_anomaly.csv          rows=1000 trueAtk= 300 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
UNSW_NB15_Fuzzers_10pct_anomaly.csv           rows=1000 trueAtk= 100 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
UNSW_NB15_Generic_50pct_anomaly.csv           rows=1000 trueAtk= 500 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
[CIC_SPECIALIST] weighted_TPR=0.2463 weighted_FPR=0.0000 weighted_Prec=0.9880
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[Benign_Normal_Traffic].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[DDoS].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[Port_Scan].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[BruteForce-SSH_FTP].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\CIC-IDS2017_[Web_Attacks-Brute_XSS_SQL].csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\NSL_KDD_train.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (4000, 44)
[LOG] Loading file: datasets\Training_Ready\UNSW_NB15_train.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (4000, 45)
Dropped rare classes: [6, 9, 12, 17] removed 4

[MEGA_MIX] train_time=10.0s thr=0.30 P=0.994 R=0.998 F1=0.996
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_DDoS_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 88.4% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_PortScan_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_WebAttack_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 88.4% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 95.3% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 95.3% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Neptune_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Satan_Probe_15pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Smurf_DoS_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Family mode forced for NSL-KDD: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Exploits_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Fuzzers_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Generic_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Family mode forced for UNSW-NB15: preserving native feature space.
[LOG] Pipeline Complete. Output shape: (1000, 45)

[MEGA_MIX] TEST_DATA
CIC-IDS2017_DDoS_50pct_anomaly.csv            rows=1000 trueAtk= 500 predAtk= 500 TPR=1.000 FPR=0.000 P=1.000
CIC-IDS2017_PortScan_30pct_anomaly.csv        rows=1000 trueAtk= 300 predAtk=   1 TPR=0.003 FPR=0.000 P=1.000
CIC-IDS2017_WebAttack_10pct_anomaly.csv       rows=1000 trueAtk= 100 predAtk=  93 TPR=0.930 FPR=0.000 P=1.000
CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv  rows=1000 trueAtk= 200 predAtk=   1 TPR=0.005 FPR=0.000 P=1.000
CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv  rows=1000 trueAtk= 300 predAtk= 146 TPR=0.487 FPR=0.000 P=1.000
NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv     rows= 500 trueAtk= 100 predAtk=   8 TPR=0.040 FPR=0.010 P=0.500
NSL-KDD_Neptune_DoS_20pct_anomaly.csv         rows=1000 trueAtk= 200 predAtk= 208 TPR=1.000 FPR=0.010 P=0.962
NSL-KDD_Satan_Probe_15pct_anomaly.csv         rows=1000 trueAtk= 150 predAtk= 158 TPR=1.000 FPR=0.009 P=0.949
NSL-KDD_Smurf_DoS_10pct_anomaly.csv           rows= 500 trueAtk=  50 predAtk=  56 TPR=1.000 FPR=0.013 P=0.893
UNSW_NB15_DoS_20pct_anomaly.csv               rows=1000 trueAtk= 200 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
UNSW_NB15_Exploits_30pct_anomaly.csv          rows=1000 trueAtk= 300 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
UNSW_NB15_Fuzzers_10pct_anomaly.csv           rows=1000 trueAtk= 100 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
UNSW_NB15_Generic_50pct_anomaly.csv           rows=1000 trueAtk= 500 predAtk=   0 TPR=0.000 FPR=0.000 P=0.000
[MEGA_MIX] weighted_TPR=0.3817 weighted_FPR=0.0029 weighted_Prec=0.9745
2026-03-25 23:01:10.645 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
2026-03-25 23:01:10.646 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
2026-03-25 23:01:10.647 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
[Preprocessor] 34 columns filled with 0: ['bwd_iat_max', 'fwd_packet_length_min', 'bwd_iat_min', 'psh_flag_count', 'bwd_packet_length_max']...
[Preprocessor] 34 columns filled with 0: ['bwd_iat_max', 'fwd_packet_length_min', 'bwd_iat_min', 'psh_flag_count', 'bwd_packet_length_max']...
[Preprocessor] 34 columns filled with 0: ['bwd_iat_max', 'fwd_packet_length_min', 'bwd_iat_min', 'psh_flag_count', 'bwd_packet_length_max']...
[Preprocessor] 34 columns filled with 0: ['bwd_iat_max', 'fwd_packet_length_min', 'bwd_iat_min', 'psh_flag_count', 'bwd_packet_length_max']...
[Preprocessor] 24 columns filled with 0: ['bwd_iat_max', 'fwd_packet_length_min', 'bwd_iat_min', 'psh_flag_count', 'dst_port']...
[Preprocessor] 24 columns filled with 0: ['bwd_iat_max', 'fwd_packet_length_min', 'bwd_iat_min', 'psh_flag_count', 'dst_port']...
[Preprocessor] 24 columns filled with 0: ['bwd_iat_max', 'fwd_packet_length_min', 'bwd_iat_min', 'psh_flag_count', 'dst_port']...
[Preprocessor] 24 columns filled with 0: ['bwd_iat_max', 'fwd_packet_length_min', 'bwd_iat_min', 'psh_flag_count', 'dst_port']...
[Preprocessor] 59 columns filled with 0: ['ct_srv_dst', 'same_srv_rate', 'num_compromised', 'synack', 'dst_host_rerror_rate']...
[Preprocessor] 59 columns filled with 0: ['ct_srv_dst', 'same_srv_rate', 'num_compromised', 'synack', 'dst_host_rerror_rate']...
[Preprocessor] 59 columns filled with 0: ['ct_srv_dst', 'same_srv_rate', 'num_compromised', 'synack', 'dst_host_rerror_rate']...
[Preprocessor] 59 columns filled with 0: ['ct_srv_dst', 'same_srv_rate', 'num_compromised', 'synack', 'dst_host_rerror_rate']...
[Preprocessor] 59 columns filled with 0: ['ct_srv_dst', 'same_srv_rate', 'num_compromised', 'synack', 'dst_host_rerror_rate']...
[Preprocessor] 60 columns filled with 0: ['bwd_iat_max', 'ct_srv_dst', 'fwd_packet_length_min', 'synack', 'trans_depth']...
[Preprocessor] 60 columns filled with 0: ['bwd_iat_max', 'ct_srv_dst', 'fwd_packet_length_min', 'synack', 'trans_depth']...
[Preprocessor] 60 columns filled with 0: ['bwd_iat_max', 'ct_srv_dst', 'fwd_packet_length_min', 'synack', 'trans_depth']...
[Preprocessor] 60 columns filled with 0: ['bwd_iat_max', 'ct_srv_dst', 'fwd_packet_length_min', 'synack', 'trans_depth']...
[Preprocessor] 56 columns filled with 0: ['bwd_iat_max', 'same_srv_rate', 'fwd_packet_length_min', 'num_compromised', 'dst_host_rerror_rate']...
[Preprocessor] 56 columns filled with 0: ['bwd_iat_max', 'same_srv_rate', 'fwd_packet_length_min', 'num_compromised', 'dst_host_rerror_rate']...
[Preprocessor] 56 columns filled with 0: ['bwd_iat_max', 'same_srv_rate', 'fwd_packet_length_min', 'num_compromised', 'dst_host_rerror_rate']...
[Preprocessor] 56 columns filled with 0: ['bwd_iat_max', 'same_srv_rate', 'fwd_packet_length_min', 'num_compromised', 'dst_host_rerror_rate']...


--- Output 9 ---
Exit code: 0
Wall time: 0.5 seconds
Output:
19:from sklearn.model_selection import train_test_split
32:    _infer_dataset_family_name,
35:    detect_scan_file_family_info
48:    smart_results = st.session_state.get('smart_training_results')
49:    if smart_results:
50:        benchmarks = smart_results.get('benchmarks', {})
51:        best_metrics = smart_results.get('best_metrics', {})
52:        best_algo = smart_results.get('best_algo', '')
166:    show_smart_training = not is_expert_mode
167:    smart_quick_mode = True
169:    if show_smart_training:
180:        smart_col1, smart_col2 = st.columns([3, 1])
181:        with smart_col1:
186:        with smart_col2:
187:            smart_quick_mode = st.checkbox(
193:    if show_smart_training and st.button(
195:        key="smart_train_btn",
202:        smart_log_container = st.empty()
203:        smart_logs: list[str] = []
205:        def smart_log(message: str) -> None:
206:            smart_logs.append(message)
207:            smart_log_container.code('\n'.join(smart_logs), language='text')
212:            rows_per_file = 25000 if smart_quick_mode else 50000
213:            smart_log(f"Режим: {'Швидкий' if smart_quick_mode else 'Повний'} (до {rows_per_file:,} рядків/файл)")
222:            smart_log(f"Знайдено Training_Ready файлів: {len(all_ready_files)}")
223:            smart_log(f"Нормальних: {len(normal_files)}, атакуючих: {len(attack_files)}")
227:            smart_log("Крок 1/2: Формуємо універсальну tabular-модель (Two-Stage, авто-вибір алгоритму)...")
236:                    df_part = loader.load_file(str(file_path), max_rows=rows_per_file, multiclass=True)
239:                        smart_log(f"+ {file_path.name}: {len(df_part):,} рядків")
241:                    smart_log(f"! Пропущено {file_path.name}: {exc}")
247:            df_tab = pd.concat(dfs_tab, ignore_index=True)
250:            from sklearn.model_selection import train_test_split as _tts_smart
255:                df_tab_train, df_tab_test = _tts_smart(
257:                    stratify=label_raw_tab
260:                df_tab_train, df_tab_test = _tts_smart(df_tab, test_size=0.2, random_state=42)
280:            smart_log(f"[Anti-leakage] Preprocessor fitted ONLY on train split ({len(X_train_tab):,} rows). Test: {len(X_test_tab):,} rows.")
282:            # Clean rare classes from TRAIN only
283:            min_samples = 5
284:            rare_classes = y_train_tab.value_counts()[y_train_tab.value_counts() < min_samples].index.tolist()
285:            if rare_classes:
286:                mask_tr = ~y_train_tab.isin(rare_classes)
290:                mask_te = ~y_test_tab.isin(rare_classes)
319:                    smart_log(f"Спроба Two-Stage моделі на базі {algo}...")
342:                    smart_log(
356:                    smart_log(f"Обрано найкращий алгоритм для Two-Stage: {tab_best_algo}")
357:                    smart_log(
368:                    smart_log("! Не вдалося стабільно підібрати Two-Stage модель. Створено лише IF-модель.")
370:                smart_log("! Недостатньо класів для Two-Stage. Створено лише IF-модель.")
372:            smart_tab_model_name = f"ids_model_smart_tabular_{timestamp}.joblib"
375:                    smart_tab_model_name,
383:                        'smart_autotrain': True,
403:                smart_log(f"Збережено tabular-модель: {smart_tab_model_name}")
407:            smart_log("Крок 2/2: Формуємо PCAP anomaly-модель (Isolation Forest)...")
412:                smart_log("! Не знайдено доречних PCAP-файлів у Training_Ready.")
416:                    df_part = loader.load_file(str(file_path), max_rows=rows_per_file, multiclass=False)
421:                    smart_log(f"! IF source skip {file_path.name}: {exc}")
424:                smart_log("! Тренування PCAP-моделі скасовано. Бракує даних.")
428:                    st.session_state['training_flash_message'] = f"Додано модель: {smart_tab_model_name}."
429:                    st.session_state['smart_training_results'] = {
439:            df_if = pd.concat(dfs_if, ignore_index=True)
456:                    'n_estimators': 120 if smart_quick_mode else 180,
476:                smart_log(
486:                smart_log("IF calibration: unsupervised fallback")
488:            smart_if_model_name = f"ids_model_smart_if_{timestamp}.joblib"
490:                smart_if_model_name,
498:                    'smart_autotrain': True,
508:            smart_log(f"Збережено IF-модель: {smart_if_model_name}")
514:                    f"Розумне тренування завершено. Додано моделі: {smart_tab_model_name}, {smart_if_model_name}."
517:                st.session_state['smart_training_results'] = {
525:                st.caption(f"Модель: {smart_if_model_name}")
527:                    f"Розумне тренування завершено. Додано модель: {smart_if_model_name}."
529:                st.session_state.pop('smart_training_results', None)
573:    mega_model_files = []
685:            family_set = {
689:            if len(family_set) > 1:
736:        mega_model_files = list(set(found_files))
738:        if not mega_model_files:
741:            st.success(f"Знайдено {len(mega_model_files)} файлів для об'єднання.")
750:            dataset_path = mega_model_files[0] 
780:        is_mega_model = "Mega-Model" in training_strategy
808:        if is_mega_model:
1159:                files_for_training_used: list[Path] = []
1161:                def _resolve_family_for_file(path: Path) -> str:
1164:                        info = detect_scan_file_family_info(str(path), stat.st_mtime, stat.st_size)
1165:                        fam = str(info.get('family', '')).strip()
1170:                    return _infer_dataset_family_name(path.name)
1192:                def _min_coverage_for_family(family_name: str) -> float:
1197:                    }.get(family_name, 0.20)
1199:                if is_mega_model:
1200:                    files_for_training = list(mega_model_files)
1206:                detected_families = [_resolve_family_for_file(f) for f in files_for_training]
1208:                unknown_family = any(not fam for fam in detected_families)
1209:                mega_multi_family = bool(is_mega_model and len(known_families) > 1 and not is_pcap_training_source)
1210:                multi_family = len(known_families) > 1
1211:                family_mode_required = any(fam in {"NSL-KDD", "UNSW-NB15"} for fam in known_families)
1214:                    or ((multi_family and not is_mega_model) and not family_mode_required)
1216:                if family_mode_required:
1219:                if unknown_family:
1225:                if family_mode_required:
1230:                    if multi_family and not is_mega_model:
1243:                    if mega_multi_family:
1257:                    merge_caption = "Mega-Model" if is_mega_model else "Вибрані датасети"
1262:                    base_total_budget = 140000 if is_mega_model else 120000
1264:                        base_total_budget = 280000 if is_mega_model else 200000
1266:                    files_by_family: dict[str, list[Path]] = {}
1269:                        files_by_family.setdefault(key, []).append(f)
1270:                    if is_mega_model and "Generic" in files_by_family:
1275:                        files_by_family.pop("Generic", None)
1277:                    family_keys = list(files_by_family.keys())
1278:                    family_budget = max(20000, int(base_total_budget / max(1, len(family_keys))))
1281:                    for family_key, family_files in files_by_family.items():
1282:                        remaining_family = family_budget
1283:                        per_file_budget = max(5000, int(np.ceil(family_budget / max(1, len(family_files)))))
1286:                        for f in family_files:
1287:                            if remaining_family <= 0:
1292:                                    max_rows=min(per_file_budget, remaining_family),
1296:                                if multi_family:
1298:                                    df_part["family_hint"] = family_key or "Unknown"
1300:                                family_name = _infer_dataset_family_name(f.name)
1301:                                min_cov = _min_coverage_for_family(family_name)
1302:                                if align_to_schema and is_mega_model and coverage < min_cov:
1304:                                        f"{f.name}: низька сумісність ознак ({coverage:.0%}) для {family_name or 'Generic'} "
1307:                                    add_log(f"Пропущено {f.name}: coverage={coverage:.2f}, family={family_name or 'Generic'}")
1317:                                files_for_training_used.append(f)
1318:                                remaining_family -= len(df_part)
1328:                    df = pd.concat(dfs, ignore_index=True)
1337:                        max_rows=single_cap,
1341:                    files_for_training_used = [files_for_training[0]]
1395:                        df_train_raw, df_test_raw = train_test_split(
1397:                            stratify=label_raw_pre
1400:                        df_train_raw, df_test_raw = train_test_split(
1437:                y = pd.concat([y_train, y_test])
1439:                # Clean rare classes from TRAIN only
1440:                min_samples = 5
1441:                if is_mega_model and len(y_train):
1442:                    min_samples = max(min_samples, int(0.001 * len(y_train)))
1444:                rare_classes = class_counts[class_counts < min_samples].index.tolist()
1446:                if rare_classes:
1448:                        rare_names = preprocessor.target_encoder.inverse_transform(rare_classes)
1449:                        rare_str = ", ".join(map(str, rare_names))
1451:                        rare_str = str(rare_classes)
1453:                    mask_tr = ~y_train.isin(rare_classes)
1456:                    mask_te = ~y_test.isin(rare_classes)
1460:                    st.warning(f"Виключено рідкісні класи (< {min_samples} прикл.): {rare_str}")
1722:                    list(files_for_training_used)
1723:                    if files_for_training_used
1725:                        list(mega_model_files)
1726:                        if is_mega_model
1731:                    fam for fam in (_infer_dataset_family_name(Path(p).name) for p in training_files_used) if fam
1739:                effective_is_mega = bool(is_mega_model and len(trained_families_used) >= 2)
1740:                if is_mega_model and not effective_is_mega:
1750:                    is_mega_model=effective_is_mega,
1751:                    trained_family_count=len(trained_families_used),
1758:                schema_mode = "unified" if align_to_schema else "family"
1764:                        if effective_is_mega
1820:                    allow_override = bool(is_mega_model or quality_score >= 55)


--- Output 10 ---
Exit code: 0
Wall time: 0.5 seconds
Output:
                    else:
                        st.info(
                            "РЎС–РјРµР№РЅРёР№ СЂРµР¶РёРј РѕР·РЅР°Рє СѓРІС–РјРєРЅРµРЅРѕ: РјРѕРґРµР»СЊ Р·Р±РµСЂС–РіР°С” СЃРїРµС†РёС„С–С‡РЅС– РєРѕР»РѕРЅРєРё С†СЊРѕРіРѕ РґР°С‚Р°СЃРµС‚Сѓ "
                            "РґР»СЏ РєСЂР°С‰РѕС— С‚РѕС‡РЅРѕСЃС‚С–."
                        )

                if len(files_for_training) > 1:
                    dfs = []
                    merge_caption = "Mega-Model" if is_mega_model else "Р’РёР±СЂР°РЅС– РґР°С‚Р°СЃРµС‚Рё"
                    st.write(f"РћР±'С”РґРЅР°РЅРЅСЏ {len(files_for_training)} РґР°С‚Р°СЃРµС‚С–РІ ({merge_caption})...")

                    data_mode_fast = not is_expert_mode
                    rows_per_file_cap = 20000 if data_mode_fast else 50000
                    base_total_budget = 140000 if is_mega_model else 120000
                    if not data_mode_fast:
                        base_total_budget = 280000 if is_mega_model else 200000

                    files_by_family: dict[str, list[Path]] = {}
                    for f, fam in zip(files_for_training, detected_families):
                        key = fam or "Generic"
                        files_by_family.setdefault(key, []).append(f)
                    if is_mega_model and "Generic" in files_by_family:
                        st.warning(
                            "РЈ Mega-Model РІРёСЏРІР»РµРЅРѕ С„Р°Р№Р»Рё Р±РµР· С‡С–С‚РєРѕРіРѕ СЃС–РјРµР№СЃС‚РІР°. "
                            "Р‡С… РїСЂРѕРїСѓС‰РµРЅРѕ, С‰РѕР± РЅРµ Р·РЅРёР¶СѓРІР°С‚Рё СЏРєС–СЃС‚СЊ СѓР·Р°РіР°Р»СЊРЅРµРЅРЅСЏ."
                        )
                        files_by_family.pop("Generic", None)

                    family_keys = list(files_by_family.keys())
                    family_budget = max(20000, int(base_total_budget / max(1, len(family_keys))))
                    total_loaded = 0

                    for family_key, family_files in files_by_family.items():
                        remaining_family = family_budget
                        per_file_budget = max(5000, int(np.ceil(family_budget / max(1, len(family_files)))))
                        per_file_budget = min(per_file_budget, rows_per_file_cap)

                        for f in family_files:
                            if remaining_family <= 0:
                                break
                            try:
                                df_part = loader.load_file(
                                    str(f),
                                    max_rows=min(per_file_budget, remaining_family),
                                    multiclass=two_stage_mode,
                                    align_to_schema=align_to_schema
                                )
                                if multi_family:
                                    df_part = df_part.copy()
                                    df_part["family_hint"] = family_key or "Unknown"
                                coverage = 1.0 if not align_to_schema else _estimate_schema_coverage(df_part, loader.schema_features)
                                family_name = _infer_dataset_family_name(f.name)
                                min_cov = _min_coverage_for_family(family_name)
                                if align_to_schema and is_mega_model and coverage < min_cov:
                                    st.warning(
                                        f"{f.name}: РЅРёР·СЊРєР° СЃСѓРјС–СЃРЅС–СЃС‚СЊ РѕР·РЅР°Рє ({coverage:.0%}) РґР»СЏ {family_name or 'Generic'} "
                                        "вЂ” С„Р°Р№Р» РІРёРєР»СЋС‡РµРЅРѕ Р· Mega-С‚СЂРµРЅСѓРІР°РЅРЅСЏ."
                                    )
                                    add_log(f"РџСЂРѕРїСѓС‰РµРЅРѕ {f.name}: coverage={coverage:.2f}, family={family_name or 'Generic'}")
                                    continue

                                if align_to_schema and coverage < min_cov:
                                    st.warning(
                                        f"{f.name}: РЅРёР·СЊРєР° СЃСѓРјС–СЃРЅС–СЃС‚СЊ РѕР·РЅР°Рє ({coverage:.0%}). "
                                        "РЇРєС–СЃС‚СЊ РјРѕР¶Рµ Р±СѓС‚Рё РЅРёР¶С‡РѕСЋ, РїРµСЂРµРІС–СЂС‚Рµ РґР°С‚Р°СЃРµС‚."
                                    )

                                dfs.append(df_part)
                                files_for_training_used.append(f)
                                remaining_family -= len(df_part)
                                total_loaded += len(df_part)
                                st.caption(f"{f.name} ({len(df_part)} СЂСЏРґРєС–РІ)")
                            except Exception as e:
                                st.error(f"РџРѕРјРёР»РєР° Р· С„Р°Р№Р»РѕРј {f.name}: {e}")

                    if not dfs:
                        st.error("РќРµ РІРґР°Р»РѕСЃСЏ Р·Р°РІР°РЅС‚Р°Р¶РёС‚Рё РґР°РЅС–.")
                        st.stop()

                    df = pd.concat(dfs, ignore_index=True)
                    st.success(f"РћР±'С”РґРЅР°РЅРѕ. Р’СЃСЊРѕРіРѕ Р·Р°РїРёСЃС–РІ: {len(df):,}")
                else:
                    if not files_for_training:
                        st.error("РќРµ РІРёР±СЂР°РЅРѕ Р¶РѕРґРЅРѕРіРѕ СЃСѓРјС–СЃРЅРѕРіРѕ С„Р°Р№Р»Сѓ РґР»СЏ С‚СЂРµРЅСѓРІР°РЅРЅСЏ.")
                        st.stop()
                    single_cap = 150000 if not is_expert_mode else 320000
                    df = loader.load_file(
                        str(files_for_training[0]),
                        max_rows=single_cap,
                        multiclass=two_stage_mode,
                        align_to_schema=align_to_schema
                    )
                    files_for_training_used = [files_for_training[0]]

                print(f"[LOG] Data loaded. Shape: {df.shape}")

                if 'label' not in df.columns:
                    if is_isolation_algorithm:
                        # Unsupervised fallback: allow training IF on unlabeled files (PCAP/NF/CSV).
                        # IMPORTANT: quality depends on how "normal" this file is.
                        df['label'] = 'BENIGN'
                        st.warning(
                            "РЈ С„Р°Р№Р»С– РЅРµ Р·РЅР°Р№РґРµРЅРѕ РєРѕР»РѕРЅРєСѓ label. Р”Р»СЏ Isolation Forest РІСЃС– Р·Р°РїРёСЃРё РІРІР°Р¶Р°СЋС‚СЊСЃСЏ "
                            "РЅРѕСЂРјР°Р»СЊРЅРёРјРё (BENIGN) РїС–Рґ С‡Р°СЃ С‚СЂРµРЅСѓРІР°РЅРЅСЏ."
                        )
                    else:
                        label_cols = [c for c in df.columns if 'label' in c.lower() or 'class' in c.lower()]
                        if label_cols:
                            df.rename(columns={label_cols[0]: 'label'}, inplace=True)
                        else:
                            st.error("РќРµ Р·РЅР°Р№РґРµРЅРѕ РєРѕР»РѕРЅРєСѓ Р· РјС–С‚РєР°РјРё (label). РџРµСЂРµРІС–СЂС‚Рµ СЃС‚СЂСѓРєС‚СѓСЂСѓ С„Р°Р№Р»Сѓ.")
                            st.stop()

                label_candidates = FeatureRegistry.get_synonyms().get('label', [])
                cols_to_drop = [c for c in df.columns if c in label_candidates and c != 'label']
                if cols_to_drop:
                    df.drop(columns=cols_to_drop, inplace=True)
                    st.warning(f"Р’РёРґР°Р»РµРЅРѕ РєРѕР»РѕРЅРєРё РґР»СЏ СѓРЅРёРєРЅРµРЅРЅСЏ РІРёС‚РѕРєСѓ РґР°РЅРёС…: {', '.join(cols_to_drop)}")

                if not is_isolation_algorithm and 'label' in df.columns:
                    label_norm = df['label'].astype(str).str.strip().str.lower()
                    unique_attack_labels = sorted(set(label_norm[~label_norm.isin(BENIGN_LABEL_TOKENS)].tolist()))
                    if len(unique_attack_labels) <= 1:
                        st.warning(
                            "РЈ С‚СЂРµРЅСѓРІР°Р»СЊРЅРѕРјСѓ С„Р°Р№Р»С– Р»РёС€Рµ РѕРґРёРЅ С‚РёРї Р°С‚Р°РєРё (Р°Р±Рѕ РјР°Р№Р¶Рµ РЅРµРјР°С” РІР°СЂС–Р°С‚РёРІРЅРѕСЃС‚С–). "
                            "РўР°РєР° РјРѕРґРµР»СЊ РјРѕР¶Рµ РґРѕР±СЂРµ Р»РѕРІРёС‚Рё СЃР°РјРµ С†РµР№ СЃС†РµРЅР°СЂС–Р№, Р°Р»Рµ РїСЂРѕРїСѓСЃРєР°С‚Рё С–РЅС€С– Р°С‚Р°РєРё. "
                            "Р”Р»СЏ РєСЂР°С‰РѕРіРѕ СѓР·Р°РіР°Р»СЊРЅРµРЅРЅСЏ РІРёРєРѕСЂРёСЃС‚Р°Р№С‚Рµ Mega-Model Р°Р±Рѕ Р РѕР·СѓРјРЅРµ С‚СЂРµРЅСѓРІР°РЅРЅСЏ (1 РєР»С–Рє)."
                        )

                st.info(f"Р—РЅР°Р№РґРµРЅРѕ {len(df):,} Р·Р°РїРёСЃС–РІ С‚Р° {df['label'].nunique()} С‚РёРїС–РІ С‚СЂР°С„С–РєСѓ")

                progress.progress(30)
                st.info("РћР±СЂРѕР±РєР° С‚Р° РїС–РґРіРѕС‚РѕРІРєР° РґР°РЅРёС…...")

                print("[LOG] Starting preprocessing (anti-leakage: raw split first)...")
                enable_scaling = (algorithm == "Logistic Regression")

                # *** ANTI-LEAKAGE: split raw DataFrame FIRST, fit preprocessor ONLY on train ***
                # This prevents the scaler/encoder from seeing test-set statistics during training.
                if 'label' in df.columns:
                    label_raw_pre = df['label'].astype(str).str.strip().str.lower()
                    raw_split_ok = (
                        label_raw_pre.nunique() >= 2
                        and label_raw_pre.value_counts().min() >= 2
                    )
                    if raw_split_ok:
                        df_train_raw, df_test_raw = train_test_split(
                            df, test_size=test_size / 100, random_state=42,
                            stratify=label_raw_pre
                        )
                    else:
                        df_train_raw, df_test_raw = train_test_split(
                            df, test_size=test_size / 100, random_state=42
                        )
                    add_log(
                        f"[Anti-leakage] Raw split: train={len(df_train_raw):,}, test={len(df_test_raw):,} "
                        f"(preprocessor will fit ONLY on train)."
                    )
                else:
                    # IF unsupervised вЂ” no label to split on
                    df_train_raw = df.copy()
                    df_test_raw = df.copy()

                preprocessor = Preprocessor(enable_scaling=enable_scaling)
                X_train, y_train = preprocessor.fit_transform(df_train_raw, target_col='label')
                X_test = preprocessor.transform(df_test_raw.drop(columns=['label'], errors='ignore'))

                # Encode test labels with the already-fitted target_encoder
                y_test_raw_series = df_test_raw['label'].astype(str).str.strip() if 'label' in df_test_raw.columns else pd.Series(dtype=str)
                try:
                    y_test = pd.Series(
                        preprocessor.target_encoder.transform(y_test_raw_series),
                        index=df_test_raw.index


--- Output 11 ---
Exit code: 0
Wall time: 0.5 seconds
Output:
                        index=df_test_raw.index
                    )
                except Exception:
                    known_mask_te = y_test_raw_series.isin(preprocessor.target_encoder.classes_)
                    X_test = X_test[known_mask_te]
                    y_test = pd.Series(
                        preprocessor.target_encoder.transform(y_test_raw_series[known_mask_te]),
                        index=df_test_raw.index[known_mask_te]
                    )

                scaling_state = "enabled" if enable_scaling else "disabled"
                print(f"[LOG] Preprocessing finished. X_train: {X_train.shape}, X_test: {X_test.shape}, scaling {scaling_state}")
                add_log(f"РќР°РІС‡Р°Р»СЊРЅР° РІРёР±С–СЂРєР°: {len(X_train):,} СЂСЏРґРєС–РІ | РўРµСЃС‚РѕРІР°: {len(X_test):,} СЂСЏРґРєС–РІ")
                if enable_scaling:
                    add_log("Р”Р»СЏ Logistic Regression СѓРІС–РјРєРЅРµРЅРѕ Р°РІС‚РѕРјР°С‚РёС‡РЅРµ РјР°СЃС€С‚Р°Р±СѓРІР°РЅРЅСЏ (StandardScaler).")

                y = pd.concat([y_train, y_test])

                # Clean rare classes from TRAIN only
                min_samples = 5
                if is_mega_model and len(y_train):
                    min_samples = max(min_samples, int(0.001 * len(y_train)))
                class_counts = y_train.value_counts()
                rare_classes = class_counts[class_counts < min_samples].index.tolist()

                if rare_classes:
                    try:
                        rare_names = preprocessor.target_encoder.inverse_transform(rare_classes)
                        rare_str = ", ".join(map(str, rare_names))
                    except Exception:
                        rare_str = str(rare_classes)

                    mask_tr = ~y_train.isin(rare_classes)
                    X_train = X_train[mask_tr]
                    y_train = y_train[mask_tr]
                    mask_te = ~y_test.isin(rare_classes)
                    X_test = X_test[mask_te]
                    y_test = y_test[mask_te]

                    st.warning(f"Р’РёРєР»СЋС‡РµРЅРѕ СЂС–РґРєС–СЃРЅС– РєР»Р°СЃРё (< {min_samples} РїСЂРёРєР».): {rare_str}")

                # XGBoost РІРёРјР°РіР°С” СЃСѓС†С–Р»СЊРЅС– С–РЅРґРµРєСЃРё РєР»Р°СЃС–РІ 0..N-1.
                if algorithm == "XGBoost" and not two_stage_mode:
                    unique_codes = np.sort(pd.Series(y_train).unique())
                    remap = {int(old_code): int(new_code) for new_code, old_code in enumerate(unique_codes)}
                    y_train = pd.Series(y_train).map(remap).astype(int)
                    y_test = pd.Series(y_test).map(remap).fillna(-1).astype(int)
                    y_test = y_test[y_test >= 0]  # drop unknowns
                    X_test = X_test.loc[y_test.index]

                    if hasattr(preprocessor, 'target_encoder') and hasattr(preprocessor.target_encoder, 'classes_'):
                        try:
                            old_classes = np.asarray(preprocessor.target_encoder.classes_)
                            new_classes = old_classes[unique_codes.astype(int)]
                            preprocessor.target_encoder.classes_ = new_classes
                            add_log(
                                f"XGBoost: РІРёРєРѕРЅР°РЅРѕ reindex РєР»Р°СЃС–РІ РїС–СЃР»СЏ РѕС‡РёСЃС‚РєРё СЂС–РґРєС–СЃРЅРёС… РјС–С‚РѕРє "
                                f"({len(unique_codes)} Р°РєС‚РёРІРЅРёС… РєР»Р°СЃС–РІ)."
                            )
                        except Exception:
                            add_log(
                                "вљ пёЏ XGBoost: reindex РєР»Р°СЃС–РІ РІРёРєРѕРЅР°РЅРѕ, Р°Р»Рµ СЃРёРЅС…СЂРѕРЅС–Р·СѓРІР°С‚Рё target_encoder "
                                "Р°РІС‚РѕРјР°С‚РёС‡РЅРѕ РЅРµ РІРґР°Р»РѕСЃСЏ."
                            )

                progress.progress(45)


--- Output 12 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
                    coverage = 1.0 - (zero_cols / max(1, len(numeric_cols)))
                    return float(max(0.0, min(1.0, coverage)))

                def _min_coverage_for_family(family_name: str) -> float:
                    return {
                        "CIC-IDS": 0.45,
                        "UNSW-NB15": 0.12,
                        "NSL-KDD": 0.08,
                    }.get(family_name, 0.20)

                if is_mega_model:
                    files_for_training = list(mega_model_files)
                elif len(selected_training_files) > 1:
                    files_for_training = list(selected_training_files)
                else:
                    files_for_training = [dataset_path] if dataset_path else []

                detected_families = [_resolve_family_for_file(f) for f in files_for_training]
                known_families = {fam for fam in detected_families if fam}
                unknown_family = any(not fam for fam in detected_families)
                mega_multi_family = bool(is_mega_model and len(known_families) > 1 and not is_pcap_training_source)
                multi_family = len(known_families) > 1
                family_mode_required = any(fam in {"NSL-KDD", "UNSW-NB15"} for fam in known_families)
                align_to_schema = bool(
                    is_pcap_training_source
                    or ((multi_family and not is_mega_model) and not family_mode_required)
                )
                if family_mode_required:
                    align_to_schema = False

                if unknown_family:
                    st.warning(
                        "РќРµ РІРґР°Р»РѕСЃСЏ С‚РѕС‡РЅРѕ РІРёР·РЅР°С‡РёС‚Рё СЃС–РјРµР№СЃС‚РІРѕ РґР»СЏ С‡Р°СЃС‚РёРЅРё С„Р°Р№Р»С–РІ. "
                        "РЎРёСЃС‚РµРјР° РїСЂР°С†СЋРІР°С‚РёРјРµ Сѓ СЃС–РјРµР№РЅРѕРјСѓ СЂРµР¶РёРјС– РѕР·РЅР°Рє, Р°Р»Рµ СЏРєС–СЃС‚СЊ РјРѕР¶Рµ Р·Р°Р»РµР¶Р°С‚Рё РІС–Рґ СЃС‚СЂСѓРєС‚СѓСЂРё С†РёС… РґР°С‚Р°СЃРµС‚С–РІ."
                    )

                if family_mode_required:
                    st.info(
                        "Р”Р»СЏ NSL-KDD/UNSW-NB15 СѓРІС–РјРєРЅРµРЅРѕ СЃС–РјРµР№РЅРёР№ СЂРµР¶РёРј РѕР·РЅР°Рє, "
                        "С‰РѕР± Р·Р±РµСЂРµРіС‚Рё РїРѕРІРЅРёР№ РЅР°Р±С–СЂ РєРѕР»РѕРЅРѕРє С– РЅРµ РІС‚СЂР°С‡Р°С‚Рё СЏРєС–СЃС‚СЊ."
                    )
                    if multi_family and not is_mega_model:
                        st.warning(
                            "РћР±СЂР°РЅРѕ СЂС–Р·РЅС– СЃС–РјРµР№СЃС‚РІР° (NSL/UNSW + С–РЅС€С–). "
                            "Р РµРєРѕРјРµРЅРґСѓС”С‚СЊСЃСЏ С‚СЂРµРЅСѓРІР°С‚Рё РѕРєСЂРµРјС– РјРѕРґРµР»С– Р°Р±Рѕ РІРёРєРѕСЂРёСЃС‚Р°С‚Рё Mega-Model."
                        )

                if align_to_schema:
                    if len(known_families) > 1:
                        st.warning(
                            "Р’РёСЏРІР»РµРЅРѕ СЂС–Р·РЅС– СЃС–РјРµР№СЃС‚РІР° РґР°РЅРёС…. Р”Р»СЏ СЃС‚Р°Р±С–Р»СЊРЅРѕСЃС‚С– РІРёРєРѕСЂРёСЃС‚РѕРІСѓС”С‚СЊСЃСЏ СѓРЅС–С„С–РєРѕРІР°РЅР° СЃС…РµРјР° РѕР·РЅР°Рє "
                            "(С†Рµ РјРѕР¶Рµ С‚СЂРѕС…Рё Р·РЅРёР·РёС‚Рё С‚РѕС‡РЅС–СЃС‚СЊ Сѓ РІСѓР·СЊРєРёС… РґРѕРјРµРЅР°С…)."
                        )
                else:
                    if mega_multi_family:
                        st.info(
                            "Mega-Model: СѓРІС–РјРєРЅРµРЅРѕ СЃС–РјРµР№РЅРёР№ СЂРµР¶РёРј РѕР·РЅР°Рє. "
                            "РњРѕРґРµР»СЊ Р·Р±РµСЂС–РіР°С” СЂРѕР·С€РёСЂРµРЅРёР№ РЅР°Р±С–СЂ РєРѕР»РѕРЅРѕРє С–Р· РєРѕР¶РЅРѕРіРѕ СЃС–РјРµР№СЃС‚РІР° "
                            "РґР»СЏ РєСЂР°С‰РѕС— РєСЂРѕСЃ-РґРѕРјРµРЅРЅРѕС— С‚РѕС‡РЅРѕСЃС‚С–."
                        )
                    else:
                        st.info(
                            "РЎС–РјРµР№РЅРёР№ СЂРµР¶РёРј РѕР·РЅР°Рє СѓРІС–РјРєРЅРµРЅРѕ: РјРѕРґРµР»СЊ Р·Р±РµСЂС–РіР°С” СЃРїРµС†РёС„С–С‡РЅС– РєРѕР»РѕРЅРєРё С†СЊРѕРіРѕ РґР°С‚Р°СЃРµС‚Сѓ "
                            "РґР»СЏ РєСЂР°С‰РѕС— С‚РѕС‡РЅРѕСЃС‚С–."
                        )

                if len(files_for_training) > 1:
                    dfs = []
                    merge_caption = "Mega-Model" if is_mega_model else "Р’РёР±СЂР°РЅС– РґР°С‚Р°СЃРµС‚Рё"
                    st.write(f"РћР±'С”РґРЅР°РЅРЅСЏ {len(files_for_training)} РґР°С‚Р°СЃРµС‚С–РІ ({merge_caption})...")

                    data_mode_fast = not is_expert_mode
                    rows_per_file_cap = 20000 if data_mode_fast else 50000
                    base_total_budget = 140000 if is_mega_model else 120000
                    if not data_mode_fast:
                        base_total_budget = 280000 if is_mega_model else 200000

                    files_by_family: dict[str, list[Path]] = {}
                    for f, fam in zip(files_for_training, detected_families):
                        key = fam or "Generic"
                        files_by_family.setdefault(key, []).append(f)
                    if is_mega_model and "Generic" in files_by_family:
                        st.warning(
                            "РЈ Mega-Model РІРёСЏРІР»РµРЅРѕ С„Р°Р№Р»Рё Р±РµР· С‡С–С‚РєРѕРіРѕ СЃС–РјРµР№СЃС‚РІР°. "
                            "Р‡С… РїСЂРѕРїСѓС‰РµРЅРѕ, С‰РѕР± РЅРµ Р·РЅРёР¶СѓРІР°С‚Рё СЏРєС–СЃС‚СЊ СѓР·Р°РіР°Р»СЊРЅРµРЅРЅСЏ."
                        )
                        files_by_family.pop("Generic", None)

                    family_keys = list(files_by_family.keys())
                    family_budget = max(20000, int(base_total_budget / max(1, len(family_keys))))
                    total_loaded = 0

                    for family_key, family_files in files_by_family.items():
                        remaining_family = family_budget
                        per_file_budget = max(5000, int(np.ceil(family_budget / max(1, len(family_files)))))
                        per_file_budget = min(per_file_budget, rows_per_file_cap)

                        for f in family_files:
                            if remaining_family <= 0:
                                break
                            try:
                                df_part = loader.load_file(
                                    str(f),
                                    max_rows=min(per_file_budget, remaining_family),
                                    multiclass=two_stage_mode,
                                    align_to_schema=align_to_schema
                                )
                                if multi_family:
                                    df_part = df_part.copy()
                                    df_part["family_hint"] = family_key or "Unknown"
                                coverage = 1.0 if not align_to_schema else _estimate_schema_coverage(df_part, loader.schema_features)
                                family_name = _infer_dataset_family_name(f.name)
                                min_cov = _min_coverage_for_family(family_name)
                                if align_to_schema and is_mega_model and coverage < min_cov:
                                    st.warning(
                                        f"{f.name}: РЅРёР·СЊРєР° СЃСѓРјС–СЃРЅС–СЃС‚СЊ РѕР·РЅР°Рє ({coverage:.0%}) РґР»СЏ {family_name or 'Generic'} "
                                        "вЂ” С„Р°Р№Р» РІРёРєР»СЋС‡РµРЅРѕ Р· Mega-С‚СЂРµРЅСѓРІР°РЅРЅСЏ."
                                    )
                                    add_log(f"РџСЂРѕРїСѓС‰РµРЅРѕ {f.name}: coverage={coverage:.2f}, family={family_name or 'Generic'}")
                                    continue

                                if align_to_schema and coverage < min_cov:
                                    st.warning(
                                        f"{f.name}: РЅРёР·СЊРєР° СЃСѓРјС–СЃРЅС–СЃС‚СЊ РѕР·РЅР°Рє ({coverage:.0%}). "
                                        "РЇРєС–СЃС‚СЊ РјРѕР¶Рµ Р±СѓС‚Рё РЅРёР¶С‡РѕСЋ, РїРµСЂРµРІС–СЂС‚Рµ РґР°С‚Р°СЃРµС‚."
                                    )

                                dfs.append(df_part)
                                files_for_training_used.append(f)
                                remaining_family -= len(df_part)
                                total_loaded += len(df_part)
                                st.caption(f"{f.name} ({len(df_part)} СЂСЏРґРєС–РІ)")
                            except Exception as e:
                                st.error(f"РџРѕРјРёР»РєР° Р· С„Р°Р№Р»РѕРј {f.name}: {e}")

                    if not dfs:
                        st.error("РќРµ РІРґР°Р»РѕСЃСЏ Р·Р°РІР°РЅС‚Р°Р¶РёС‚Рё РґР°РЅС–.")
                        st.stop()

                    df = pd.concat(dfs, ignore_index=True)
                    st.success(f"РћР±'С”РґРЅР°РЅРѕ. Р’СЃСЊРѕРіРѕ Р·Р°РїРёСЃС–РІ: {len(df):,}")
                else:
                    if not files_for_training:
                        st.error("РќРµ РІРёР±СЂР°РЅРѕ Р¶РѕРґРЅРѕРіРѕ СЃСѓРјС–СЃРЅРѕРіРѕ С„Р°Р№Р»Сѓ РґР»СЏ С‚СЂРµРЅСѓРІР°РЅРЅСЏ.")
                        st.stop()
                    single_cap = 150000 if not is_expert_mode else 320000
                    df = loader.load_file(
                        str(files_for_training[0]),
                        max_rows=single_cap,
                        multiclass=two_stage_mode,
                        align_to_schema=align_to_schema


--- Output 13 ---
Exit code: 0
Wall time: 0.4 seconds
Output:

    training_strategy = st.radio(
        "РўРёРї РјРѕРґРµР»С–:",
        ["РЎРїРµС†С–Р°Р»С–СЃС‚ (РћРґРёРЅ Р°Р±Рѕ РґРµРєС–Р»СЊРєР° РґР°С‚Р°СЃРµС‚С–РІ)", "Mega-Model (Р’СЃС– РґР°С‚Р°СЃРµС‚Рё)"],
        horizontal=True,
        help=(
            "РЎРїРµС†С–Р°Р»С–СЃС‚: С‚СЂРµРЅСѓРІР°РЅРЅСЏ РЅР° 1+ РѕР±СЂР°РЅРёС… С„Р°Р№Р»Р°С…. "
            "Mega-Model: РѕР±'С”РґРЅР°РЅРЅСЏ РєР»СЋС‡РѕРІРёС… РґР°С‚Р°СЃРµС‚С–РІ Training_Ready."
        )
    )

    dataset_path = None
    mega_model_files = []
    selected_training_files: list[Path] = []
    selected_training_meta: dict[str, dict[str, Any]] = {}

    if "РЎРїРµС†С–Р°Р»С–СЃС‚" in training_strategy:
        # --- Specialist: single or multi-dataset training ---
        training_source = st.radio(
            "Р”Р¶РµСЂРµР»Рѕ РґР°РЅРёС…:",
            ["Р“РѕС‚РѕРІС– РґР°С‚Р°СЃРµС‚Рё", "Р—Р°РІР°РЅС‚Р°Р¶РёС‚Рё РІР»Р°СЃРЅС– С„Р°Р№Р»Рё"],
            horizontal=True
        )

        if training_source == "Р“РѕС‚РѕРІС– РґР°С‚Р°СЃРµС‚Рё":
            ready_dir = ROOT_DIR / 'datasets' / 'Training_Ready'
            if not ready_dir.exists():
                st.error(f"РџР°РїРєР° {ready_dir} РЅРµ С–СЃРЅСѓС”.")
            else:
                ready_files = [
                    f for f in ready_dir.glob('*.*')
                    if f.suffix.lower() in SUPPORTED_SCAN_EXTENSIONS
                ]
                if not ready_files:
                    st.warning("РџР°РїРєР° datasets/Training_Ready РїРѕСЂРѕР¶РЅСЏ.")
                else:
                    ready_reports: dict[str, dict[str, Any]] = {}
                    compatible_ready: list[Path] = []
                    incompatible_ready: list[tuple[Path, str]] = []
                    for file_path in sorted(ready_files):
                        stat = file_path.stat()
                        report = assess_training_file_compatibility(
                            str(file_path), stat.st_mtime, stat.st_size
                        )
                        ready_reports[str(file_path)] = report
                        if report.get('compatible', False):
                            compatible_ready.append(file_path)
                        else:
                            incompatible_ready.append((file_path, str(report.get('reason', 'РќРµРІС–РґРѕРјР° РїСЂРёС‡РёРЅР°'))))

                    if incompatible_ready:
                        with st.expander("РќРµСЃСѓРјС–СЃРЅС– С„Р°Р№Р»Рё (РЅРµРґРѕСЃС‚СѓРїРЅС– РґР»СЏ РІРёР±РѕСЂСѓ)"):
                            for bad_file, reason in incompatible_ready:
                                st.warning(f"{bad_file.name}: {reason}")

                    if not compatible_ready:
                        st.error("РќРµ Р·РЅР°Р№РґРµРЅРѕ СЃСѓРјС–СЃРЅРёС… С„Р°Р№Р»С–РІ РґР»СЏ С‚СЂРµРЅСѓРІР°РЅРЅСЏ.")
                    else:
                        selected_training_files = st.multiselect(
                            "РћР±РµСЂС–С‚СЊ РѕРґРёРЅ Р°Р±Рѕ РґРµРєС–Р»СЊРєР° С„Р°Р№Р»С–РІ РґР»СЏ С‚СЂРµРЅСѓРІР°РЅРЅСЏ:",
                            options=compatible_ready,
                            default=compatible_ready[:1],
                            format_func=lambda x: x.name
                        )
                        selected_training_meta = {
                            str(path): ready_reports.get(str(path), {})
                            for path in selected_training_files
                        }

        else: # Upload own files
            uploaded_files = st.file_uploader(
                "Р—Р°РІР°РЅС‚Р°Р¶С‚Рµ CSV/NF Р°Р±Рѕ PCAP (РјРѕР¶РЅР° РґРµРєС–Р»СЊРєР°)",
                type=['csv', 'nf', 'nfdump', 'pcap', 'pcapng', 'cap'],
                accept_multiple_files=True
            )
            if uploaded_files:
                user_dir = ROOT_DIR / 'datasets' / 'User_Uploads'
                user_dir.mkdir(parents=True, exist_ok=True)
                uploaded_paths: list[Path] = []
                uploaded_keys = set(st.session_state.get('last_train_upload_keys', []))
                for uploaded_file in uploaded_files:
                    save_path = user_dir / uploaded_file.name
                    upload_key = f"{uploaded_file.name}_{uploaded_file.size}"
                    if upload_key not in uploaded_keys or not save_path.exists():
                        with open(save_path, "wb") as f:
                            f.write(uploaded_file.getbuffer())
                    uploaded_keys.add(upload_key)
                    uploaded_paths.append(save_path)
                st.session_state['last_train_upload_keys'] = list(uploaded_keys)
                st.success(f"Р—Р°РІР°РЅС‚Р°Р¶РµРЅРѕ С„Р°Р№Р»С–РІ: {len(uploaded_paths)}")

                uploaded_reports: dict[str, dict[str, Any]] = {}
                compatible_uploaded: list[Path] = []
                incompatible_uploaded: list[tuple[Path, str]] = []
                for file_path in uploaded_paths:
                    stat = file_path.stat()
                    report = assess_training_file_compatibility(
                        str(file_path), stat.st_mtime, stat.st_size
                    )
                    uploaded_reports[str(file_path)] = report
                    if report.get('compatible', False):
                        compatible_uploaded.append(file_path)
                    else:
                        incompatible_uploaded.append((file_path, str(report.get('reason', 'РќРµРІС–РґРѕРјР° РїСЂРёС‡РёРЅР°'))))

                if incompatible_uploaded:
                    with st.expander("Р—Р°РІР°РЅС‚Р°Р¶РµРЅС–, Р°Р»Рµ РЅРµСЃСѓРјС–СЃРЅС– С„Р°Р№Р»Рё"):
                        for bad_file, reason in incompatible_uploaded:
                            st.warning(f"{bad_file.name}: {reason}")

                if compatible_uploaded:
                    selected_training_files = st.multiselect(
                        "РћР±РµСЂС–С‚СЊ С„Р°Р№Р»Рё РґР»СЏ С‚СЂРµРЅСѓРІР°РЅРЅСЏ:",
                        options=compatible_uploaded,
                        default=compatible_uploaded,
                        format_func=lambda x: x.name,
                        key="uploaded_train_multiselect"
                    )
                    selected_training_meta = {
                        str(path): uploaded_reports.get(str(path), {})
                        for path in selected_training_files
                    }

        if selected_training_files:
            family_set = {
                'pcap' if f.suffix.lower() in PCAP_EXTENSIONS else 'tabular'
                for f in selected_training_files
            }
            if len(family_set) > 1:
                st.error(
                    "РќРµ РјРѕР¶РЅР° РѕРґРЅРѕС‡Р°СЃРЅРѕ С‚СЂРµРЅСѓРІР°С‚Рё РѕРґРЅСѓ РјРѕРґРµР»СЊ РЅР° СЃСѓРјС–С€С– PCAP С‚Р° CSV/NF. "
                    "РћР±РµСЂС–С‚СЊ Р»РёС€Рµ РѕРґРёРЅ С‚РёРї РґР¶РµСЂРµР»."
                )
            else:
                dataset_path = selected_training_files[0]
                if len(selected_training_files) > 1:
                    st.success(f"РћР±СЂР°РЅРѕ {len(selected_training_files)} С„Р°Р№Р»С–РІ РґР»СЏ РѕР±'С”РґРЅР°РЅРѕРіРѕ С‚СЂРµРЅСѓРІР°РЅРЅСЏ.")
                st.caption(f"РџРµСЂС€РёР№ С„Р°Р№Р» РґР»СЏ РїРѕРїРµСЂРµРґРЅСЊРѕРіРѕ РїРµСЂРµРіР»СЏРґСѓ: {dataset_path.name}")
                if all(f.suffix.lower() in PCAP_EXTENSIONS for f in selected_training_files):
                    st.warning(
                        "Р”Р»СЏ РЅР°Р±РѕСЂС–РІ PCAP РґРѕСЃС‚СѓРїРЅРёР№ Р»РёС€Рµ Isolation Forest (unsupervised СЂРµР¶РёРј)."
                    )

    else:
        # --- MEGA-MODEL LOGIC ---
        st.markdown("""
        <div class="info-box">
            <b>Mega-Model</b><br>
            РЎРёСЃС‚РµРјР° Р°РІС‚РѕРјР°С‚РёС‡РЅРѕ Р·РЅР°Р№РґРµ С‚Р° РѕР±'С”РґРЅР°С” РґР°С‚Р°СЃРµС‚Рё Р· РїР°РїРєРё <code>datasets/Training_Ready</code>:
            <ul>
                <li>CIC-IDS2017 (РћСЃРЅРѕРІРЅР° Р±Р°Р·Р°)</li>
                <li>UNSW-NB15 (РЎРєР»Р°РґРЅС– РїР°С‚РµСЂРЅРё)</li>
                <li>CIC-IDS2018 (РЎСѓС‡Р°СЃРЅС– Р°С‚Р°РєРё)</li>
            </ul>
            Р¦Рµ Р·Р°Р±РµР·РїРµС‡РёС‚СЊ РјР°РєСЃРёРјР°Р»СЊРЅСѓ С‚РѕС‡РЅС–СЃС‚СЊ РґРµС‚РµРєС†С–С— (~98.4%).
        </div>
        """, unsafe_allow_html=True)

        required_patterns = ["CIC-IDS2017", "UNSW-NB15", "CIC-IDS2018"]
        found_files = []
        ready_dir = ROOT_DIR / 'datasets' / 'Training_Ready'
        unsw_found_in_ready = False

        if ready_dir.exists():
            all_files = [
                f for f in ready_dir.glob('*.*')
                if f.suffix.lower() in TABULAR_EXTENSIONS
            ]
            for pattern in required_patterns:
                matches = [f for f in all_files if pattern in f.name]
                if matches:
                    found_files.extend(matches)
                    if pattern == "UNSW-NB15":
                        unsw_found_in_ready = True

        mega_model_files = list(set(found_files))

        if not mega_model_files:
            st.error("РќРµ Р·РЅР°Р№РґРµРЅРѕ РЅРµРѕР±С…С–РґРЅРёС… РґР°С‚Р°СЃРµС‚С–РІ Сѓ РїР°РїС†С– Training_Ready!")
        else:
            st.success(f"Р—РЅР°Р№РґРµРЅРѕ {len(mega_model_files)} С„Р°Р№Р»С–РІ РґР»СЏ РѕР±'С”РґРЅР°РЅРЅСЏ.")
            if unsw_found_in_ready:
                st.info("UNSW-NB15 Р·РЅР°Р№РґРµРЅРѕ Сѓ Training_Ready: Mega РЅР°РІС‡Р°С‚РёРјРµС‚СЊСЃСЏ Р· СѓСЂР°С…СѓРІР°РЅРЅСЏРј UNSW.")
            else:
                st.info(
                    "UNSW-NB15 РІС–РґСЃСѓС‚РЅС–Р№ Сѓ Training_Ready: Mega РЅР°РІС‡Р°С‚РёРјРµС‚СЊСЃСЏ Р»РёС€Рµ РЅР° РґРѕСЃС‚СѓРїРЅРёС… СЃС‚Р°Р±С–Р»СЊРЅРёС… РЅР°Р±РѕСЂР°С… "
                    "(Р±РµР· РґРѕРјС–С€СѓРІР°РЅРЅСЏ С„Р°Р№Р»С–РІ С–Р· TEST_DATA)."
                )
            # Dummy path to enable Next Step
            dataset_path = mega_model_files[0] 

    # --- SHOW ALGORITHM SELECTOR IF DATA IS READY ---
    if dataset_path:
        # РЁРІРёРґРєР° РїРµСЂРµРІС–СЂРєР° РЅР° РїРѕС‚РµРЅС†С–Р№РЅС– РїСЂРѕР±Р»РµРјРё (С‚Р°Р±Р»РёС‡РЅС– С„РѕСЂРјР°С‚Рё)
        if dataset_path.suffix.lower() in TABULAR_EXTENSIONS:
            try:
                preview_df = pd.read_csv(dataset_path, nrows=5)
                label_synonyms = FeatureRegistry.get_synonyms().get('label', [])
                suspicious = [c for c in preview_df.columns if c.lower().strip() in label_synonyms]

                if len(suspicious) > 1: # Р‘С–Р»СЊС€Рµ РЅС–Р¶ РѕРґРЅР° РєРѕР»РѕРЅРєР°, С‰Рѕ СЃС…РѕР¶Р° РЅР° Label
                    st.warning(f"РЈРІР°РіР°: Р—РЅР°Р№РґРµРЅРѕ РґРµРєС–Р»СЊРєР° РїРѕС‚РµРЅС†С–Р№РЅРёС… РєРѕР»РѕРЅРѕРє СЂРµР·СѓР»СЊС‚Р°С‚Сѓ: {suspicious}. Р¦Рµ РјРѕР¶Рµ СЃРїРѕС‚РІРѕСЂРёС‚Рё С‚РѕС‡РЅС–СЃС‚СЊ (data leakage). РЎРёСЃС‚РµРјР° СЃРїСЂРѕР±СѓС” Р°РІС‚РѕРјР°С‚РёС‡РЅРѕ РѕС‡РёСЃС‚РёС‚Рё С—С… РїСЂРё С‚СЂРµРЅСѓРІР°РЅРЅС–.")
            except Exception:
                pass

        st.markdown("<br>", unsafe_allow_html=True)

        # РљР РћРљ 2: Р’РёР±С–СЂ Р°Р»РіРѕСЂРёС‚РјСѓ Р· WIKI
        st.markdown("""
        <div class="section-card">
            <div class="section-title">РљСЂРѕРє 2: РћР±РµСЂС–С‚СЊ Р°Р»РіРѕСЂРёС‚Рј РјР°С€РёРЅРЅРѕРіРѕ РЅР°РІС‡Р°РЅРЅСЏ</div>
            <p class="text-muted mb-1">
                РЎРёСЃС‚РµРјР° Р°РІС‚РѕРјР°С‚РёС‡РЅРѕ РїС–РґР±РµСЂРµ РЅР°Р№РєСЂР°С‰С– РЅР°Р»Р°С€С‚СѓРІР°РЅРЅСЏ. 
                Р’Р°Рј РїРѕС‚СЂС–Р±РЅРѕ Р»РёС€Рµ РѕР±СЂР°С‚Рё Р°Р»РіРѕСЂРёС‚Рј.
            </p>
        </div>
        """, unsafe_allow_html=True)

        # Matrix compatibility and mode selection.
        is_mega_model = "Mega-Model" in training_strategy
        is_specialist = "РЎРїРµС†С–Р°Р»С–СЃС‚" in training_strategy
        is_multi_specialist = (is_specialist and len(selected_training_files) > 1)
        supported_algorithms = set(ModelEngine.ALGORITHMS.keys())

        selected_sources = selected_training_files if selected_training_files else (
            [dataset_path] if isinstance(dataset_path, Path) else []
        )
        selected_extensions = {src.suffix.lower() for src in selected_sources}
        is_pcap_training_source = bool(selected_extensions) and selected_extensions.issubset(PCAP_EXTENSIONS)
        is_tabular_training_source = bool(selected_extensions) and selected_extensions.issubset(TABULAR_EXTENSIONS)
        has_unlabeled_tabular = any(
            src.suffix.lower() in TABULAR_EXTENSIONS and
            ('supervised' not in set(selected_training_meta.get(str(src), {}).get('allowed_modes', [])))
            for src in selected_training_files
        )

        supervised_algorithms = [
            name for name in ["Random Forest", "XGBoost", "Logistic Regression"]
            if name in supported_algorithms
        ]
        if_only_algorithms = [name for name in ["Isolation Forest"] if name in supported_algorithms]
        all_algorithms = [name for name in ALGORITHM_WIKI.keys() if name in supported_algorithms]

        two_stage_mode = False
        scenario_title = ""
        scenario_hint = ""

        if is_mega_model:
            two_stage_mode = True
            available_algorithms = [name for name in ["Random Forest", "XGBoost"] if name in supported_algorithms]
            scenario_title = "Mega-Model: РІСЃС– РґР°С‚Р°СЃРµС‚Рё Training_Ready"
            scenario_hint = (
                "Two-Stage СѓРІС–РјРєРЅРµРЅРѕ Р°РІС‚РѕРјР°С‚РёС‡РЅРѕ. Р”РѕСЃС‚СѓРїРЅС– Р»РёС€Рµ supervised Р°Р»РіРѕСЂРёС‚РјРё "
                "РґР»СЏ Stage-1/Stage-2 (Р±РµР· Isolation Forest)."
            )
        elif is_pcap_training_source:
            available_algorithms = if_only_algorithms
            scenario_title = "PCAP (РѕРґРёРЅ Р°Р±Рѕ РґРµРєС–Р»СЊРєР° С„Р°Р№Р»С–РІ)"
            scenario_hint = "Р”РѕСЃС‚СѓРїРЅРёР№ Р»РёС€Рµ Isolation Forest (unsupervised)."
        elif is_multi_specialist and is_tabular_training_source and has_unlabeled_tabular:
            available_algorithms = if_only_algorithms
            scenario_title = "РљС–Р»СЊРєР° С‚Р°Р±Р»РёС‡РЅРёС… С„Р°Р№Р»С–РІ, С‡Р°СЃС‚РёРЅР° Р±РµР· label"
            scenario_hint = (
                "Supervised/Two-Stage РЅРµРґРѕСЃС‚СѓРїРЅС– С‡РµСЂРµР· РІС–РґСЃСѓС‚РЅС–СЃС‚СЊ label РІ С‡Р°СЃС‚РёРЅС– С„Р°Р№Р»С–РІ. "
                "Р”РѕСЃС‚СѓРїРЅРёР№ Р»РёС€Рµ Isolation Forest."
            )
        elif is_multi_specialist and is_tabular_training_source:
            two_stage_mode = True
            available_algorithms = supervised_algorithms
            scenario_title = "РљС–Р»СЊРєР° С‚Р°Р±Р»РёС‡РЅРёС… С„Р°Р№Р»С–РІ С–Р· label"
            scenario_hint = (
                "Two-Stage СѓРІС–РјРєРЅРµРЅРѕ Р°РІС‚РѕРјР°С‚РёС‡РЅРѕ. Р‘Р°Р·РѕРІРёР№ Р°Р»РіРѕСЂРёС‚Рј РѕР±РёСЂР°С”С‚СЊСЃСЏ РґР»СЏ РѕР±РѕС… РµС‚Р°РїС–РІ."
            )
        elif has_unlabeled_tabular:
            available_algorithms = if_only_algorithms
            scenario_title = "РћРґРёРЅ С‚Р°Р±Р»РёС‡РЅРёР№ С„Р°Р№Р» Р±РµР· label"
            scenario_hint = "Р”РѕСЃС‚СѓРїРЅРёР№ Р»РёС€Рµ Isolation Forest (unsupervised)."
        else:
            available_algorithms = all_algorithms
            scenario_title = "РћРґРёРЅ СЃСѓРјС–СЃРЅРёР№ С‚Р°Р±Р»РёС‡РЅРёР№ С„Р°Р№Р» С–Р· label"
            scenario_hint = "Р”РѕСЃС‚СѓРїРЅС– РІСЃС– Р°Р»РіРѕСЂРёС‚РјРё (supervised + Isolation Forest)."


--- Output 14 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
 D create_shortcut.ps1
 M requirements.txt
 M src/core/data_loader.py
 M src/core/feature_mapper.py
 M src/core/feature_validator.py
 M src/core/label_normalizer.py
 M src/core/leakage_filter.py
 M src/core/model_engine.py
 M src/core/preprocessor.py
 M src/core/two_stage_model.py
 M src/core/unit_normalizer.py
 M src/services/database.py
 M src/services/gemini_service.py
 M src/services/visualizer.py
 M src/ui/core_state.py
 M src/ui/scan_renderer.py
 M src/ui/styles.py
 M src/ui/tabs/scanning.py
 M src/ui/tabs/training.py
 M src/ui/utils/model_helpers.py
 M src/ui/utils/scan_diagnostics.py
 M src/ui/utils/training_helpers.py
?? cursor_ids_ml_analyzer.md
?? dtypes_debug.txt
?? error.log
?? fix_mojibake.py
?? logs/
?? next_agent_prompt.md
?? scripts/
?? simulation_mega_report.md
?? simulation_pcap_report.md
?? simulation_report.md
?? src/services/threat_catalog.py
warning: could not open directory '.pytest_cache/': Permission denied


--- Output 15 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
.pytest_cache
.qoder
.streamlit
.venv
.vscode
config
datasets
logs
models
scripts
skills
src
.gitignore
app_icon.ico
cursor_ids_ml_analyzer.md
dtypes_debug.txt
error.log
fix_mojibake.py
ids_history.db
next_agent_prompt.md
requirements.txt
run_app.bat
simulation_mega_report.md
simulation_pcap_report.md
simulation_report.md
start_app.py
tuned_params.txt
user_settings.json


--- Output 16 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
src/core/data_loader.py:112:        # Force family-specific mode for NSL-KDD/UNSW to preserve rich features.
src/core/data_loader.py:113:        # Unified CIC schema drops most NSL/UNSW columns and hurts detection quality.
src/core/data_loader.py:114:        if align_to_schema and dataset_type in {"NSL-KDD", "UNSW-NB15"}:
src/core/data_loader.py:134:        # 2a. UNSW-NB15 attack_cat merge (MUST run before Aligner drops non-schema cols)
src/core/data_loader.py:135:        # The 'label' column in UNSW is binary 0/1; real attack names are in 'attack_cat'.
src/core/data_loader.py:155:            print("[DIAGNOSTIC] Schema alignment skipped (family-specific mode)")
src/core/data_loader.py:190:        # In family-specific mode we validate only existing schema columns (don't add missing ones).
src/core/data_loader.py:215:            # Dataset-aware thresholds: CIC should keep richer coverage than NSL/UNSW.
src/core/data_loader.py:218:                "NSL-KDD": 0.10,
src/core/data_loader.py:219:                "UNSW-NB15": 0.18,
src/core/data_loader.py:241:                    f"[DIAGNOSTIC] Zero schema columns (expected for cross-family mapping): "
src/ui/tabs/scanning.py:36:    _infer_dataset_family_name,
src/ui/tabs/scanning.py:38:    _resolve_two_stage_profile_threshold,
src/ui/tabs/scanning.py:39:    _clamp_two_stage_threshold,
src/ui/tabs/scanning.py:40:    _normalize_two_stage_profile,
src/ui/tabs/scanning.py:41:    _threshold_to_sensitivity_level,
src/ui/tabs/scanning.py:42:    _sensitivity_level_to_threshold,
src/ui/tabs/scanning.py:44:    detect_scan_file_family_info
src/ui/tabs/scanning.py:160:            'two_stage_threshold_default': float(DEFAULT_SENSITIVITY_THRESHOLD),
src/ui/tabs/scanning.py:162:            'two_stage_profile_default': DEFAULT_TWO_STAGE_PROFILE,
src/ui/tabs/scanning.py:163:            'two_stage_threshold_strict': _resolve_two_stage_profile_threshold(
src/ui/tabs/scanning.py:193:        file_family = _infer_dataset_family_name(file_path.name)
src/ui/tabs/scanning.py:194:        file_family_confidence = 0.55 if file_family else 0.0
src/ui/tabs/scanning.py:195:        file_family_ambiguous = False
src/ui/tabs/scanning.py:198:            family_info = detect_scan_file_family_info(
src/ui/tabs/scanning.py:203:            detected_family = str(family_info.get('family', ''))
src/ui/tabs/scanning.py:204:            if detected_family:
src/ui/tabs/scanning.py:205:                file_family = detected_family
src/ui/tabs/scanning.py:206:            file_family_confidence = float(family_info.get('confidence', file_family_confidence))
src/ui/tabs/scanning.py:207:            file_family_ambiguous = bool(family_info.get('ambiguous', False))
src/ui/tabs/scanning.py:222:                family_from_name = _infer_dataset_family_name(candidate)
src/ui/tabs/scanning.py:223:                if family_from_name:
src/ui/tabs/scanning.py:224:                    trained_families.add(family_from_name)
src/ui/tabs/scanning.py:255:                family_reliable = bool(file_family) and (not file_family_ambiguous) and file_family_confidence >= 0.60
src/ui/tabs/scanning.py:256:                if family_reliable:
src/ui/tabs/scanning.py:257:                    if file_family in trained_families:
src/ui/tabs/scanning.py:258:                        score += 260.0 * max(0.35, min(1.0, file_family_confidence))
src/ui/tabs/scanning.py:259:                        reason = 'family_match_two_stage' if is_two_stage else 'family_match'
src/ui/tabs/scanning.py:262:                elif file_family and trained_families:
src/ui/tabs/scanning.py:263:                    if file_family in trained_families:
src/ui/tabs/scanning.py:265:                    reason = 'family_ambiguous'
src/ui/tabs/scanning.py:301:                help="Система автоматично обере: IF для PCAP, RF/XGBoost/Two-Stage для CSV/NF"
src/ui/tabs/scanning.py:425:            # Show sensitivity control only when selected model supports Two-Stage thresholding.
src/ui/tabs/scanning.py:433:                'two_stage_threshold_default': float(DEFAULT_SENSITIVITY_THRESHOLD),
src/ui/tabs/scanning.py:435:                'two_stage_profile_default': DEFAULT_TWO_STAGE_PROFILE,
src/ui/tabs/scanning.py:436:                'two_stage_threshold_strict': _resolve_two_stage_profile_threshold(
src/ui/tabs/scanning.py:443:            selected_two_stage_threshold = float(DEFAULT_SENSITIVITY_THRESHOLD)
src/ui/tabs/scanning.py:444:            selected_two_stage_profile_key = DEFAULT_TWO_STAGE_PROFILE
src/ui/tabs/scanning.py:445:            selected_two_stage_profile_label = TWO_STAGE_PROFILE_RULES[selected_two_stage_profile_key]['label']
src/ui/tabs/scanning.py:448:                    model_mode = 'Two-Stage'
src/ui/tabs/scanning.py:459:                    "NSL-KDD: тестові файли часто містять нові типи атак, яких не було у тренуванні. "
src/ui/tabs/scanning.py:466:                default_threshold = _clamp_two_stage_threshold(
src/ui/tabs/scanning.py:467:                    float(preview_manifest.get('two_stage_threshold_default', DEFAULT_SENSITIVITY_THRESHOLD))
src/ui/tabs/scanning.py:469:                strict_threshold = _clamp_two_stage_threshold(
src/ui/tabs/scanning.py:472:                            'two_stage_threshold_strict',
src/ui/tabs/scanning.py:473:                            _resolve_two_stage_profile_threshold(default_threshold, "strict")
src/ui/tabs/scanning.py:480:                default_profile_key = _normalize_two_stage_profile(DEFAULT_TWO_STAGE_PROFILE)
src/ui/tabs/scanning.py:481:                profile_labels = [TWO_STAGE_PROFILE_RULES[key]['label'] for key in TWO_STAGE_PROFILE_ORDER]
src/ui/tabs/scanning.py:482:                profile_label_to_key = {
src/ui/tabs/scanning.py:485:                profile_index = TWO_STAGE_PROFILE_ORDER.index(default_profile_key)
src/ui/tabs/scanning.py:486:                selected_two_stage_profile_label = st.radio(
src/ui/tabs/scanning.py:488:                    options=profile_labels,
src/ui/tabs/scanning.py:489:                    index=profile_index,
src/ui/tabs/scanning.py:496:                selected_two_stage_profile_key = profile_label_to_key[selected_two_stage_profile_label]
src/ui/tabs/scanning.py:498:                if selected_two_stage_profile_key == "strict":
src/ui/tabs/scanning.py:499:                    selected_two_stage_threshold = strict_threshold
src/ui/tabs/scanning.py:501:                    selected_two_stage_threshold = default_threshold
src/ui/tabs/scanning.py:503:                base_sensitivity_level = _threshold_to_sensitivity_level(selected_two_stage_threshold)
src/ui/tabs/scanning.py:522:                    selected_two_stage_threshold = _sensitivity_level_to_threshold(sensitivity_level)
src/ui/tabs/scanning.py:523:                    selected_two_stage_profile_label = f"{selected_two_stage_profile_label} + ручне"
src/ui/tabs/scanning.py:527:                active_profile_label = TWO_STAGE_PROFILE_RULES[selected_two_stage_profile_key]['label']
src/ui/tabs/scanning.py:530:                        f"Поточний режим: {active_profile_label}. "
src/ui/tabs/scanning.py:535:                        f"Поточний режим: {active_profile_label}. "
src/ui/tabs/scanning.py:552:                st.caption("Параметр чутливості приховано: він працює лише для Two-Stage моделей.")
src/ui/tabs/scanning.py:558:            # Fast compatibility gate: auto-reroute to compatible model when possible.
src/ui/tabs/scanning.py:624:                    d4.metric("Прогноз якості детекції", f"{int(checks.get('quality_score', readiness.get('score', 0)))}/100")
src/ui/tabs/scanning.py:628:                        "Прогноз якості детекції = оцінка ризику FP/FN та OOD на тестовій preview-вибірці. "
src/ui/tabs/scanning.py:649:                        drift_cov = float(checks.get('distribution_profile_coverage', 0.0))
src/ui/tabs/scanning.py:651:                            f"• OOD-дрейф розподілу: **{drift_score:.2f}** "
src/ui/tabs/scanning.py:654:                    if 'two_stage_threshold' in checks:
src/ui/tabs/scanning.py:655:                        profile_default_preview = _normalize_two_stage_profile(
src/ui/tabs/scanning.py:656:                            checks.get('two_stage_profile_default', DEFAULT_TWO_STAGE_PROFILE)
src/ui/tabs/scanning.py:658:                        profile_default_label = TWO_STAGE_PROFILE_RULES[profile_default_preview]['label']
src/ui/tabs/scanning.py:660:                            f"• Рекомендований профіль моделі: **{profile_default_label}**. "
src/ui/tabs/scanning.py:730:                        elif auto_reason == 'family_match_two_stage':
src/ui/tabs/scanning.py:731:                            st.success(f"Автовибір за сімейством датасету: **{selected_model}** (Two-Stage)")
src/ui/tabs/scanning.py:732:                        elif auto_reason == 'family_match':
src/ui/tabs/scanning.py:734:                        elif auto_reason == 'family_ambiguous':
src/ui/tabs/scanning.py:737:                            st.success(f"Автовибір для CSV/NF: **{selected_model}** (Two-Stage)")
src/ui/tabs/scanning.py:769:                            "Для CSV/NF зазвичай точніші моделі класифікації (Random Forest / Two-Stage). "
src/ui/tabs/scanning.py:814:                    align_to_schema = schema_mode != 'family'
src/ui/tabs/scanning.py:838:                    # Add family hint if the model was trained on multiple families.
src/ui/tabs/scanning.py:839:                    if hasattr(preprocessor, 'feature_columns') and 'family_hint' in preprocessor.feature_columns:
src/ui/tabs/scanning.py:840:                        file_family = _infer_dataset_family_name(dataset_path.name)
src/ui/tabs/scanning.py:844:                                fam_info = detect_scan_file_family_info(str(dataset_path), stat.st_mtime, stat.st_size)
src/ui/tabs/scanning.py:845:                                detected_family = str(fam_info.get('family', '')).strip()
src/ui/tabs/scanning.py:846:                                if detected_family:
src/ui/tabs/scanning.py:847:                                    file_family = detected_family
src/ui/tabs/scanning.py:851:                        df['family_hint'] = file_family or "Unknown"
src/ui/tabs/scanning.py:940:                        predictions = model.predict(X, threshold=selected_two_stage_threshold)
src/ui/tabs/scanning.py:942:                            "Використано Two-Stage Detection "
src/ui/tabs/scanning.py:943:                            f"(Профіль: {selected_two_stage_profile_label}, "
src/ui/tabs/scanning.py:944:                            f"Threshold: {selected_two_stage_threshold:.2f}, "
src/ui/tabs/scanning.py:948:                        # Use engine.predict() which handles IF correctly with threshold
src/ui/tabs/scanning.py:963:                        if hasattr(engine, 'if_threshold_') and engine.if_threshold_ is not None:
src/ui/tabs/scanning.py:964:                            threshold_mode = getattr(engine, 'if_threshold_mode_', 'decision_zero')
src/ui/tabs/scanning.py:967:                                f"(IF threshold: {engine.if_threshold_:.4f}, mode: {threshold_mode})"
src/ui/tabs/scanning.py:996:                                adaptive_threshold = float(np.quantile(scores, capped_rate))
src/ui/tabs/scanning.py:997:                                predictions = np.where(scores < adaptive_threshold, 1, 0).astype(int)
src/ui/tabs/scanning.py:1005:                                    f"adaptive_threshold={adaptive_threshold:.6f}"
src/ui/tabs/scanning.py:1054:                                        adaptive_threshold = float(np.quantile(scores, floor_rate))
src/ui/tabs/scanning.py:1055:                                        adaptive_mask = scores <= adaptive_threshold
src/ui/tabs/scanning.py:1067:                                            f"adaptive_threshold={adaptive_threshold:.6f}"
src/ui/tabs/scanning.py:1073:                    # if model returns almost all-benign, run lightweight OOD detector and mark suspicious flows.
src/ui/tabs/scanning.py:1089:                                # OOD rescue must stay lightweight on very large scans.
src/ui/tabs/scanning.py:1102:                                        "OOD-перевірку виконано на репрезентативній вибірці великого файлу "
src/ui/tabs/scanning.py:1108:                                aux_threshold = float(np.quantile(aux_scores, 0.02))
src/ui/tabs/scanning.py:1110:                                local_mask = np.asarray(aux_scores <= aux_threshold, dtype=bool)
src/ui/tabs/scanning.py:1118:                                    # Тип атаки не вигадуємо: пізніше позначимо ці рядки як "Аномалія (OOD)".
src/ui/tabs/scanning.py:1124:                                        "Додатково застосовано OOD-перевірку для підозрілих відхилень."
src/ui/tabs/scanning.py:1127:                                        f"[LOG] OOD rescue applied: base_rate={base_rate:.6f}, "
src/ui/tabs/scanning.py:1129:                                        f"aux_threshold={aux_threshold:.6f}"
src/ui/tabs/scanning.py:1132:                                print(f"[WARN] OOD rescue skipped: {aux_exc}")
src/ui/tabs/scanning.py:1137:                        print(f"[LOG] OOD rescue benign overrides: {ood_rescued_count}")
src/ui/tabs/scanning.py:1197:                            predictions_decoded[idx] = "Аномалія (OOD)"
src/ui/tabs/scanning.py:1200:                            print(f"[LOG] OOD rescue relabeled benign flows: {patched}")
src/ui/tabs/scanning.py:1232:                    # Safety: force OOD-rescued rows into anomaly counters even if label decoding changes.
src/core/model_engine.py:154:    def _assert_supervised_label_quality(y: pd.Series, algorithm: str) -> None:
src/core/model_engine.py:208:            self._assert_supervised_label_quality(y_fit, algorithm)
src/core/model_engine.py:338:            # Use this native boundary to avoid unstable percentile re-thresholding.
src/core/model_engine.py:344:            # Canonical threshold for decision_function
src/core/model_engine.py:345:            self.if_threshold_ = 0.0
src/core/model_engine.py:346:            self.if_threshold_mode_ = "decision_zero"
src/core/model_engine.py:354:                'threshold': float(self.if_threshold_),
src/core/model_engine.py:356:                'threshold_mode': self.if_threshold_mode_
src/core/model_engine.py:360:            logger.info(f"[ModelEngine] IF threshold mode={self.if_threshold_mode_}, threshold={self.if_threshold_:.4f}")
src/core/model_engine.py:363:            self._assert_supervised_label_quality(y_fit, algorithm)
src/core/model_engine.py:377:            self.if_threshold_ = None
src/core/model_engine.py:378:            self.if_threshold_mode_ = None
src/core/model_engine.py:385:    def auto_calibrate_isolation_threshold(
src/core/model_engine.py:394:        Auto-calibrate decision threshold for Isolation Forest to reduce manual tuning.
src/core/model_engine.py:425:        candidate_threshold = float(np.quantile(scores, target_fp_rate))
src/core/model_engine.py:437:            # Search threshold by F2 (recall-focused) with FP guard.
src/core/model_engine.py:470:                        'threshold': float(thr),
src/core/model_engine.py:484:                candidate_threshold = float(best_stats['threshold'])
src/core/model_engine.py:488:        current_rate = float(np.mean(scores < candidate_threshold))
src/core/model_engine.py:490:            candidate_threshold = float(np.quantile(scores, min_anomaly_rate))
src/core/model_engine.py:492:            current_rate = float(np.mean(scores < candidate_threshold))
src/core/model_engine.py:494:            candidate_threshold = float(np.quantile(scores, max_anomaly_rate))
src/core/model_engine.py:496:            current_rate = float(np.mean(scores < candidate_threshold))
src/core/model_engine.py:498:        self.if_threshold_ = float(candidate_threshold)
src/core/model_engine.py:499:        self.if_threshold_mode_ = calibration_mode
src/core/model_engine.py:507:            'threshold': float(self.if_threshold_),
src/core/model_engine.py:508:            'threshold_mode': self.if_threshold_mode_,
src/core/model_engine.py:518:            f"[ModelEngine] IF auto calibration: mode={self.if_threshold_mode_}, "
src/core/model_engine.py:519:            f"threshold={self.if_threshold_:.6f}, anomaly_rate={current_rate:.4f}, "
src/core/model_engine.py:524:            'threshold': float(self.if_threshold_),
src/core/model_engine.py:525:            'mode': self.if_threshold_mode_,
src/core/model_engine.py:601:        self._assert_supervised_label_quality(y_fit, config.algorithm)
src/core/model_engine.py:671:                'class_weight': 'balanced_subsample',
src/core/model_engine.py:684:                'class_weight': 'balanced'
src/core/model_engine.py:765:                threshold = self.if_threshold_ if getattr(self, 'if_threshold_', None) is not None else 0.0
src/core/model_engine.py:766:                predictions = np.where(scores < threshold, 1, 0).astype(int)
src/core/model_engine.py:769:                    f"threshold={threshold:.4f}, mode={getattr(self, 'if_threshold_mode_', 'decision_zero')}"
src/core/model_engine.py:825:            if hasattr(self, 'if_threshold_') and self.if_threshold_ is not None:
src/core/model_engine.py:826:                bundle['if_threshold'] = self.if_threshold_
src/core/model_engine.py:827:                logger.info(f"[ModelEngine] Saving IF threshold: {self.if_threshold_:.4f}")
src/core/model_engine.py:828:                bundle['if_threshold_mode'] = getattr(self, 'if_threshold_mode_', 'decision_zero')
src/core/model_engine.py:869:                if 'if_threshold' in loaded:
src/core/model_engine.py:870:                    loaded_mode = loaded.get('if_threshold_mode')
src/core/model_engine.py:871:                    loaded_threshold = float(loaded['if_threshold'])
src/core/model_engine.py:874:                    # legacy models stored percentile-based threshold for decision_function.
src/core/model_engine.py:875:                    # That threshold was unstable, so we normalize to canonical 0.0 boundary.
src/core/model_engine.py:877:                        self.if_threshold_ = 0.0
src/core/model_engine.py:878:                        self.if_threshold_mode_ = "decision_zero_legacy_normalized"
src/core/model_engine.py:880:                            "[ModelEngine] Legacy IF threshold detected and normalized to 0.0 "
src/core/model_engine.py:881:                            f"(legacy value={loaded_threshold:.4f})"
src/core/model_engine.py:884:                        self.if_threshold_ = float(loaded_threshold)
src/core/model_engine.py:885:                        self.if_threshold_mode_ = loaded_mode
src/core/model_engine.py:887:                            f"[ModelEngine] Restored IF threshold: {self.if_threshold_:.4f} "
src/core/model_engine.py:888:                            f"(mode={self.if_threshold_mode_})"
src/core/model_engine.py:891:                    self.if_threshold_ = 0.0
src/core/model_engine.py:892:                    self.if_threshold_mode_ = "decision_zero_default"
src/core/model_engine.py:893:                    logger.warning("[ModelEngine] IF model loaded without threshold, using default 0.0")
src/ui/tabs/training.py:25:    _calibrate_two_stage_threshold,
src/ui/tabs/training.py:28:    _evaluate_training_quality_gate
src/ui/tabs/training.py:30:from src.ui.utils.scan_diagnostics import _build_training_distribution_profile
src/ui/tabs/training.py:32:    _infer_dataset_family_name,
src/ui/tabs/training.py:34:    _resolve_two_stage_profile_threshold,
src/ui/tabs/training.py:35:    detect_scan_file_family_info
src/ui/tabs/training.py:177:            "1) Two-Stage для CSV/NF, 2) Isolation Forest для PCAP."
src/ui/tabs/training.py:225:            # 1) TABULAR MODEL (Two-Stage, авто-вибір алгоритму)
src/ui/tabs/training.py:227:            smart_log("Крок 1/2: Формуємо універсальну tabular-модель (Two-Stage, авто-вибір алгоритму)...")
src/ui/tabs/training.py:297:            tab_threshold_info: dict[str, Any] = {
src/ui/tabs/training.py:298:                'threshold': float(DEFAULT_SENSITIVITY_THRESHOLD),
src/ui/tabs/training.py:319:                    smart_log(f"Спроба Two-Stage моделі на базі {algo}...")
src/ui/tabs/training.py:327:                    local_threshold_info = _calibrate_two_stage_threshold(
src/ui/tabs/training.py:333:                    y_pred_tab = local_model.predict(X_test_tab, threshold=float(local_threshold_info['threshold']))
src/ui/tabs/training.py:345:                        f"Threshold={float(local_threshold_info['threshold']):.2f}"
src/ui/tabs/training.py:352:                        tab_threshold_info = local_threshold_info
src/ui/tabs/training.py:356:                    smart_log(f"Обрано найкращий алгоритм для Two-Stage: {tab_best_algo}")
src/ui/tabs/training.py:358:                        "Two-Stage авто-поріг: "
src/ui/tabs/training.py:359:                        f"{float(tab_threshold_info['threshold']):.2f} "
src/ui/tabs/training.py:360:                        f"(F1_attack={float(tab_threshold_info['f1_attack']):.3f}, "
src/ui/tabs/training.py:361:                        f"F2_attack={float(tab_threshold_info.get('f2_attack', 0.0)):.3f}, "
src/ui/tabs/training.py:362:                        f"Recall_attack={float(tab_threshold_info['recall_attack']):.3f})"
src/ui/tabs/training.py:365:                    engine_tab.algorithm_name = f"Two-Stage ({tab_best_algo})"
src/ui/tabs/training.py:368:                    smart_log("! Не вдалося стабільно підібрати Two-Stage модель. Створено лише IF-модель.")
src/ui/tabs/training.py:370:                smart_log("! Недостатньо класів для Two-Stage. Створено лише IF-модель.")
src/ui/tabs/training.py:384:                        'two_stage_threshold_default': float(tab_threshold_info['threshold']),
src/ui/tabs/training.py:385:                        'two_stage_profile_default': DEFAULT_TWO_STAGE_PROFILE,
src/ui/tabs/training.py:386:                        'two_stage_threshold_strict': _resolve_two_stage_profile_threshold(
src/ui/tabs/training.py:387:                            float(tab_threshold_info['threshold']),
src/ui/tabs/training.py:390:                        'two_stage_threshold_calibration': {
src/ui/tabs/training.py:391:                            'f1_attack': float(tab_threshold_info.get('f1_attack', 0.0)),
src/ui/tabs/training.py:392:                            'f2_attack': float(tab_threshold_info.get('f2_attack', 0.0)),
src/ui/tabs/training.py:393:                            'precision_attack': float(tab_threshold_info.get('precision_attack', 0.0)),
src/ui/tabs/training.py:394:                            'recall_attack': float(tab_threshold_info.get('recall_attack', 0.0)),
src/ui/tabs/training.py:395:                            'evaluated_points': int(tab_threshold_info.get('evaluated_points', 0)),
src/ui/tabs/training.py:396:                            'objective': float(tab_threshold_info.get('objective', 0.0)),
src/ui/tabs/training.py:400:                        'description': 'Smart one-click Two-Stage model for CSV/NF'
src/ui/tabs/training.py:471:                calib_info = engine_if.auto_calibrate_isolation_threshold(
src/ui/tabs/training.py:478:                    f"threshold={float(calib_info.get('threshold', 0.0)):.4f}"
src/ui/tabs/training.py:481:                calib_info = engine_if.auto_calibrate_isolation_threshold(
src/ui/tabs/training.py:502:                    'if_threshold_mode': getattr(engine_if, 'if_threshold_mode_', 'decision_zero'),
src/ui/tabs/training.py:573:    mega_model_files = []
src/ui/tabs/training.py:685:            family_set = {
src/ui/tabs/training.py:689:            if len(family_set) > 1:
src/ui/tabs/training.py:712:                <li>UNSW-NB15 (Складні патерни)</li>
src/ui/tabs/training.py:719:        required_patterns = ["CIC-IDS2017", "UNSW-NB15", "CIC-IDS2018"]
src/ui/tabs/training.py:733:                    if pattern == "UNSW-NB15":
src/ui/tabs/training.py:736:        mega_model_files = list(set(found_files))
src/ui/tabs/training.py:738:        if not mega_model_files:
src/ui/tabs/training.py:741:            st.success(f"Знайдено {len(mega_model_files)} файлів для об'єднання.")
src/ui/tabs/training.py:743:                st.info("UNSW-NB15 знайдено у Training_Ready: Mega навчатиметься з урахуванням UNSW.")
src/ui/tabs/training.py:746:                    "UNSW-NB15 відсутній у Training_Ready: Mega навчатиметься лише на доступних стабільних наборах "
src/ui/tabs/training.py:750:            dataset_path = mega_model_files[0] 
src/ui/tabs/training.py:780:        is_mega_model = "Mega-Model" in training_strategy
src/ui/tabs/training.py:808:        if is_mega_model:
src/ui/tabs/training.py:813:                "Two-Stage увімкнено автоматично. Доступні лише supervised алгоритми "
src/ui/tabs/training.py:824:                "Supervised/Two-Stage недоступні через відсутність label в частині файлів. "
src/ui/tabs/training.py:832:                "Two-Stage увімкнено автоматично. Базовий алгоритм обирається для обох етапів."
src/ui/tabs/training.py:936:                "У Two-Stage режимі параметри Turbo/Search вимкнені: "
src/ui/tabs/training.py:980:                    st.caption("Метод пошуку вимкнено: Two-Stage тренування працює без Grid/Random Search.")
src/ui/tabs/training.py:1090:                f"**Модель:** Two-Stage Detection ({algorithm})",
src/ui/tabs/training.py:1126:                <b>Two-Stage Detection увімкнено автоматично</b><br>
src/ui/tabs/training.py:1161:                def _resolve_family_for_file(path: Path) -> str:
src/ui/tabs/training.py:1164:                        info = detect_scan_file_family_info(str(path), stat.st_mtime, stat.st_size)
src/ui/tabs/training.py:1165:                        fam = str(info.get('family', '')).strip()
src/ui/tabs/training.py:1170:                    return _infer_dataset_family_name(path.name)
src/ui/tabs/training.py:1192:                def _min_coverage_for_family(family_name: str) -> float:
src/ui/tabs/training.py:1195:                        "UNSW-NB15": 0.12,
src/ui/tabs/training.py:1196:                        "NSL-KDD": 0.08,
src/ui/tabs/training.py:1197:                    }.get(family_name, 0.20)
src/ui/tabs/training.py:1199:                if is_mega_model:
src/ui/tabs/training.py:1200:                    files_for_training = list(mega_model_files)
src/ui/tabs/training.py:1206:                detected_families = [_resolve_family_for_file(f) for f in files_for_training]
src/ui/tabs/training.py:1208:                unknown_family = any(not fam for fam in detected_families)
src/ui/tabs/training.py:1209:                mega_multi_family = bool(is_mega_model and len(known_families) > 1 and not is_pcap_training_source)
src/ui/tabs/training.py:1210:                multi_family = len(known_families) > 1
src/ui/tabs/training.py:1211:                family_mode_required = any(fam in {"NSL-KDD", "UNSW-NB15"} for fam in known_families)
src/ui/tabs/training.py:1214:                    or ((multi_family and not is_mega_model) and not family_mode_required)
src/ui/tabs/training.py:1216:                if family_mode_required:
src/ui/tabs/training.py:1219:                if unknown_family:
src/ui/tabs/training.py:1225:                if family_mode_required:
src/ui/tabs/training.py:1227:                        "Для NSL-KDD/UNSW-NB15 увімкнено сімейний режим ознак, "
src/ui/tabs/training.py:1230:                    if multi_family and not is_mega_model:
src/ui/tabs/training.py:1232:                            "Обрано різні сімейства (NSL/UNSW + інші). "
src/ui/tabs/training.py:1243:                    if mega_multi_family:
src/ui/tabs/training.py:1257:                    merge_caption = "Mega-Model" if is_mega_model else "Вибрані датасети"
src/ui/tabs/training.py:1262:                    base_total_budget = 140000 if is_mega_model else 120000
src/ui/tabs/training.py:1264:                        base_total_budget = 280000 if is_mega_model else 200000
src/ui/tabs/training.py:1266:                    files_by_family: dict[str, list[Path]] = {}
src/ui/tabs/training.py:1269:                        files_by_family.setdefault(key, []).append(f)
src/ui/tabs/training.py:1270:                    if is_mega_model and "Generic" in files_by_family:
src/ui/tabs/training.py:1275:                        files_by_family.pop("Generic", None)
src/ui/tabs/training.py:1277:                    family_keys = list(files_by_family.keys())
src/ui/tabs/training.py:1278:                    family_budget = max(20000, int(base_total_budget / max(1, len(family_keys))))
src/ui/tabs/training.py:1281:                    for family_key, family_files in files_by_family.items():
src/ui/tabs/training.py:1282:                        remaining_family = family_budget
src/ui/tabs/training.py:1283:                        per_file_budget = max(5000, int(np.ceil(family_budget / max(1, len(family_files)))))
src/ui/tabs/training.py:1286:                        for f in family_files:
src/ui/tabs/training.py:1287:                            if remaining_family <= 0:
src/ui/tabs/training.py:1292:                                    max_rows=min(per_file_budget, remaining_family),
src/ui/tabs/training.py:1296:                                if multi_family:
src/ui/tabs/training.py:1298:                                    df_part["family_hint"] = family_key or "Unknown"
src/ui/tabs/training.py:1300:                                family_name = _infer_dataset_family_name(f.name)
src/ui/tabs/training.py:1301:                                min_cov = _min_coverage_for_family(family_name)
src/ui/tabs/training.py:1302:                                if align_to_schema and is_mega_model and coverage < min_cov:
src/ui/tabs/training.py:1304:                                        f"{f.name}: низька сумісність ознак ({coverage:.0%}) для {family_name or 'Generic'} "
src/ui/tabs/training.py:1307:                                    add_log(f"Пропущено {f.name}: coverage={coverage:.2f}, family={family_name or 'Generic'}")
src/ui/tabs/training.py:1318:                                remaining_family -= len(df_part)
src/ui/tabs/training.py:1348:                        # IMPORTANT: quality depends on how "normal" this file is.
src/ui/tabs/training.py:1441:                if is_mega_model and len(y_train):
src/ui/tabs/training.py:1507:                two_stage_threshold_info: dict[str, Any] = {
src/ui/tabs/training.py:1508:                    'threshold': float(DEFAULT_SENSITIVITY_THRESHOLD),
src/ui/tabs/training.py:1529:                            "Two-Stage вимкнено — використовується звичайна класифікація."
src/ui/tabs/training.py:1531:                        add_log("⚠ BENIGN клас не знайдено — Two-Stage вимкнено (fallback до supervised).")
src/ui/tabs/training.py:1546:                        add_log(f"Визначено BENIGN код для Two-Stage: {benign_code_for_two_stage}")
src/ui/tabs/training.py:1549:                        add_log("⚠️ BENIGN код не визначено однозначно; використовується евристика Two-Stage")
src/ui/tabs/training.py:1552:                    two_stage_threshold_info = _calibrate_two_stage_threshold(
src/ui/tabs/training.py:1559:                        "Two-Stage авто-поріг: "
src/ui/tabs/training.py:1560:                        f"{float(two_stage_threshold_info['threshold']):.2f} "
src/ui/tabs/training.py:1561:                        f"(F1_attack={float(two_stage_threshold_info['f1_attack']):.3f}, "
src/ui/tabs/training.py:1562:                        f"F2_attack={float(two_stage_threshold_info.get('f2_attack', 0.0)):.3f}, "
src/ui/tabs/training.py:1563:                        f"Recall_attack={float(two_stage_threshold_info['recall_attack']):.3f})"
src/ui/tabs/training.py:1567:                    engine.algorithm_name = f"Two-Stage ({algorithm})"
src/ui/tabs/training.py:1569:                        'best_params': 'Default (Two-Stage Mode)',
src/ui/tabs/training.py:1571:                        'two_stage_threshold': two_stage_threshold_info
src/ui/tabs/training.py:1638:                        calibration_info = engine.auto_calibrate_isolation_threshold(
src/ui/tabs/training.py:1646:                            f"threshold={float(calibration_info.get('threshold', 0.0)):.4f}, "
src/ui/tabs/training.py:1650:                        engine.if_threshold_ = 0.0
src/ui/tabs/training.py:1651:                        engine.if_threshold_mode_ = "decision_zero_manual"
src/ui/tabs/training.py:1653:                            'mode': engine.if_threshold_mode_,
src/ui/tabs/training.py:1654:                            'threshold': float(engine.if_threshold_),
src/ui/tabs/training.py:1725:                        list(mega_model_files)
src/ui/tabs/training.py:1726:                        if is_mega_model
src/ui/tabs/training.py:1731:                    fam for fam in (_infer_dataset_family_name(Path(p).name) for p in training_files_used) if fam
src/ui/tabs/training.py:1733:                training_distribution_profile = _build_training_distribution_profile(
src/ui/tabs/training.py:1739:                effective_is_mega = bool(is_mega_model and len(trained_families_used) >= 2)
src/ui/tabs/training.py:1740:                if is_mega_model and not effective_is_mega:
src/ui/tabs/training.py:1746:                quality_gate = _evaluate_training_quality_gate(
src/ui/tabs/training.py:1750:                    is_mega_model=effective_is_mega,
src/ui/tabs/training.py:1751:                    trained_family_count=len(trained_families_used),
src/ui/tabs/training.py:1758:                schema_mode = "unified" if align_to_schema else "family"
src/ui/tabs/training.py:1764:                        if effective_is_mega
src/ui/tabs/training.py:1773:                    'training_distribution_profile': training_distribution_profile,
src/ui/tabs/training.py:1785:                    model_metadata['if_threshold_mode'] = getattr(engine, 'if_threshold_mode_', 'decision_zero')
src/ui/tabs/training.py:1787:                    model_metadata['two_stage_threshold_default'] = float(two_stage_threshold_info.get('threshold', DEFAULT_SENSITIVITY_THRESHOLD))
src/ui/tabs/training.py:1790:                    model_metadata['two_stage_profile_default'] = DEFAULT_TWO_STAGE_PROFILE
src/ui/tabs/training.py:1791:                    model_metadata['two_stage_threshold_strict'] = _resolve_two_stage_profile_threshold(
src/ui/tabs/training.py:1792:                        float(model_metadata['two_stage_threshold_default']),
src/ui/tabs/training.py:1795:                    model_metadata['two_stage_threshold_calibration'] = {
src/ui/tabs/training.py:1796:                        'f1_attack': float(two_stage_threshold_info.get('f1_attack', 0.0)),
src/ui/tabs/training.py:1797:                        'f2_attack': float(two_stage_threshold_info.get('f2_attack', 0.0)),
src/ui/tabs/training.py:1798:                        'precision_attack': float(two_stage_threshold_info.get('precision_attack', 0.0)),
src/ui/tabs/training.py:1799:                        'recall_attack': float(two_stage_threshold_info.get('recall_attack', 0.0)),
src/ui/tabs/training.py:1800:                        'evaluated_points': int(two_stage_threshold_info.get('evaluated_points', 0)),
src/ui/tabs/training.py:1801:                        'objective': float(two_stage_threshold_info.get('objective', 0.0)),
src/ui/tabs/training.py:1805:                model_metadata['quality_gate'] = quality_gate
src/ui/tabs/training.py:1818:                if not quality_gate.get('passed', False):
src/ui/tabs/training.py:1819:                    quality_score = int(quality_gate.get('score', 0))
src/ui/tabs/training.py:1820:                    allow_override = bool(is_mega_model or quality_score >= 55)
src/ui/tabs/training.py:1824:                            f"Оцінка Quality Gate: {quality_score}/100. "
src/ui/tabs/training.py:1827:                        for reason in quality_gate.get('failures', [])[:6]:
src/ui/tabs/training.py:1830:                            f"Quality Gate FAIL ({quality_score}/100): "
src/ui/tabs/training.py:1831:                            + "; ".join(quality_gate.get('failures', []))
src/ui/tabs/training.py:1840:                            f"Quality Gate OVERRIDE ({quality_score}/100): "
src/ui/tabs/training.py:1841:                            + "; ".join(quality_gate.get('failures', []))
src/ui/tabs/training.py:1843:                        quality_gate['override_saved'] = True
src/ui/tabs/training.py:1845:                    add_log(f"Quality Gate PASS ({int(quality_gate.get('score', 0))}/100)")


--- Output 17 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
                                            "Увімкнено додаткову евристичну перевірку мережевих флувів."
                                        )
                                        print(
                                            f"[LOG] IF PCAP rescue applied: "
                                            f"if_hits={raw_if_anomalies}, heuristic_hits={heuristic_hits}, "
                                            f"combined_hits={int(np.sum(predictions == 1))}"
                                        )

                                combined_hits = int(np.sum(predictions == 1))
                                if combined_hits < min_expected and 'scores' in locals():
                                    syn = (
                                        pd.to_numeric(base_df['tcp_syn_count'], errors='coerce').fillna(0)
                                        if 'tcp_syn_count' in base_df.columns
                                        else pd.Series(0, index=base_df.index, dtype=float)
                                    )
                                    ack = (
                                        pd.to_numeric(base_df['tcp_ack_count'], errors='coerce').fillna(0)
                                        if 'tcp_ack_count' in base_df.columns
                                        else pd.Series(0, index=base_df.index, dtype=float)
                                    )
                                    bwd = (
                                        pd.to_numeric(base_df['packets_bwd'], errors='coerce').fillna(0)
                                        if 'packets_bwd' in base_df.columns
                                        else pd.Series(0, index=base_df.index, dtype=float)
                                    )
                                    pcap_suspicion = float(np.mean((syn >= 1) & (ack <= 0) & (bwd <= 0)))

                                    if pcap_suspicion >= 0.05:
                                        floor_rate = float(np.clip(
                                            (metadata or {}).get('if_min_anomaly_rate', 0.02),
                                            0.005,
                                            0.10
                                        ))
                                        adaptive_threshold = float(np.quantile(scores, floor_rate))
                                        adaptive_mask = scores <= adaptive_threshold
                                        predictions = np.where((predictions == 1) | adaptive_mask, 1, 0).astype(int)
                                        st.warning(
                                            "Для підозрілого PCAP застосовано адаптивний поріг IF, "
                                            "щоб зменшити ризик пропуску атак."
                                        )
                                        print(
                                            f"[LOG] IF PCAP adaptive floor applied: "
                                            f"old_hits={combined_hits}, "
                                            f"new_hits={int(np.sum(predictions == 1))}, "
                                            f"suspicion={pcap_suspicion:.3f}, "
                                            f"floor_rate={floor_rate:.4f}, "
                                            f"adaptive_threshold={adaptive_threshold:.6f}"
                                        )

                    raw_anomalies = int(np.sum(predictions == 1)) if is_isolation_forest else int(np.sum(predictions != 0))

                    # Rescue detection for tabular/supervised runs:
                    # if model returns almost all-benign, run lightweight OOD detector and mark suspicious flows.
                    if (not is_isolation_forest) and file_ext in TABULAR_EXTENSIONS:
                        base_rate = float(raw_anomalies / max(1, len(predictions)))
                        if base_rate < 0.001:
                            try:
                                from sklearn.ensemble import IsolationForest
                                aux_if = IsolationForest(
                                    n_estimators=80,
                                    contamination=0.02,
                                    random_state=42,
                                    n_jobs=1
                                )
                                fit_source = X
                                score_source = X
                                score_index = None

                                # OOD rescue must stay lightweight on very large scans.
                                if len(X) > 150000:
                                    sample_size = min(150000, len(X))
                                    rng = np.random.default_rng(42)
                                    sampled_idx = np.sort(rng.choice(len(X), size=sample_size, replace=False))
                                    if hasattr(X, 'iloc'):
                                        fit_source = X.iloc[sampled_idx]
                                        score_source = fit_source
                                    else:
                                        fit_source = X[sampled_idx]
                                        score_source = fit_source
                                    score_index = sampled_idx
                                    st.info(
                                        "OOD-перевірку виконано на репрезентативній вибірці великого файлу "
                                        "для стабільної швидкодії."
                                    )

                                aux_if.fit(fit_source)
                                aux_scores = aux_if.decision_function(score_source)
                                aux_threshold = float(np.quantile(aux_scores, 0.02))
                                aux_ood_mask = np.zeros(len(predictions), dtype=bool)
                                local_mask = np.asarray(aux_scores <= aux_threshold, dtype=bool)
                                if score_index is None:
                                    aux_ood_mask = local_mask
                                else:
                                    aux_ood_mask[score_index] = local_mask
                                rescued_hits = int(np.sum(aux_ood_mask))
                                if rescued_hits > 0:
                                    # Зберігаємо тільки маску rescue для рядків, що були benign.
                                    # Тип атаки не вигадуємо: пізніше позначимо ці рядки як "Аномалія (OOD)".
                                    benign_numeric_mask = (predictions == 0)
                                    ood_from_benign_mask = (aux_ood_mask & benign_numeric_mask)
                                    rescued_numeric = int(np.sum(ood_from_benign_mask))
                                    st.warning(
                                        "Модель позначила майже весь трафік як нормальний. "
                                        "Додатково застосовано OOD-перевірку для підозрілих відхилень."
                                    )
                                    print(
                                        f"[LOG] OOD rescue applied: base_rate={base_rate:.6f}, "
                                        f"rescued_hits={rescued_hits}, rescued_numeric={rescued_numeric}, "
                                        f"aux_threshold={aux_threshold:.6f}"
                                    )
                            except Exception as aux_exc:
                                print(f"[WARN] OOD rescue skipped: {aux_exc}")

                    ood_rescued_count = int(np.sum(ood_from_benign_mask)) if ood_from_benign_mask is not None else 0
                    print(f"[LOG] Prediction finished. Found {raw_anomalies} anomalies (raw)")
                    if ood_rescued_count > 0:
                        print(f"[LOG] OOD rescue benign overrides: {ood_rescued_count}")

                    # Діагностика - чому 0 аномалій?
                    if raw_anomalies == 0 and ood_rescued_count == 0:
                        print(f"[DIAGNOSTIC] WARNING: 0 anomalies detected!")
                        print(f"[DIAGNOSTIC] Model type: {type(model)}")
                        print(f"[DIAGNOSTIC] Predictions unique values: {np.unique(predictions, return_counts=True)}")
                        if hasattr(model, 'decision_function'):
                            scores_dbg = model.decision_function(X)
                            print(f"[DIAGNOSTIC] Anomaly scores range: {scores_dbg.min():.4f} to {scores_dbg.max():.4f}")
                            print(f"[DIAGNOSTIC] Anomaly scores mean: {scores_dbg.mean():.4f}")

                    print(f"[LOG] Raw predictions (first 20): {predictions[:20]}")
                    print(f"[LOG] Unique predictions: {np.unique(predictions, return_counts=True)}")

                    # ── Декодування міток назад у оригінальні назви атак ──
                    is_anomaly_model = is_isolation_forest
                    anomaly_scores_list = st.session_state.get('anomaly_scores', None)

                    if is_anomaly_model:
                        # IF: severity на основі anomaly scores
                        if anomaly_scores_list:
                            scores_arr = np.array(anomaly_scores_list)
                            predictions_decoded = []
                            for i, p in enumerate(predictions):
                                if p == 0:
                                    predictions_decoded.append('Норма')
                                else:
                                    score = scores_arr[i] if i < len(scores_arr) else 0.0
                                    score_info = classify_if_anomaly_score(score)
                                    predictions_decoded.append(score_info['label'])
                        else:
                            predictions_decoded = ['Норма' if p == 0 else 'Аномалія' for p in predictions]
                        print(f"[LOG] IF predictions decoded (first 20): {predictions_decoded[:20]}")
                    elif hasattr(preprocessor, 'decode_labels'):
                        predictions_decoded = preprocessor.decode_labels(predictions)
                        print(f"[LOG] Decoded predictions (first 20): {predictions_decoded[:20]}")
                    else:
                        predictions_decoded = ['Норма' if p == 0 else 'Виявлено загрозу' for p in predictions]

                    # ── Локалізація через threat_catalog ──
                    def localize_label(pred):
                        s = str(pred).strip()
                        s_lower = s.lower()
                        if _is_benign_prediction(s):
                            return 'Норма'
                        # IF severity labels — вже українською, пропускаємо
                        if s_lower.startswith(('критична ', 'висока ', 'помірна ', 'слабка ')):
                            return s
                        # Використовуємо threat_catalog для всього іншого
                        info = get_threat_info(s)
                        return info.get('name_uk', s)

                    predictions_decoded = [localize_label(p) for p in predictions_decoded]

                    if ood_from_benign_mask is not None and len(ood_from_benign_mask) == len(predictions_decoded):
                        patched = 0
                        for idx, flag in enumerate(ood_from_benign_mask):
                            if not flag:
                                continue
                            predictions_decoded[idx] = "Аномалія (OOD)"
                            patched += 1
                        if patched > 0:
                            print(f"[LOG] OOD rescue relabeled benign flows: {patched}")

                    st.info("Аналіз результатів...")
                    progress.progress(85)

                    # Створюємо компактний result_df, щоб не забивати RAM на великих сканах.
                    selected_columns = _select_result_columns(
                        source_df=original_df,
                        feature_columns=getattr(preprocessor, 'feature_columns', []),
                        total_rows=len(X.index)
                    )
                    result_df = original_df.loc[X.index, selected_columns].copy()
                    result_df['prediction'] = pd.Series(predictions_decoded, index=X.index).astype(str)

                    # ── Збагачення severity та описами з threat_catalog ──
                    enriched = enrich_predictions(
                        predictions=predictions.tolist(),
                        prediction_labels=predictions_decoded,
                        anomaly_scores=anomaly_scores_list,
                        is_isolation_forest=is_anomaly_model
                    )
                    result_df['severity'] = pd.Series(
                        [e['severity'] for e in enriched], index=X.index
                    )
                    result_df['severity_label'] = pd.Series(
                        [e['severity_label'] for e in enriched], index=X.index
                    )
                    result_df['threat_description'] = pd.Series(
                        [e['description'] for e in enriched], index=X.index
                    )

                    is_anomaly = result_df['prediction'].apply(lambda x: not _is_benign_prediction(x))
                    # Safety: force OOD-rescued rows into anomaly counters even if label decoding changes.
                    if ood_from_benign_mask is not None and len(ood_from_benign_mask) == len(result_df):
                        ood_series = pd.Series(ood_from_benign_mask, index=result_df.index)
                        is_anomaly = (is_anomaly | ood_series)
                    total = int(len(result_df))
                    anomalies_count = int(is_anomaly.sum())
                    top_threats = (
                        result_df.loc[is_anomaly, 'prediction'].value_counts().head(5).to_dict()
                        if anomalies_count > 0 else {}
                    )

                    # Для великих сканів зберігаємо в сесії лише оптимізовану вибірку для UI,
                    # а не весь масив рядків (інакше браузер/процес може "зависати").
                    result_df_ui, ui_sampled = _build_ui_result_sample(result_df, is_anomaly, max_rows=180000)
                    anomalies_ui = result_df_ui[result_df_ui['prediction'].apply(lambda x: not _is_benign_prediction(x))]
                    if ui_sampled:
                        st.info(
                            f"Для стабільної роботи інтерфейсу показано вибірку {len(result_df_ui):,} із {total:,} рядків. "
                            "Підсумкові метрики пораховано на повному скані."
                        )

                    # --- PERFORMANCE FIX: High Precision Risk Score ---
                    risk_score_raw = (anomalies_count / max(total, 1)) * 100
                    if 0 < risk_score_raw < 1:
                        risk_score = round(risk_score_raw, 2)
                    else:
                        risk_score = round(risk_score_raw, 1)

                    progress.progress(100)
                    st.empty()

                    # --- Persistence Logic ---
                    # Saving results to session state
                    st.session_state['scan_done'] = True
                    st.session_state['scan_results'] = result_df_ui


--- Output 18 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
                    st.session_state['scan_anomalies'] = anomalies_ui
                    st.session_state['scan_metrics'] = {
                        'total': total,
                        'anomalies_count': anomalies_count,
                        'risk_score': risk_score,
                        'model_name': selected_model,
                        'algorithm': engine.algorithm_name if hasattr(engine, 'algorithm_name') else 'Unknown',
                        'filename': dataset_path.name,
                        'ui_sampled': ui_sampled
                    }

                    # Clear previously generated reports for new scan
                    if 'heavy_reports' in st.session_state:
                         del st.session_state['heavy_reports']

                    # Save to DB
                    services['db'].save_scan(
                        filename=dataset_path.name,
                        total=total,
                        anomalies=anomalies_count,
                        risk_score=risk_score,
                        model_name=selected_model,
                        duration=0.0,
                        details={'top_threats': top_threats}
                    )
                    del df, X, predictions, result_df, result_df_ui, anomalies_ui
                    gc.collect()

                except Exception as e:
                    st.error(f"Помилка під час аналізу: {str(e)}")
                    print(f"[ERROR] Scan failed: {e}")
                    import traceback
                    traceback.print_exc()
                finally:
                    st.session_state['scan_in_progress'] = False

                # Clean memory
                gc.collect()

            # --- RENDER RESULTS (PERSISTENT) ---
            scan_dashboard_slot = st.container()
            if st.session_state.get('scan_done', False):
                # Render dashboard in a dedicated container to avoid UI duplication artifacts on reruns.
                with scan_dashboard_slot:
                    render_comprehensive_dashboard(
                        result_df=st.session_state['scan_results'],
                        anomalies=st.session_state['scan_anomalies'],
                        metrics=st.session_state['scan_metrics'],
                        services=services,
                        gemini_key=gemini_key
                    )










--- Output 19 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
                    ]
                    if not numeric_cols:
                        return 0.0
                    zero_cols = 0
                    for col in numeric_cols:
                        series = pd.to_numeric(df_part[col], errors='coerce').fillna(0)
                        if float(series.abs().sum()) == 0.0:
                            zero_cols += 1
                    coverage = 1.0 - (zero_cols / max(1, len(numeric_cols)))
                    return float(max(0.0, min(1.0, coverage)))

                def _min_coverage_for_family(family_name: str) -> float:
                    return {
                        "CIC-IDS": 0.45,
                        "UNSW-NB15": 0.12,
                        "NSL-KDD": 0.08,
                    }.get(family_name, 0.20)

                if is_mega_model:
                    files_for_training = list(mega_model_files)
                elif len(selected_training_files) > 1:
                    files_for_training = list(selected_training_files)
                else:
                    files_for_training = [dataset_path] if dataset_path else []

                detected_families = [_resolve_family_for_file(f) for f in files_for_training]
                known_families = {fam for fam in detected_families if fam}
                unknown_family = any(not fam for fam in detected_families)
                mega_multi_family = bool(is_mega_model and len(known_families) > 1 and not is_pcap_training_source)
                multi_family = len(known_families) > 1
                family_mode_required = any(fam in {"NSL-KDD", "UNSW-NB15"} for fam in known_families)
                align_to_schema = bool(
                    is_pcap_training_source
                    or ((multi_family and not is_mega_model) and not family_mode_required)
                )
                if family_mode_required:
                    align_to_schema = False

                if unknown_family:
                    st.warning(
                        "РќРµ РІРґР°Р»РѕСЃСЏ С‚РѕС‡РЅРѕ РІРёР·РЅР°С‡РёС‚Рё СЃС–РјРµР№СЃС‚РІРѕ РґР»СЏ С‡Р°СЃС‚РёРЅРё С„Р°Р№Р»С–РІ. "
                        "РЎРёСЃС‚РµРјР° РїСЂР°С†СЋРІР°С‚РёРјРµ Сѓ СЃС–РјРµР№РЅРѕРјСѓ СЂРµР¶РёРјС– РѕР·РЅР°Рє, Р°Р»Рµ СЏРєС–СЃС‚СЊ РјРѕР¶Рµ Р·Р°Р»РµР¶Р°С‚Рё РІС–Рґ СЃС‚СЂСѓРєС‚СѓСЂРё С†РёС… РґР°С‚Р°СЃРµС‚С–РІ."
                    )

                if family_mode_required:
                    st.info(
                        "Р”Р»СЏ NSL-KDD/UNSW-NB15 СѓРІС–РјРєРЅРµРЅРѕ СЃС–РјРµР№РЅРёР№ СЂРµР¶РёРј РѕР·РЅР°Рє, "
                        "С‰РѕР± Р·Р±РµСЂРµРіС‚Рё РїРѕРІРЅРёР№ РЅР°Р±С–СЂ РєРѕР»РѕРЅРѕРє С– РЅРµ РІС‚СЂР°С‡Р°С‚Рё СЏРєС–СЃС‚СЊ."
                    )
                    if multi_family and not is_mega_model:
                        st.warning(
                            "РћР±СЂР°РЅРѕ СЂС–Р·РЅС– СЃС–РјРµР№СЃС‚РІР° (NSL/UNSW + С–РЅС€С–). "
                            "Р РµРєРѕРјРµРЅРґСѓС”С‚СЊСЃСЏ С‚СЂРµРЅСѓРІР°С‚Рё РѕРєСЂРµРјС– РјРѕРґРµР»С– Р°Р±Рѕ РІРёРєРѕСЂРёСЃС‚Р°С‚Рё Mega-Model."
                        )

                if align_to_schema:
                    if len(known_families) > 1:
                        st.warning(
                            "Р’РёСЏРІР»РµРЅРѕ СЂС–Р·РЅС– СЃС–РјРµР№СЃС‚РІР° РґР°РЅРёС…. Р”Р»СЏ СЃС‚Р°Р±С–Р»СЊРЅРѕСЃС‚С– РІРёРєРѕСЂРёСЃС‚РѕРІСѓС”С‚СЊСЃСЏ СѓРЅС–С„С–РєРѕРІР°РЅР° СЃС…РµРјР° РѕР·РЅР°Рє "
                            "(С†Рµ РјРѕР¶Рµ С‚СЂРѕС…Рё Р·РЅРёР·РёС‚Рё С‚РѕС‡РЅС–СЃС‚СЊ Сѓ РІСѓР·СЊРєРёС… РґРѕРјРµРЅР°С…)."
                        )
                else:
                    if mega_multi_family:
                        st.info(
                            "Mega-Model: СѓРІС–РјРєРЅРµРЅРѕ СЃС–РјРµР№РЅРёР№ СЂРµР¶РёРј РѕР·РЅР°Рє. "
                            "РњРѕРґРµР»СЊ Р·Р±РµСЂС–РіР°С” СЂРѕР·С€РёСЂРµРЅРёР№ РЅР°Р±С–СЂ РєРѕР»РѕРЅРѕРє С–Р· РєРѕР¶РЅРѕРіРѕ СЃС–РјРµР№СЃС‚РІР° "
                            "РґР»СЏ РєСЂР°С‰РѕС— РєСЂРѕСЃ-РґРѕРјРµРЅРЅРѕС— С‚РѕС‡РЅРѕСЃС‚С–."
                        )
                    else:
                        st.info(
                            "РЎС–РјРµР№РЅРёР№ СЂРµР¶РёРј РѕР·РЅР°Рє СѓРІС–РјРєРЅРµРЅРѕ: РјРѕРґРµР»СЊ Р·Р±РµСЂС–РіР°С” СЃРїРµС†РёС„С–С‡РЅС– РєРѕР»РѕРЅРєРё С†СЊРѕРіРѕ РґР°С‚Р°СЃРµС‚Сѓ "
                            "РґР»СЏ РєСЂР°С‰РѕС— С‚РѕС‡РЅРѕСЃС‚С–."
                        )

                if len(files_for_training) > 1:
                    dfs = []
                    merge_caption = "Mega-Model" if is_mega_model else "Р’РёР±СЂР°РЅС– РґР°С‚Р°СЃРµС‚Рё"
                    st.write(f"РћР±'С”РґРЅР°РЅРЅСЏ {len(files_for_training)} РґР°С‚Р°СЃРµС‚С–РІ ({merge_caption})...")

                    data_mode_fast = not is_expert_mode
                    rows_per_file_cap = 20000 if data_mode_fast else 50000
                    base_total_budget = 140000 if is_mega_model else 120000
                    if not data_mode_fast:
                        base_total_budget = 280000 if is_mega_model else 200000

                    files_by_family: dict[str, list[Path]] = {}
                    for f, fam in zip(files_for_training, detected_families):
                        key = fam or "Generic"
                        files_by_family.setdefault(key, []).append(f)
                    if is_mega_model and "Generic" in files_by_family:
                        st.warning(
                            "РЈ Mega-Model РІРёСЏРІР»РµРЅРѕ С„Р°Р№Р»Рё Р±РµР· С‡С–С‚РєРѕРіРѕ СЃС–РјРµР№СЃС‚РІР°. "
                            "Р‡С… РїСЂРѕРїСѓС‰РµРЅРѕ, С‰РѕР± РЅРµ Р·РЅРёР¶СѓРІР°С‚Рё СЏРєС–СЃС‚СЊ СѓР·Р°РіР°Р»СЊРЅРµРЅРЅСЏ."
                        )
                        files_by_family.pop("Generic", None)

                    family_keys = list(files_by_family.keys())
                    family_budget = max(20000, int(base_total_budget / max(1, len(family_keys))))
                    total_loaded = 0

                    for family_key, family_files in files_by_family.items():
                        remaining_family = family_budget
                        per_file_budget = max(5000, int(np.ceil(family_budget / max(1, len(family_files)))))
                        per_file_budget = min(per_file_budget, rows_per_file_cap)

                        for f in family_files:
                            if remaining_family <= 0:
                                break
                            try:
                                df_part = loader.load_file(
                                    str(f),
                                    max_rows=min(per_file_budget, remaining_family),
                                    multiclass=two_stage_mode,
                                    align_to_schema=align_to_schema
                                )
                                if multi_family:
                                    df_part = df_part.copy()
                                    df_part["family_hint"] = family_key or "Unknown"
                                coverage = 1.0 if not align_to_schema else _estimate_schema_coverage(df_part, loader.schema_features)
                                family_name = _infer_dataset_family_name(f.name)
                                min_cov = _min_coverage_for_family(family_name)
                                if align_to_schema and is_mega_model and coverage < min_cov:
                                    st.warning(
                                        f"{f.name}: РЅРёР·СЊРєР° СЃСѓРјС–СЃРЅС–СЃС‚СЊ РѕР·РЅР°Рє ({coverage:.0%}) РґР»СЏ {family_name or 'Generic'} "
                                        "вЂ” С„Р°Р№Р» РІРёРєР»СЋС‡РµРЅРѕ Р· Mega-С‚СЂРµРЅСѓРІР°РЅРЅСЏ."
                                    )
                                    add_log(f"РџСЂРѕРїСѓС‰РµРЅРѕ {f.name}: coverage={coverage:.2f}, family={family_name or 'Generic'}")
                                    continue

                                if align_to_schema and coverage < min_cov:
                                    st.warning(
                                        f"{f.name}: РЅРёР·СЊРєР° СЃСѓРјС–СЃРЅС–СЃС‚СЊ РѕР·РЅР°Рє ({coverage:.0%}). "
                                        "РЇРєС–СЃС‚СЊ РјРѕР¶Рµ Р±СѓС‚Рё РЅРёР¶С‡РѕСЋ, РїРµСЂРµРІС–СЂС‚Рµ РґР°С‚Р°СЃРµС‚."
                                    )

                                dfs.append(df_part)
                                files_for_training_used.append(f)
                                remaining_family -= len(df_part)
                                total_loaded += len(df_part)
                                st.caption(f"{f.name} ({len(df_part)} СЂСЏРґРєС–РІ)")
                            except Exception as e:
                                st.error(f"РџРѕРјРёР»РєР° Р· С„Р°Р№Р»РѕРј {f.name}: {e}")

                    if not dfs:
                        st.error("РќРµ РІРґР°Р»РѕСЃСЏ Р·Р°РІР°РЅС‚Р°Р¶РёС‚Рё РґР°РЅС–.")
                        st.stop()

                    df = pd.concat(dfs, ignore_index=True)
                    st.success(f"РћР±'С”РґРЅР°РЅРѕ. Р’СЃСЊРѕРіРѕ Р·Р°РїРёСЃС–РІ: {len(df):,}")
                else:
                    if not files_for_training:
                        st.error("РќРµ РІРёР±СЂР°РЅРѕ Р¶РѕРґРЅРѕРіРѕ СЃСѓРјС–СЃРЅРѕРіРѕ С„Р°Р№Р»Сѓ РґР»СЏ С‚СЂРµРЅСѓРІР°РЅРЅСЏ.")
                        st.stop()
                    single_cap = 150000 if not is_expert_mode else 320000
                    df = loader.load_file(
                        str(files_for_training[0]),
                        max_rows=single_cap,
                        multiclass=two_stage_mode,
                        align_to_schema=align_to_schema
                    )
                    files_for_training_used = [files_for_training[0]]

                print(f"[LOG] Data loaded. Shape: {df.shape}")

                if 'label' not in df.columns:
                    if is_isolation_algorithm:
                        # Unsupervised fallback: allow training IF on unlabeled files (PCAP/NF/CSV).
                        # IMPORTANT: quality depends on how "normal" this file is.
                        df['label'] = 'BENIGN'
                        st.warning(
                            "РЈ С„Р°Р№Р»С– РЅРµ Р·РЅР°Р№РґРµРЅРѕ РєРѕР»РѕРЅРєСѓ label. Р”Р»СЏ Isolation Forest РІСЃС– Р·Р°РїРёСЃРё РІРІР°Р¶Р°СЋС‚СЊСЃСЏ "
                            "РЅРѕСЂРјР°Р»СЊРЅРёРјРё (BENIGN) РїС–Рґ С‡Р°СЃ С‚СЂРµРЅСѓРІР°РЅРЅСЏ."
                        )
                    else:
                        label_cols = [c for c in df.columns if 'label' in c.lower() or 'class' in c.lower()]
                        if label_cols:
                            df.rename(columns={label_cols[0]: 'label'}, inplace=True)
                        else:
                            st.error("РќРµ Р·РЅР°Р№РґРµРЅРѕ РєРѕР»РѕРЅРєСѓ Р· РјС–С‚РєР°РјРё (label). РџРµСЂРµРІС–СЂС‚Рµ СЃС‚СЂСѓРєС‚СѓСЂСѓ С„Р°Р№Р»Сѓ.")
                            st.stop()

                label_candidates = FeatureRegistry.get_synonyms().get('label', [])
                cols_to_drop = [c for c in df.columns if c in label_candidates and c != 'label']
                if cols_to_drop:
                    df.drop(columns=cols_to_drop, inplace=True)
                    st.warning(f"Р’РёРґР°Р»РµРЅРѕ РєРѕР»РѕРЅРєРё РґР»СЏ СѓРЅРёРєРЅРµРЅРЅСЏ РІРёС‚РѕРєСѓ РґР°РЅРёС…: {', '.join(cols_to_drop)}")

                if not is_isolation_algorithm and 'label' in df.columns:
                    label_norm = df['label'].astype(str).str.strip().str.lower()
                    unique_attack_labels = sorted(set(label_norm[~label_norm.isin(BENIGN_LABEL_TOKENS)].tolist()))
                    if len(unique_attack_labels) <= 1:
                        st.warning(
                            "РЈ С‚СЂРµРЅСѓРІР°Р»СЊРЅРѕРјСѓ С„Р°Р№Р»С– Р»РёС€Рµ РѕРґРёРЅ С‚РёРї Р°С‚Р°РєРё (Р°Р±Рѕ РјР°Р№Р¶Рµ РЅРµРјР°С” РІР°СЂС–Р°С‚РёРІРЅРѕСЃС‚С–). "
                            "РўР°РєР° РјРѕРґРµР»СЊ РјРѕР¶Рµ РґРѕР±СЂРµ Р»РѕРІРёС‚Рё СЃР°РјРµ С†РµР№ СЃС†РµРЅР°СЂС–Р№, Р°Р»Рµ РїСЂРѕРїСѓСЃРєР°С‚Рё С–РЅС€С– Р°С‚Р°РєРё. "
                            "Р”Р»СЏ РєСЂР°С‰РѕРіРѕ СѓР·Р°РіР°Р»СЊРЅРµРЅРЅСЏ РІРёРєРѕСЂРёСЃС‚Р°Р№С‚Рµ Mega-Model Р°Р±Рѕ Р РѕР·СѓРјРЅРµ С‚СЂРµРЅСѓРІР°РЅРЅСЏ (1 РєР»С–Рє)."
                        )

                st.info(f"Р—РЅР°Р№РґРµРЅРѕ {len(df):,} Р·Р°РїРёСЃС–РІ С‚Р° {df['label'].nunique()} С‚РёРїС–РІ С‚СЂР°С„С–РєСѓ")

                progress.progress(30)
                st.info("РћР±СЂРѕР±РєР° С‚Р° РїС–РґРіРѕС‚РѕРІРєР° РґР°РЅРёС…...")

                print("[LOG] Starting preprocessing (anti-leakage: raw split first)...")
                enable_scaling = (algorithm == "Logistic Regression")

                # *** ANTI-LEAKAGE: split raw DataFrame FIRST, fit preprocessor ONLY on train ***
                # This prevents the scaler/encoder from seeing test-set statistics during training.
                if 'label' in df.columns:
                    label_raw_pre = df['label'].astype(str).str.strip().str.lower()
                    raw_split_ok = (
                        label_raw_pre.nunique() >= 2
                        and label_raw_pre.value_counts().min() >= 2
                    )
                    if raw_split_ok:
                        df_train_raw, df_test_raw = train_test_split(
                            df, test_size=test_size / 100, random_state=42,
                            stratify=label_raw_pre
                        )
                    else:
                        df_train_raw, df_test_raw = train_test_split(
                            df, test_size=test_size / 100, random_state=42
                        )
                    add_log(
                        f"[Anti-leakage] Raw split: train={len(df_train_raw):,}, test={len(df_test_raw):,} "
                        f"(preprocessor will fit ONLY on train)."
                    )


--- Output 20 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
                else:
                    # IF unsupervised вЂ” no label to split on
                    df_train_raw = df.copy()
                    df_test_raw = df.copy()

                preprocessor = Preprocessor(enable_scaling=enable_scaling)
                X_train, y_train = preprocessor.fit_transform(df_train_raw, target_col='label')
                X_test = preprocessor.transform(df_test_raw.drop(columns=['label'], errors='ignore'))

                # Encode test labels with the already-fitted target_encoder
                y_test_raw_series = df_test_raw['label'].astype(str).str.strip() if 'label' in df_test_raw.columns else pd.Series(dtype=str)
                try:
                    y_test = pd.Series(
                        preprocessor.target_encoder.transform(y_test_raw_series),
                        index=df_test_raw.index
                    )
                except Exception:
                    known_mask_te = y_test_raw_series.isin(preprocessor.target_encoder.classes_)
                    X_test = X_test[known_mask_te]
                    y_test = pd.Series(
                        preprocessor.target_encoder.transform(y_test_raw_series[known_mask_te]),
                        index=df_test_raw.index[known_mask_te]
                    )

                scaling_state = "enabled" if enable_scaling else "disabled"
                print(f"[LOG] Preprocessing finished. X_train: {X_train.shape}, X_test: {X_test.shape}, scaling {scaling_state}")
                add_log(f"РќР°РІС‡Р°Р»СЊРЅР° РІРёР±С–СЂРєР°: {len(X_train):,} СЂСЏРґРєС–РІ | РўРµСЃС‚РѕРІР°: {len(X_test):,} СЂСЏРґРєС–РІ")
                if enable_scaling:
                    add_log("Р”Р»СЏ Logistic Regression СѓРІС–РјРєРЅРµРЅРѕ Р°РІС‚РѕРјР°С‚РёС‡РЅРµ РјР°СЃС€С‚Р°Р±СѓРІР°РЅРЅСЏ (StandardScaler).")

                y = pd.concat([y_train, y_test])

                # Clean rare classes from TRAIN only
                min_samples = 5
                if is_mega_model and len(y_train):
                    min_samples = max(min_samples, int(0.001 * len(y_train)))
                class_counts = y_train.value_counts()
                rare_classes = class_counts[class_counts < min_samples].index.tolist()

                if rare_classes:
                    try:
                        rare_names = preprocessor.target_encoder.inverse_transform(rare_classes)
                        rare_str = ", ".join(map(str, rare_names))
                    except Exception:
                        rare_str = str(rare_classes)

                    mask_tr = ~y_train.isin(rare_classes)
                    X_train = X_train[mask_tr]
                    y_train = y_train[mask_tr]
                    mask_te = ~y_test.isin(rare_classes)
                    X_test = X_test[mask_te]
                    y_test = y_test[mask_te]

                    st.warning(f"Р’РёРєР»СЋС‡РµРЅРѕ СЂС–РґРєС–СЃРЅС– РєР»Р°СЃРё (< {min_samples} РїСЂРёРєР».): {rare_str}")

                # XGBoost РІРёРјР°РіР°С” СЃСѓС†С–Р»СЊРЅС– С–РЅРґРµРєСЃРё РєР»Р°СЃС–РІ 0..N-1.
                if algorithm == "XGBoost" and not two_stage_mode:
                    unique_codes = np.sort(pd.Series(y_train).unique())
                    remap = {int(old_code): int(new_code) for new_code, old_code in enumerate(unique_codes)}
                    y_train = pd.Series(y_train).map(remap).astype(int)
                    y_test = pd.Series(y_test).map(remap).fillna(-1).astype(int)
                    y_test = y_test[y_test >= 0]  # drop unknowns
                    X_test = X_test.loc[y_test.index]

                    if hasattr(preprocessor, 'target_encoder') and hasattr(preprocessor.target_encoder, 'classes_'):
                        try:
                            old_classes = np.asarray(preprocessor.target_encoder.classes_)
                            new_classes = old_classes[unique_codes.astype(int)]
                            preprocessor.target_encoder.classes_ = new_classes
                            add_log(
                                f"XGBoost: РІРёРєРѕРЅР°РЅРѕ reindex РєР»Р°СЃС–РІ РїС–СЃР»СЏ РѕС‡РёСЃС‚РєРё СЂС–РґРєС–СЃРЅРёС… РјС–С‚РѕРє "
                                f"({len(unique_codes)} Р°РєС‚РёРІРЅРёС… РєР»Р°СЃС–РІ)."
                            )
                        except Exception:
                            add_log(
                                "вљ пёЏ XGBoost: reindex РєР»Р°СЃС–РІ РІРёРєРѕРЅР°РЅРѕ, Р°Р»Рµ СЃРёРЅС…СЂРѕРЅС–Р·СѓРІР°С‚Рё target_encoder "
                                "Р°РІС‚РѕРјР°С‚РёС‡РЅРѕ РЅРµ РІРґР°Р»РѕСЃСЏ."
                            )

                progress.progress(45)

                st.info(f"РўСЂРµРЅСѓРІР°РЅРЅСЏ РЅР° {len(X_train):,} Р·Р°РїРёСЃР°С…, РїРµСЂРµРІС–СЂРєР° РЅР° {len(X_test):,}...")
                progress.progress(55)

                engine = ModelEngine(models_dir=str(ROOT_DIR / 'models'))

                if two_stage_mode:
                    add_log("Р Р•Р–РРњ: TWO-STAGE DETECTION ENABLED")
                elif turbo_mode:
                    add_log("Р Р•Р–РРњ: РўРЈР Р‘Рћ")
                else:
                    add_log("Р Р•Р–РРњ: РџРћР’РќРР™")

                st.info("РћРїС‚РёРјС–Р·Р°С†С–СЏ РїР°СЂР°РјРµС‚СЂС–РІ (С†Рµ РјРѕР¶Рµ Р·Р°Р№РЅСЏС‚Рё С‡Р°СЃ)...")
                progress.progress(60)

                print(f"[LOG] Starting training with algorithm: {algorithm}, Turbo={turbo_mode}, TwoStage={two_stage_mode}")

                model = None
                search_info = {}
                two_stage_threshold_info: dict[str, Any] = {
                    'threshold': float(DEFAULT_SENSITIVITY_THRESHOLD),
                    'f1_attack': 0.0,
                    'f2_attack': 0.0,
                    'precision_attack': 0.0,
                    'recall_attack': 0.0,
                    'evaluated_points': 0,
                    'objective': 0.0,
                }

                benign_candidates: list[int] = []
                if two_stage_mode:
                    label_map = preprocessor.get_label_map() if hasattr(preprocessor, 'get_label_map') else {}
                    benign_tokens = BENIGN_LABEL_TOKENS
                    benign_candidates = [
                        class_id
                        for class_id, class_name in label_map.items()
                        if str(class_name).strip().lower() in benign_tokens
                    ]
                    if not benign_candidates:
                        st.warning(
                            "РЈ РґР°С‚Р°СЃРµС‚С– РЅРµ Р·РЅР°Р№РґРµРЅРѕ BENIGN/Normal РєР»Р°СЃ. "
                            "Two-Stage РІРёРјРєРЅРµРЅРѕ вЂ” РІРёРєРѕСЂРёСЃС‚РѕРІСѓС”С‚СЊСЃСЏ Р·РІРёС‡Р°Р№РЅР° РєР»Р°СЃРёС„С–РєР°С†С–СЏ."
                        )
                        add_log("вљ  BENIGN РєР»Р°СЃ РЅРµ Р·РЅР°Р№РґРµРЅРѕ вЂ” Two-Stage РІРёРјРєРЅРµРЅРѕ (fallback РґРѕ supervised).")
                        two_stage_mode = False

                if two_stage_mode:
                    add_log("Р†РЅС–С†С–Р°Р»С–Р·Р°С†С–СЏ РґРІРѕРµС‚Р°РїРЅРѕС— РјРѕРґРµР»С– (Binary + Multiclass)...")

                    binary_base = engine._create_base_model(algorithm)
                    multiclass_base = engine._create_base_model(algorithm)
                    model = TwoStageModel(binary_model=binary_base, multiclass_model=multiclass_base)

                    add_log("РўСЂРµРЅСѓРІР°РЅРЅСЏ Stage 1 С‚Р° Stage 2...")

                    benign_code_for_two_stage = None
                    if benign_candidates:
                        benign_code_for_two_stage = benign_candidates[0]
                        add_log(f"Р’РёР·РЅР°С‡РµРЅРѕ BENIGN РєРѕРґ РґР»СЏ Two-Stage: {benign_code_for_two_stage}")
                        model.fit(X_train, y_train, benign_code=benign_code_for_two_stage)
                    else:
                        add_log("вљ пёЏ BENIGN РєРѕРґ РЅРµ РІРёР·РЅР°С‡РµРЅРѕ РѕРґРЅРѕР·РЅР°С‡РЅРѕ; РІРёРєРѕСЂРёСЃС‚РѕРІСѓС”С‚СЊСЃСЏ РµРІСЂРёСЃС‚РёРєР° Two-Stage")
                        model.fit(X_train, y_train)

                    two_stage_threshold_info = _calibrate_two_stage_threshold(
                        model,
                        X_test,
                        y_test,
                        benign_code=benign_code_for_two_stage
                    )
                    add_log(
                        "Two-Stage Р°РІС‚Рѕ-РїРѕСЂС–Рі: "
                        f"{float(two_stage_threshold_info['threshold']):.2f} "
                        f"(F1_attack={float(two_stage_threshold_info['f1_attack']):.3f}, "
                        f"F2_attack={float(two_stage_threshold_info.get('f2_attack', 0.0)):.3f}, "
                        f"Recall_attack={float(two_stage_threshold_info['recall_attack']):.3f})"
                    )

                    engine.model = model
                    engine.algorithm_name = f"Two-Stage ({algorithm})"
                    search_info = {
                        'best_params': 'Default (Two-Stage Mode)',
                        'best_score': 'N/A',
                        'two_stage_threshold': two_stage_threshold_info
                    }

                elif is_isolation_algorithm:
                    add_log("Р Р•Р–РРњ: ANOMALY DETECTION (Isolation Forest)")
                    add_log("РќР°РІС‡Р°РЅРЅСЏ С‚С–Р»СЊРєРё РЅР° РЅРѕСЂРјР°Р»СЊРЅРѕРјСѓ С‚СЂР°С„С–РєСѓ (BENIGN/NORMAL)...")

                    label_map = preprocessor.get_label_map() if hasattr(preprocessor, 'get_label_map') else {}
                    normal_tokens = BENIGN_LABEL_TOKENS
                    normal_ids = [
                        class_id
                        for class_id, label_name in label_map.items()
                        if str(label_name).strip().lower() in normal_tokens
                    ]
                    if not normal_ids:
                        normal_ids = [0]

                    normal_mask = y_train.isin(normal_ids)
                    X_train_normal = X_train[normal_mask]

                    if len(X_train_normal) < 10:
                        st.error("РќРµРґРѕСЃС‚Р°С‚РЅСЊРѕ РЅРѕСЂРјР°Р»СЊРЅРѕРіРѕ С‚СЂР°С„С–РєСѓ РґР»СЏ РЅР°РІС‡Р°РЅРЅСЏ (РјС–РЅС–РјСѓРј 10 Р·Р°РїРёСЃС–РІ BENIGN/NORMAL)")
                        st.stop()

                    add_log(f"Р’РёРєРѕСЂРёСЃС‚Р°РЅРѕ {len(X_train_normal)} Р·Р°РїРёСЃС–РІ РЅРѕСЂРјР°Р»СЊРЅРѕРіРѕ С‚СЂР°С„С–РєСѓ")
                    if_effective_contamination = (
                        float(if_contamination) if if_manual_contamination else float(DEFAULT_IF_CONTAMINATION)
                    )
                    if if_manual_contamination:
                        add_log(f"Р СѓС‡РЅРёР№ contamination: {if_effective_contamination:.3f}")
                    else:
                        add_log(f"Р’РёРєРѕСЂРёСЃС‚Р°РЅРѕ СЂРµРєРѕРјРµРЅРґРѕРІР°РЅРёР№ contamination: {if_effective_contamination:.3f}")

                    model = engine.train(
                        X_train_normal,
                        y_train[normal_mask],
                        algorithm='Isolation Forest',
                        params={
                            'n_estimators': int(if_n_estimators),
                            'contamination': float(if_effective_contamination),
                            'random_state': 42,
                            'n_jobs': 1
                        }
                    )
                    add_log(f"IF С‚СЂРµРЅСѓРІР°РЅРЅСЏ: n_estimators={if_n_estimators}, contamination={if_effective_contamination:.4f}")

                    calibration_info = {}
                    y_attack_holdout = (~y_test.isin(normal_ids)).astype(int).to_numpy(dtype=int)
                    X_calib = X_test
                    y_attack_calib = y_attack_holdout

                    if int(np.sum(y_attack_holdout == 1)) == 0:
                        add_log("РЈ РІР°Р»С–РґР°С†С–Р№РЅС–Р№ РІРёР±С–СЂС†С– РЅРµРјР°С” Р°С‚Р°Рє. РЁСѓРєР°С”РјРѕ Р·РѕРІРЅС–С€РЅС– РґР°РЅС– РґР»СЏ Р°РІС‚Рѕ-РєР°Р»С–Р±СЂСѓРІР°РЅРЅСЏ...")
                        X_ext_calib, y_ext_attack = _load_if_external_calibration(
                            loader=loader,
                            preprocessor=preprocessor,
                            exclude_path=dataset_path if (dataset_path and isinstance(dataset_path, Path)) else None
                        )
                        if X_ext_calib is not None and y_ext_attack is not None and int(np.sum(y_ext_attack == 1)) > 0:
                            X_calib = X_ext_calib
                            y_attack_calib = y_ext_attack
                            add_log(f"Р—Р°РІР°РЅС‚Р°Р¶РµРЅРѕ Р·РѕРІРЅС–С€РЅСЋ РєР°Р»С–Р±СЂРѕРІРєСѓ: {len(X_calib)} Р·Р°РїРёСЃС–РІ")
                        else:
                            y_attack_calib = None
                            add_log("Р—РѕРІРЅС–С€РЅСЋ РєР°Р»С–Р±СЂРѕРІРєСѓ РЅРµ Р·РЅР°Р№РґРµРЅРѕ. Р’РёРєРѕСЂРёСЃС‚РѕРІСѓС”РјРѕ unsupervised РєР°Р»С–Р±СЂСѓРІР°РЅРЅСЏ.")

                    if if_auto_calibration:
                        calibration_info = engine.auto_calibrate_isolation_threshold(
                            X_calib,
                            y_attack_binary=y_attack_calib,
                            target_fp_rate=float(if_target_fp_rate)
                        )
                        add_log(
                            "IF auto-calibration: "
                            f"mode={calibration_info.get('mode')}, "
                            f"threshold={float(calibration_info.get('threshold', 0.0)):.4f}, "
                            f"anomaly_rate={float(calibration_info.get('anomaly_rate', 0.0)):.2%}"
                        )
                    else:
                        engine.if_threshold_ = 0.0
                        engine.if_threshold_mode_ = "decision_zero_manual"
                        calibration_info = {
                            'mode': engine.if_threshold_mode_,
                            'threshold': float(engine.if_threshold_),
                            'target_fp_rate': float(if_target_fp_rate),
                            'supervised_used': False
                        }
                        add_log("IF auto-calibration РІРёРјРєРЅРµРЅРѕ. Р’РёРєРѕСЂРёСЃС‚Р°РЅРѕ РєР»Р°СЃРёС‡РЅРёР№ РїРѕСЂС–Рі 0.0.")

                    scores = model.decision_function(X_train_normal)
                    print(f"[LOG] IF scores on training: min={scores.min():.4f}, max={scores.max():.4f}, mean={scores.mean():.4f}")
                    add_log(
                        f"IF training scores: min={scores.min():.4f}, max={scores.max():.4f}, "
                        f"mean={scores.mean():.4f}"
                    )

                    search_info = {
                        'best_params': {'n_estimators': int(if_n_estimators), 'contamination': float(if_effective_contamination)},
                        'best_score': 'N/A (Unsupervised)',
                        'if_calibration': calibration_info
                    }


--- Output 21 ---
Exit code: 0
Wall time: 0.3 seconds
Output:
938:                    ood_from_benign_mask = None
940:                        predictions = model.predict(X, threshold=selected_two_stage_threshold)
949:                        predictions = engine.predict(X)
975:                        predictions = engine.predict(X)
977:                    predictions = np.asarray(predictions)
983:                            predictions = np.where(predictions == -1, 1, 0).astype(int)
985:                            predictions = predictions.astype(int, copy=False)
988:                            predictions = np.where(predictions > 0, 1, 0).astype(int)
992:                            anomaly_rate_if = float(np.mean(predictions == 1))
997:                                predictions = np.where(scores < adaptive_threshold, 1, 0).astype(int)
1004:                                    f"old_rate={anomaly_rate_if:.4f}, new_rate={np.mean(predictions == 1):.4f}, "
1010:                            raw_if_anomalies = int(np.sum(predictions == 1))
1018:                                        predictions = np.where((predictions == 1) | heuristic_mask, 1, 0).astype(int)
1026:                                            f"combined_hits={int(np.sum(predictions == 1))}"
1029:                                combined_hits = int(np.sum(predictions == 1))
1056:                                        predictions = np.where((predictions == 1) | adaptive_mask, 1, 0).astype(int)
1064:                                            f"new_hits={int(np.sum(predictions == 1))}, "
1070:                    raw_anomalies = int(np.sum(predictions == 1)) if is_isolation_forest else int(np.sum(predictions != 0))
1075:                        base_rate = float(raw_anomalies / max(1, len(predictions)))
1119:                                    benign_numeric_mask = (predictions == 0)
1120:                                    ood_from_benign_mask = (aux_ood_mask & benign_numeric_mask)
1121:                                    rescued_numeric = int(np.sum(ood_from_benign_mask))
1134:                    ood_rescued_count = int(np.sum(ood_from_benign_mask)) if ood_from_benign_mask is not None else 0
1135:                    print(f"[LOG] Prediction finished. Found {raw_anomalies} anomalies (raw)")
1140:                    if raw_anomalies == 0 and ood_rescued_count == 0:
1192:                    if ood_from_benign_mask is not None and len(ood_from_benign_mask) == len(predictions_decoded):
1194:                        for idx, flag in enumerate(ood_from_benign_mask):
1233:                    if ood_from_benign_mask is not None and len(ood_from_benign_mask) == len(result_df):
1234:                        ood_series = pd.Series(ood_from_benign_mask, index=result_df.index)


--- Output 22 ---
Exit code: 0
Wall time: 0.3 seconds
Output:
import pandas as pd
import numpy as np
import time
import gc
import traceback
from pathlib import Path
from datetime import datetime
from typing import Any
from src.core.data_loader import DataLoader
from src.core.feature_registry import FeatureRegistry
from src.core.preprocessor import Preprocessor
from src.core.model_engine import ModelEngine
from src.core.two_stage_model import TwoStageModel
from src.ui.utils.scan_diagnostics import *
from src.ui.utils.training_helpers import *
from src.ui.utils.model_helpers import *

from src.ui.core_state import (
    clear_session_memory,
    DEFAULT_SENSITIVITY_LEVEL,
    TWO_STAGE_PROFILE_RULES,
    TWO_STAGE_PROFILE_ORDER
)
from src.ui.scan_renderer import render_comprehensive_dashboard
from src.services.threat_catalog import (
    get_threat_info, get_severity, get_severity_label,
    get_severity_color, classify_if_anomaly_score, enrich_predictions
)

from src.ui.utils.scan_diagnostics import (
    _pcap_heuristic_anomaly_mask,
    compute_scan_readiness_diagnostics
)
from src.ui.utils.model_helpers import (
    _infer_dataset_family_name,
    _normalize_compatible_types,
    _resolve_two_stage_profile_threshold,
    _clamp_two_stage_threshold,
    _normalize_two_stage_profile,
    _threshold_to_sensitivity_level,
    _sensitivity_level_to_threshold,
    load_model_manifest,
    detect_scan_file_family_info
)

def _normalize_label_token(value: Any) -> str:
    token = str(value or "").strip().lower()
    token = token.replace(" ", "").replace("_", "").replace("-", "")
    return token


def _is_benign_prediction(value: Any) -> bool:
    token = _normalize_label_token(value)
    if token in {"", "0", "00", "benign", "normal", "ok", "норма", "безпечно"}:
        return True
    if "benign" in token or "norm" in token:
        return True
    if "норм" in token or "безпеч" in token:
        return True
    return False


def _resolve_scan_row_cap(file_ext: str, file_size_bytes: int) -> tuple[int | None, str | None]:
    if file_ext not in TABULAR_EXTENSIONS:
        return None, None

    if file_size_bytes >= 250 * 1024 * 1024:
        return 250000, "Файл дуже великий: інтерактивний скан обмежено до 250,000 рядків для стабільності UI."
    if file_size_bytes >= 120 * 1024 * 1024:
        return 350000, "Файл великий: інтерактивний скан обмежено до 350,000 рядків для стабільності UI."
    if file_size_bytes >= 60 * 1024 * 1024:
        return 500000, "Використано безпечний ліміт 500,000 рядків, щоб уникнути зависань браузера."
    return None, None


def _select_result_columns(source_df: pd.DataFrame, feature_columns: list[str], total_rows: int) -> list[str]:
    priority_candidates = [
        "timestamp", "time", "datetime", "date", "flow_start_time",
        "src_ip", "source_ip", "src", "srcaddr",
        "dst_ip", "destination_ip", "dst", "dstaddr",
        "src_port", "source_port", "sport",
        "dst_port", "destination_port", "dport", "dest_port", "port",
        "protocol", "proto", "service",
        "duration", "flow_duration",
        "packet_rate", "byte_rate", "flow_packets/s", "flow_bytes/s",
        "packets_fwd", "packets_bwd", "bytes_fwd", "bytes_bwd",
        "tcp_syn_count", "tcp_ack_count", "tcp_rst_count",
    ]
    selected: list[str] = []
    existing_cols = set(source_df.columns)
    for col in priority_candidates:
        if col in existing_cols and col not in selected:
            selected.append(col)

    numeric_budget = 28
    if total_rows > 200000:
        numeric_budget = 16
    if total_rows > 400000:
        numeric_budget = 10

    for col in feature_columns:
        if len(selected) >= (len(priority_candidates) + numeric_budget):
            break
        if col in existing_cols and col not in selected and pd.api.types.is_numeric_dtype(source_df[col]):
            selected.append(col)

    if not selected:
        return list(source_df.columns[:40])
    return selected

def _build_ui_result_sample(result_df: pd.DataFrame, anomaly_mask: pd.Series, max_rows: int = 180000) -> tuple[pd.DataFrame, bool]:
    """
    Повертає безпечну вибірку для UI, але зберігає пропорцію аномалій.
    Це зменшує RAM та "фрізи" браузера на великих сканах.
    """
    if len(result_df) <= max_rows:
        return result_df, False

    anomaly_df = result_df[anomaly_mask]
    normal_df = result_df[~anomaly_mask]

    anomaly_quota = min(len(anomaly_df), max(1000, int(max_rows * 0.40)))
    normal_quota = max_rows - anomaly_quota

    anomaly_sample = (
        anomaly_df.sample(n=anomaly_quota, random_state=42)
        if len(anomaly_df) > anomaly_quota else anomaly_df
    )
    normal_sample = (
        normal_df.sample(n=normal_quota, random_state=42)
        if len(normal_df) > normal_quota else normal_df
    )

    sampled = pd.concat([anomaly_sample, normal_sample], axis=0).sample(frac=1.0, random_state=42)
    return sampled, True


def render_scanning_tab(services: dict[str, Any], ROOT_DIR: Path, ALGORITHM_WIKI: dict, BENIGN_LABEL_TOKENS: list, PCAP_EXTENSIONS: set, TABULAR_EXTENSIONS: set, SUPPORTED_SCAN_EXTENSIONS: set, DEFAULT_SENSITIVITY_THRESHOLD: float, DEFAULT_IF_CONTAMINATION: float, DEFAULT_IF_TARGET_FP_RATE: float, DEFAULT_TWO_STAGE_PROFILE: str) -> None:

    model_files = sorted(


--- Output 23 ---
Exit code: 0
Wall time: 0.5 seconds
Output:
NO_STREAMLIT_PROC
Get-CimInstance : Access denied 
At line:2 char:10
+ $procs = Get-CimInstance Win32_Process | Where-Object { $_.Name -matc ...
+          ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : PermissionDenied: (root\cimv2:Win32_Process:String) [Get-CimInstance], CimException
    + FullyQualifiedErrorId : HRESULT 0x80041003,Microsoft.Management.Infrastructure.CimCmdlets.GetCimInstanceCommand
 


--- Output 24 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
KILLED=23248
KILLED=25020


--- Output 25 ---
Exit code: 124
Wall time: 10.1 seconds
Output:
command timed out after 10145 milliseconds


--- Output 26 ---
Exit code: 0
Wall time: 12.5 seconds
Output:
PID=25104
200


--- Output 27 ---
[{"type":"text","text":"## Pages\n1: chrome-error://chromewebdata/\n2: http://localhost:8501/ [selected]"}]

--- Output 28 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
2026-03-25 23:08:59.182 Did not auto detect external IP.
Please go to https://docs.streamlit.io/ for debugging hints.


--- Output 29 ---
Exit code: 0
Wall time: 0.4 seconds
Output:

  You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.1.27:8501



--- Output 30 ---
[{"type":"text","text":"## Latest page snapshot\nuid=4_0 RootWebArea \"IDS Analyzer\" url=\"http://localhost:8501/\"\n  uid=4_1 generic\n    uid=4_2 button \"Головна\" disableable disabled\n    uid=4_3 button \"Тренування\" disableable disabled\n    uid=4_4 button \"Сканування\" disableable disabled\n    uid=4_5 button \"Історія\" disableable disabled\n    uid=4_6 StaticText \"Налаштування сканування\"\n    uid=4_7 checkbox \"Автовибір найкращої моделі\" checked disableable disabled\n    uid=4_8 StaticText \"Автовибір найкращої моделі\"\n    uid=4_9 button \"Help for Автовибір найкращої моделі\"\n    uid=4_10 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=4_11 StaticText \"Система обере оптимальну модель для вашого файлу\"\n    uid=4_12 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=4_13 StaticText \"Gemini API підключено\"\n    uid=4_14 LineBreak \"\n\"\n    uid=4_15 StaticText \"Вибір даних для аналізу\"\n    uid=4_16 StaticText \"Оберіть файл з бібліотеки або завантажте новий (CSV, NF, PCAP)\"\n    uid=4_17 button \"Help for Джерело файлу для сканування:\"\n    uid=4_18 radio \"Бібліотека (User_Uploads)\" checked disableable disabled\n    uid=4_19 StaticText \"Бібліотека (User_Uploads)\"\n    uid=4_20 radio \"Завантажити новий\" disableable disabled\n    uid=4_21 StaticText \"Завантажити новий\"\n    uid=4_22 StaticText \"02-15-2018.csv\"\n    uid=4_23 combobox \"Selected 02-15-2018.csv. Оберіть файл:\" autocomplete=\"list\" disableable disabled expandable haspopup=\"listbox\"\n    uid=4_24 image \"open\" description=\"open\"\n    uid=4_25 StaticText \"Модель для запуску: ids_model_random_forest_194247_25032026.joblib (Two-Stage)\"\n    uid=4_26 button \"Help for Профіль виявлення загроз\"\n    uid=4_27 radio \"Збалансований\" checked disableable disabled\n    uid=4_28 StaticText \"Збалансований\"\n    uid=4_29 radio \"Строгий (менше FP)\" disableable disabled\n    uid=4_30 StaticText \"Строгий (менше FP)\"\n    uid=4_31 checkbox \"Ручне керування чутливістю\" disableable disabled\n    uid=4_32 StaticText \"Ручне керування чутливістю\"\n    uid=4_33 button \"Help for Ручне керування чутливістю\"\n    uid=4_34 StaticText \"Поточний режим: Збалансований. Чутливість автоматично взята з навченої моделі, орієнтованої на баланс між пропусками атак і хибними тривогами.\"\n    uid=4_35 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=4_36 StaticText \"Як обрати профіль:\"\n      uid=4_37 StaticText \"Збалансований\"\n      uid=4_38 StaticText \" — для щоденного використання (баланс FP/FN).\"\n      uid=4_39 StaticText \"Строгий (менше FP)\"\n      uid=4_40 StaticText \" — коли важливо зменшити хибні спрацювання.\"\n      uid=4_41 StaticText \"Якщо сумніваєтесь, залишайте \"\n      uid=4_42 StaticText \"Збалансований\"\n      uid=4_43 StaticText \".\"\n      uid=4_44 StaticText \"Ручна чутливість:\"\n      uid=4_45 StaticText \"Вище значення: більше виявлень, але можливе зростання FP.\"\n      uid=4_46 StaticText \"Нижче значення: обережніша детекція, але можливий ріст FN.\"\n    uid=4_47 StaticText \"Самодіагностика перед запуском\"\n    uid=4_48 StaticText \"Стан\"\n    uid=4_49 StaticText \"З обмеженнями\"\n    uid=4_50 StaticText \"Сумісність ознак\"\n    uid=4_51 StaticText \"77%\"\n    uid=4_52 StaticText \"Сумісність формату\"\n    uid=4_53 StaticText \"65/100\"\n    uid=4_54 StaticText \"Прогноз якості детекції\"\n    uid=4_55 StaticText \"92/100\"\n    uid=4_56 StaticText \"Сумісність формату = чи модель технічно може обробити файл (формат, ознаки, препроцесор). Прогноз якості детекції = оцінка ризику FP/FN та OOD на тестовій preview-вибірці. Це орієнтир, а не 100% гарантія якості в кожному окремому файлі. Режим моделі: Two-Stage. Оцінка ризику: Середній.\"\n    uid=4_57 StaticText \"• Відсутніх ознак: \"\n    uid=4_58 StaticText \"23\"\n    uid=4_59 StaticText \"• Оціночна частка аномалій на вибірці 3,000 рядків: \"\n    uid=4_60 StaticText \"0.83%\"\n    uid=4_61 StaticText \"• OOD-дрейф розподілу: \"\n    uid=4_62 StaticText \"3.87\"\n    uid=4_63 StaticText \" (покриття профілю: 100%)\"\n    uid=4_64 StaticText \"• Рекомендований профіль моделі: \"\n    uid=4_65 StaticText \"Строгий (менше FP)\"\n    uid=4_66 StaticText \". Поріг для цього профілю береться з навченої моделі.\"\n    uid=4_67 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=4_68 StaticText \"Низька сумісність ознак: 77%.\"\n    uid=4_69 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=4_70 StaticText \"Легкий дрейф розподілу ознак. Рекомендовано перевірити інциденти вручну.\"\n    uid=4_71 button \"Розпочати аналіз\" disableable disabled\n    uid=5_0 progressbar \"100% Loaded\" value=\"100\" valuemax=\"100\" valuemin=\"0\" valuetext=\"\"\n    uid=5_1 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_2 StaticText \"Завантаження даних...\"\n    uid=5_3 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_4 StaticText \"Завантаження моделі...\"\n    uid=5_5 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_6 StaticText \"Сімейство файлу визначено неоднозначно. Обрано найбезпечнішу сумісну модель: \"\n      uid=5_7 StaticText \"ids_model_random_forest_194247_25032026.joblib\"\n    uid=5_8 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_9 StaticText \"Файл дуже великий: інтерактивний скан обмежено до 250,000 рядків для стабільності UI.\"\n    uid=5_10 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_11 StaticText \"Обробка даних...\"\n    uid=5_12 heading \"Сумісність даних: 76%\" level=\"4\"\n    uid=5_13 StaticText \"Модель очікує 99 ознак, знайдено 80. Відсутні: 23\"\n    uid=5_14 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_15 StaticText \"Низька сумісність може призвести до неточних результатів!\"\n    uid=5_16 DisclosureTriangle \"keyboard_arrow_right Переглянути відсутні ознаки\" expandable\n    uid=5_17 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_18 StaticText \"Класифікація трафіку...\"\n    uid=5_19 StaticText \"Використано Two-Stage Detection (Профіль: Збалансований, Threshold: 0.30, еквівалент чутливості: 70/99)\"\n    uid=5_20 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_21 StaticText \"Аналіз результатів...\"\n    uid=5_22 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_23 StaticText \"Для стабільної роботи інтерфейсу показано вибірку 180,000 із 250,000 рядків. Підсумкові метрики пораховано на повному скані.\"\n    uid=5_24 StaticText \"Підсумок аналізу\"\n    uid=5_25 StaticText \"Всього пакетів\"\n    uid=5_26 button \"Help for Всього пакетів\"\n    uid=5_27 StaticText \"250,000\"\n    uid=5_28 StaticText \"Виявлено атак\"\n    uid=5_29 StaticText \"6,414\"\n    uid=5_30 StaticText \"2.60% ризик\"\n    uid=5_31 StaticText \"Нормальний трафік\"\n    uid=5_32 StaticText \"243,586\"\n    uid=5_33 StaticText \"97.40%\"\n    uid=5_34 StaticText \"Рівень ризику\"\n    uid=5_35 StaticText \"2.60%\"\n    uid=5_36 StaticText \"Низький\"\n    uid=5_37 alert atomic live=\"assertive\" relevant=\"additions text\"\n      uid=5_38 StaticText \"ЩО: Виявлено \"\n      uid=5_39 StaticText \"6,414\"\n      uid=5_40 StaticText \" підозрілих записів із 250,000 (2.6%). Типи загроз: \"\n      uid=5_41 StaticText \"DoS GoldenEye\"\n      uid=5_42 StaticText \", \"\n      uid=5_43 StaticText \"Infilteration\"\n      uid=5_44 StaticText \".\"\n      uid=5_45 StaticText \"НАСКІЛЬКИ СЕРЙОЗНО: \"\n      uid=5_46 StaticText \"Рівень ризику — Низький\"\n      uid=5_47 StaticText \" (менше 5% трафіку).\"\n      uid=5_48 StaticText \"ЩО РОБИТИ: Перевірте розділ «Мережеві деталі інциденту» нижче для ідентифікації джерел поодиноких аномалій. Звичайний моніторинг достатній.\"\n    uid=5_49 button \"Help for Режим відображення графіків\"\n    uid=5_50 radio \"Авто (рекомендовано)\" checked disableable disabled\n    uid=5_51 StaticText \"Авто (рекомендовано)\"\n    uid=5_52 radio \"Швидкий\" disableable disabled\n    uid=5_53 StaticText \"Швидкий\"\n    uid=5_54 radio \"Детальний\" disableable disabled\n    uid=5_55 StaticText \"Детальний\"\n    uid=5_56 StaticText \"Авто-режим сам обирає навантаження графіків: безпечніше для браузера на великих файлах.\"\n    uid=5_57 StaticText \"Для швидкодії графіки побудовані на вибірці 80,000 із 180,000 записів.\"\n    uid=5_58 StaticText \"Склад трафіку\"\n    uid=7_0 StaticText \"97.4%\"\n    uid=7_1 StaticText \"2.57%\"\n    uid=7_2 StaticText \"Нормальний трафік\"\n    uid=7_3 StaticText \"Атаки\"\n    uid=7_4 StaticText \"Склад трафіку\"\n    uid=7_5 StaticText \"75.3%\"\n    uid=7_6 StaticText \"24.7%\"\n    uid=7_7 StaticText \"DoS GoldenEye\"\n    uid=7_8 StaticText \"Infilteration\"\n    uid=7_9 StaticText \"Розподіл типів атак\"\n    uid=5_69 StaticText \"Часова лінія атак\"\n    uid=5_70 StaticText \"Розумний автоперіод: 1 хв. Період обрано автоматично для читабельної осі часу.\"\n    uid=7_10 StaticText \"25.03 22:39\"\n    uid=7_11 StaticText \"26.03 05:15\"\n    uid=7_12 StaticText \"26.03 11:51\"\n    uid=7_13 StaticText \"26.03 19:00\"\n    uid=7_14 StaticText \"27.03 01:36\"\n    uid=7_15 StaticText \"27.03 08:45\"\n    uid=7_16 StaticText \"27.03 15:21\"\n    uid=7_17 StaticText \"27.03 22:30\"\n    uid=7_18 StaticText \"0\"\n    uid=7_19 StaticText \"5\"\n    uid=7_20 StaticText \"10\"\n    uid=7_21 StaticText \"15\"\n    uid=7_22 StaticText \"20\"\n    uid=7_23 StaticText \"25\"\n    uid=7_24 StaticText \"30\"\n    uid=7_25 StaticText \"Тип атаки\"\n    uid=7_26 StaticText \"Infilteration\"\n    uid=7_27 StaticText \"DoS (GoldenEye)\"\n    uid=7_28 StaticText \"Активність атак (агрегація: 1 хв)\"\n    uid=7_29 StaticText \"Час\"\n    uid=7_30 StaticText \"Кількість подій\"\n    uid=5_92 StaticText \"Розподіл загроз\"\n    uid=7_31 StaticText \"4,830\"\n    uid=7_32 StaticText \"1,584\"\n    uid=7_33 StaticText \"0\"\n    uid=7_34 StaticText \"1000\"\n    uid=7_35 StaticText \"2000\"\n    uid=7_36 StaticText \"3000\"\n    uid=7_37 StaticText \"4000\"\n    uid=7_38 StaticText \"5000\"\n    uid=7_39 StaticText \"Infilteration\"\n    uid=7_40 StaticText \"DoS GoldenEye\"\n    uid=7_41 StaticText \"Кількість за типом атаки\"\n    uid=7_42 StaticText \"Кількість\"\n    uid=7_43 StaticText \"критичний\"\n    uid=7_44 StaticText \"високий\"\n    uid=7_45 StaticText \"середній\"\n    uid=7_46 StaticText \"низький\"\n    uid=7_47 StaticText \"DoS GoldenEye\"\n    uid=7_48 StaticText \"Infilteration\"\n    uid=7_49 StaticText \"0\"\n    uid=7_50 StaticText \"1000\"\n    uid=7_51 StaticText \"2000\"\n    uid=7_52 StaticText \"3000\"\n    uid=7_53 StaticText \"4000\"\n    uid=7_54 StaticText \"Розподіл за критичністю\"\n    uid=7_55 StaticText \"Критичність\"\n    uid=7_56 StaticText \"Тип загрози\"\n    uid=5_119 StaticText \"Деталі виявлених загроз\"\n    uid=5_120 StaticText \"Високий: 4,830\"\n    uid=5_121 StaticText \"Помірний: 1,584\"\n    uid=5_122 StaticText \"🟠 DoS GoldenEye\"\n    uid=5_123 StaticText \"ВИСОКИЙ\"\n    uid=5_124 StaticText \"4,830 (75.3%)\"\n    uid=5_125 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n    uid=5_126 StaticText \"⚡ Вплив: Вичерпання з'єднань сервера, відмова в обслуговуванні\"\n    uid=5_127 StaticText \"✅ Обмежити max-connections per IP\"\n    uid=5_128 StaticText \"✅ Налаштувати timeout для keep-alive\"\n    uid=5_129 StaticText \"✅ Моніторинг аномальної кількості з'єднань\"\n    uid=5_130 StaticText \"🟡 Infilteration\"\n    uid=5_131 StaticText \"ПОМІРНИЙ\"\n    uid=5_132 StaticText \"1,584 (24.7%)\"\n    uid=5_133 StaticText \"Виявлено підозрілу мережеву активність типу \"Infilteration\".\"\n    uid=5_134 StaticText \"⚡ Вплив: Потенційний ризик безпеці мережі\"\n    uid=5_135 StaticText \"✅ Проаналізувати деталі мережевого трафіку\"\n    uid=5_136 StaticText \"✅ Перевірити логи на підозрілу активність\"\n    uid=5_137 StaticText \"✅ За потреби — заблокувати підозрілі IP\"\n    uid=7_57 StaticText \"2.60%\"\n    uid=7_58 StaticText \"0\"\n    uid=7_59 StaticText \"20\"\n    uid=7_60 StaticText \"40\"\n    uid=7_61 StaticText \"60\"\n    uid=7_62 StaticText \"80\"\n    uid=7_63 StaticText \"100\"\n    uid=7_64 StaticText \"Рівень ризику системи\"\n    uid=7_65 StaticText \"undefined\"\n    uid=5_147 StaticText \"Мережеві деталі інциденту\"\n    uid=5_148 StaticText \"У цьому файлі відсутні деякі мережеві поля: IP джерела, IP призначення, порт джерела. Це залежить від формату джерела та попередньої обробки.\"\n    uid=5_149 StaticText \"Нижче наведені найчастіші джерела/порти/протоколи серед виявлених аномалій.\"\n    uid=5_150 StaticText \"Найчастіші порти призначення (аномалії)\"\n    uid=5_151 button \"Show/hide columns\"\n    uid=5_152 button \"Download as CSV\"\n    uid=5_153 button \"Search\"\n    uid=5_154 button \"Fullscreen\"\n    uid=5_155 Canvas\n      uid=5_156 columnheader \"Значення\" selectable\n        uid=5_157 StaticText \"Значення\"\n      uid=5_158 columnheader \"Кількість\" selectable\n        uid=5_159 StaticText \"Кількість\"\n      uid=5_160 gridcell \"80\" readonly selectable\n        uid=5_161 StaticText \"80\"\n      uid=5_162 gridcell \"6318\" readonly selectable\n        uid=5_163 StaticText \"6318\"\n      uid=5_164 gridcell \"53\" readonly selectable\n        uid=5_165 StaticText \"53\"\n      uid=5_166 gridcell \"36\" readonly selectable\n        uid=5_167 StaticText \"36\"\n      uid=5_168 gridcell \"443\" readonly selectable\n        uid=5_169 StaticText \"443\"\n      uid=5_170 gridcell \"21\" readonly selectable\n        uid=5_171 StaticText \"21\"\n      uid=5_172 gridcell \"5355\" readonly selectable\n        uid=5_173 StaticText \"5355\"\n      uid=5_174 gridcell \"5\" readonly selectable\n        uid=5_175 StaticText \"5\"\n      uid=5_176 gridcell \"0\" readonly selectable\n        uid=5_177 StaticText \"0\"\n      uid=5_178 gridcell \"3\" readonly selectable\n        uid=5_179 StaticText \"3\"\n      uid=5_180 gridcell \"51264\" readonly selectable\n        uid=5_181 StaticText \"51264\"\n      uid=5_182 gridcell \"1\" readonly selectable\n        uid=5_183 StaticText \"1\"\n      uid=5_184 gridcell \"52022\" readonly selectable\n        uid=5_185 StaticText \"52022\"\n      uid=5_186 gridcell \"1\" readonly selectable\n        uid=5_187 StaticText \"1\"\n      uid=5_188 gridcell \"51797\" readonly selectable\n        uid=5_189 StaticText \"51797\"\n      uid=5_190 gridcell \"1\" readonly selectable\n        uid=5_191 StaticText \"1\"\n    uid=5_192 StaticText \"Найчастіші протоколи (аномалії)\"\n    uid=5_193 button \"Show/hide columns\"\n    uid=5_194 button \"Download as CSV\"\n    uid=5_195 button \"Search\"\n    uid=5_196 button \"Fullscreen\"\n    uid=5_197 Canvas\n      uid=5_198 columnheader \"Значення\" selectable\n        uid=5_199 StaticText \"Значення\"\n      uid=5_200 columnheader \"Кількість\" selectable\n        uid=5_201 StaticText \"Кількість\"\n      uid=5_202 gridcell \"6\" readonly selectable\n        uid=5_203 StaticText \"6\"\n      uid=5_204 gridcell \"6370\" readonly selectable\n        uid=5_205 StaticText \"6370\"\n      uid=5_206 gridcell \"17\" readonly selectable\n        uid=5_207 StaticText \"17\"\n      uid=5_208 gridcell \"41\" readonly selectable\n        uid=5_209 StaticText \"41\"\n      uid=5_210 gridcell \"0\" readonly selectable\n        uid=5_211 StaticText \"0\"\n      uid=5_212 gridcell \"3\" readonly selectable\n        uid=5_213 StaticText \"3\"\n    uid=5_214 StaticText \"Приклади підозрілих подій (для ручної перевірки):\"\n    uid=5_215 button \"Show/hide columns\"\n    uid=5_216 button \"Download as CSV\"\n    uid=5_217 button \"Search\"\n    uid=5_218 button \"Fullscreen\"\n    uid=5_219 Canvas\n      uid=5_220 columnheader \"dst_port\" selectable\n        uid=5_221 StaticText \"dst_port\"\n      uid=5_222 columnheader \"protocol\" selectable\n        uid=5_223 StaticText \"protocol\"\n      uid=5_224 columnheader \"prediction\" selectable\n        uid=5_225 StaticText \"prediction\"\n      uid=5_226 columnheader \"severity_label\" selectable\n        uid=5_227 StaticText \"severity_label\"\n      uid=5_228 columnheader \"threat_description\" selectable\n        uid=5_229 StaticText \"threat_description\"\n      uid=5_230 columnheader \"duration\" selectable\n        uid=5_231 StaticText \"duration\"\n      uid=5_232 columnheader \"packet_rate\" selectable\n        uid=5_233 StaticText \"packet_rate\"\n      uid=5_234 columnheader \"byte_rate\" selectable\n        uid=5_235 StaticText \"byte_rate\"\n      uid=5_236 columnheader \"flow_packets/s\" selectable\n        uid=5_237 StaticText \"flow_packets/s\"\n      uid=5_238 columnheader \"flow_bytes/s\" selectable\n        uid=5_239 StaticText \"flow_bytes/s\"\n      uid=6_0 gridcell \"80\" readonly selectable\n        uid=6_1 StaticText \"80\"\n      uid=6_2 gridcell \"6\" readonly selectable\n        uid=6_3 StaticText \"6\"\n      uid=6_4 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=6_5 StaticText \"DoS GoldenEye\"\n      uid=6_6 gridcell \"Високий\" readonly selectable\n        uid=6_7 StaticText \"Високий\"\n      uid=6_8 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=6_9 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=6_10 gridcell \"66.248331\" readonly selectable\n        uid=6_11 StaticText \"66.248331\"\n      uid=6_12 gridcell \"0.26382043290343754\" readonly selectable\n        uid=6_13 StaticText \"0.26382043290343754\"\n      uid=6_14 gridcell \"4.146627593981605\" readonly selectable\n        uid=6_15 StaticText \"4.146627593981605\"\n      uid=6_16 gridcell \"0.3018943979\" readonly selectable\n        uid=6_17 StaticText \"0.3018943979\"\n      uid=6_18 gridcell \"62.2204354099\" readonly selectable\n        uid=6_19 StaticText \"62.2204354099\"\n      uid=6_20 gridcell \"80\" readonly selectable\n        uid=6_21 StaticText \"80\"\n      uid=6_22 gridcell \"6\" readonly selectable\n        uid=6_23 StaticText \"6\"\n      uid=6_24 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=6_25 StaticText \"DoS GoldenEye\"\n      uid=6_26 gridcell \"Високий\" readonly selectable\n        uid=6_27 StaticText \"Високий\"\n      uid=6_28 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=6_29 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=6_30 gridcell \"108.381942\" readonly selectable\n        uid=6_31 StaticText \"108.381942\"\n      uid=6_32 gridcell \"0.15364711917011342\" readonly selectable\n        uid=6_33 StaticText \"0.15364711917011342\"\n      uid=6_34 gridcell \"3.192259619069766\" readonly selectable\n        uid=6_35 StaticText \"3.192259619069766\"\n      uid=6_36 gridcell \"0.1660793271\" readonly selectable\n        uid=6_37 StaticText \"0.1660793271\"\n      uid=6_38 gridcell \"23.3433720905\" readonly selectable\n        uid=6_39 StaticText \"23.3433720905\"\n      uid=5_280 gridcell \"80\" readonly selectable\n        uid=5_281 StaticText \"80\"\n      uid=5_282 gridcell \"6\" readonly selectable\n        uid=5_283 StaticText \"6\"\n      uid=5_284 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_285 StaticText \"DoS GoldenEye\"\n      uid=5_286 gridcell \"Високий\" readonly selectable\n        uid=5_287 StaticText \"Високий\"\n      uid=5_288 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_289 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_290 gridcell \"107.710069\" readonly selectable\n        uid=5_291 StaticText \"107.710069\"\n      uid=5_292 gridcell \"0.15453514483742087\" readonly selectable\n        uid=5_293 StaticText \"0.15453514483742087\"\n      uid=5_294 gridcell \"3.1982233506553683\" readonly selectable\n        uid=5_295 StaticText \"3.1982233506553683\"\n      uid=5_296 gridcell \"0.1671152954\" readonly selectable\n        uid=5_297 StaticText \"0.1671152954\"\n      uid=5_298 gridcell \"23.4889831887\" readonly selectable\n        uid=5_299 StaticText \"23.4889831887\"\n      uid=5_300 gridcell \"80\" readonly selectable\n        uid=5_301 StaticText \"80\"\n      uid=5_302 gridcell \"6\" readonly selectable\n        uid=5_303 StaticText \"6\"\n      uid=5_304 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_305 StaticText \"DoS GoldenEye\"\n      uid=5_306 gridcell \"Високий\" readonly selectable\n        uid=5_307 StaticText \"Високий\"\n      uid=5_308 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_309 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_310 gridcell \"106.39849\" readonly selectable\n        uid=5_311 StaticText \"106.39849\"\n      uid=5_312 gridcell \"0.14822748969005287\" readonly selectable\n        uid=5_313 StaticText \"0.14822748969005287\"\n      uid=5_314 gridcell \"3.209977671350163\" readonly selectable\n        uid=5_315 StaticText \"3.209977671350163\"\n      uid=5_316 gridcell \"0.1597767036\" readonly selectable\n        uid=5_317 StaticText \"0.1597767036\"\n      uid=5_318 gridcell \"23.7785329472\" readonly selectable\n        uid=5_319 StaticText \"23.7785329472\"\n      uid=5_320 gridcell \"80\" readonly selectable\n        uid=5_321 StaticText \"80\"\n      uid=5_322 gridcell \"6\" readonly selectable\n        uid=5_323 StaticText \"6\"\n      uid=5_324 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_325 StaticText \"DoS GoldenEye\"\n      uid=5_326 gridcell \"Високий\" readonly selectable\n        uid=5_327 StaticText \"Високий\"\n      uid=5_328 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_329 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_330 gridcell \"107.261842\" readonly selectable\n        uid=5_331 StaticText \"107.261842\"\n      uid=5_332 gridcell \"0.15513331577062192\" readonly selectable\n        uid=5_333 StaticText \"0.15513331577062192\"\n      uid=5_334 gridcell \"3.2022235102040133\" readonly selectable\n        uid=5_335 StaticText \"3.2022235102040133\"\n      uid=5_336 gridcell \"0.1678136387\" readonly selectable\n        uid=5_337 StaticText \"0.1678136387\"\n      uid=5_338 gridcell \"23.5871392177\" readonly selectable\n        uid=5_339 StaticText \"23.5871392177\"\n      uid=5_340 gridcell \"80\" readonly selectable\n        uid=5_341 StaticText \"80\"\n      uid=5_342 gridcell \"6\" readonly selectable\n        uid=5_343 StaticText \"6\"\n      uid=5_344 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_345 StaticText \"DoS GoldenEye\"\n      uid=5_346 gridcell \"Високий\" readonly selectable\n        uid=5_347 StaticText \"Високий\"\n      uid=5_348 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_349 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_350 gridcell \"108.029972\" readonly selectable\n        uid=5_351 StaticText \"108.029972\"\n      uid=5_352 gridcell \"0.15411104453845376\" readonly selectable\n        uid=5_353 StaticText \"0.15411104453845376\"\n      uid=5_354 gridcell \"3.195378987708724\" readonly selectable\n        uid=5_355 StaticText \"3.195378987708724\"\n      uid=5_356 gridcell \"0.1666204264\" readonly selectable\n        uid=5_357 StaticText \"0.1666204264\"\n      uid=5_358 gridcell \"23.4194266014\" readonly selectable\n        uid=5_359 StaticText \"23.4194266014\"\n      uid=5_360 gridcell \"80\" readonly selectable\n        uid=5_361 StaticText \"80\"\n      uid=5_362 gridcell \"6\" readonly selectable\n        uid=5_363 StaticText \"6\"\n      uid=5_364 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_365 StaticText \"DoS GoldenEye\"\n      uid=5_366 gridcell \"Високий\" readonly selectable\n        uid=5_367 StaticText \"Високий\"\n      uid=5_368 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_369 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_370 gridcell \"108.398739\" readonly selectable\n        uid=5_371 StaticText \"108.398739\"\n      uid=5_372 gridcell \"0.15362504930061557\" readonly selectable\n        uid=5_373 StaticText \"0.15362504930061557\"\n      uid=5_374 gridcell \"3.1921110177674885\" readonly selectable\n        uid=5_375 StaticText \"3.1921110177674885\"\n      uid=5_376 gridcell \"0.1660535922\" readonly selectable\n        uid=5_377 StaticText \"0.1660535922\"\n      uid=5_378 gridcell \"23.3397549025\" readonly selectable\n        uid=5_379 StaticText \"23.3397549025\"\n      uid=5_380 gridcell \"80\" readonly selectable\n        uid=5_381 StaticText \"80\"\n      uid=5_382 gridcell \"6\" readonly selectable\n        uid=5_383 StaticText \"6\"\n      uid=5_384 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_385 StaticText \"DoS GoldenEye\"\n      uid=5_386 gridcell \"Високий\" readonly selectable\n        uid=5_387 StaticText \"Високий\"\n      uid=5_388 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_389 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_390 gridcell \"107.965797\" readonly selectable\n        uid=5_391 StaticText \"107.965797\"\n      uid=5_392 gridcell \"0.15419593519915975\" readonly selectable\n        uid=5_393 StaticText \"0.15419593519915975\"\n      uid=5_394 gridcell \"3.195948885167538\" readonly selectable\n        uid=5_395 StaticText \"3.195948885167538\"\n      uid=5_396 gridcell \"0.1667194658\" readonly selectable\n        uid=5_397 StaticText \"0.1667194658\"\n      uid=5_398 gridcell \"23.4333471368\" readonly selectable\n        uid=5_399 StaticText \"23.4333471368\"\n      uid=5_400 gridcell \"80\" readonly selectable\n        uid=5_401 StaticText \"80\"\n      uid=5_402 gridcell \"6\" readonly selectable\n        uid=5_403 StaticText \"6\"\n      uid=5_404 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_405 StaticText \"DoS GoldenEye\"\n      uid=5_406 gridcell \"Високий\" readonly selectable\n        uid=5_407 StaticText \"Високий\"\n      uid=5_408 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_409 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_410 gridcell \"107.662475\" readonly selectable\n        uid=5_411 StaticText \"107.662475\"\n      uid=5_412 gridcell \"0.146608783278368\" readonly selectable\n        uid=5_413 StaticText \"0.146608783278368\"\n      uid=5_414 gridcell \"3.198647275874601\" readonly selectable\n        uid=5_415 StaticText \"3.198647275874601\"\n      uid=5_416 gridcell \"0.1579008842\" readonly selectable\n        uid=5_417 StaticText \"0.1579008842\"\n      uid=5_418 gridcell \"23.4993668871\" readonly selectable\n        uid=5_419 StaticText \"23.4993668871\"\n      uid=5_420 gridcell \"80\" readonly selectable\n        uid=5_421 StaticText \"80\"\n      uid=5_422 gridcell \"6\" readonly selectable\n        uid=5_423 StaticText \"6\"\n      uid=5_424 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_425 StaticText \"DoS GoldenEye\"\n      uid=5_426 gridcell \"Високий\" readonly selectable\n        uid=5_427 StaticText \"Високий\"\n      uid=5_428 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_429 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_430 gridcell \"107.038844\" readonly selectable\n        uid=5_431 StaticText \"107.038844\"\n      uid=5_432 gridcell \"0.14740297799393504\" readonly selectable\n        uid=5_433 StaticText \"0.14740297799393504\"\n      uid=5_434 gridcell \"3.204220119999668\" readonly selectable\n        uid=5_435 StaticText \"3.204220119999668\"\n      uid=5_436 gridcell \"0.1588208483\" readonly selectable\n        uid=5_437 StaticText \"0.1588208483\"\n      uid=5_438 gridcell \"23.636279181\" readonly selectable\n        uid=5_439 StaticText \"23.636279181\"\n      uid=5_440 gridcell \"80\" readonly selectable\n        uid=5_441 StaticText \"80\"\n      uid=5_442 gridcell \"6\" readonly selectable\n        uid=5_443 StaticText \"6\"\n      uid=5_444 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_445 StaticText \"DoS GoldenEye\"\n      uid=5_446 gridcell \"Високий\" readonly selectable\n        uid=5_447 StaticText \"Високий\"\n      uid=5_448 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_449 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_450 gridcell \"108.65896\" readonly selectable\n        uid=5_451 StaticText \"108.65896\"\n      uid=5_452 gridcell \"0.15328395015786297\" readonly selectable\n        uid=5_453 StaticText \"0.15328395015786297\"\n      uid=5_454 gridcell \"3.2813714810754764\" readonly selectable\n        uid=5_455 StaticText \"3.2813714810754764\"\n      uid=5_456 gridcell \"0.1656559201\" readonly selectable\n        uid=5_457 StaticText \"0.1656559201\"\n      uid=5_458 gridcell \"25.6122458746\" readonly selectable\n        uid=5_459 StaticText \"25.6122458746\"\n      uid=5_460 gridcell \"80\" readonly selectable\n        uid=5_461 StaticText \"80\"\n      uid=5_462 gridcell \"6\" readonly selectable\n        uid=5_463 StaticText \"6\"\n      uid=5_464 gridcell \"DoS GoldenEye\" readonly selectable\n        uid=5_465 StaticText \"DoS GoldenEye\"\n      uid=5_466 gridcell \"Високий\" readonly selectable\n        uid=5_467 StaticText \"Високий\"\n      uid=5_468 gridcell \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\" readonly selectable\n        uid=5_469 StaticText \"HTTP-flood атака з використанням keep-alive з'єднань для вичерпання пулу потоків сервера.\"\n      uid=5_470 gridcell \"107.037626\" readonly selectable\n        uid=5_471 StaticText \"107.037626\"\n      uid=5_472 gridcell \"0.15543428249932625\" readonly selectable\n        uid=5_473 StaticText \"0.15543428249932625\"\n      uid=5_474 gridcell \"3.204231037229254\" readonly selectable\n        uid=5_475 StaticText \"3.204231037229254\"\n      uid=5_476 gridcell \"0.1681651646\" readonly selectable\n        uid=5_477 StaticText \"0.1681651646\"\n      uid=5_478 gridcell \"23.6365481424\" readonly selectable\n        uid=5_479 StaticText \"23.6365481424\"\n    uid=5_480 StaticText \"Детальні графіки вимкнені для швидшої роботи сторінки.\"\n    uid=5_481 StaticText \"Експорт звіту\"\n    uid=5_482 button \"⬇ Завантажити CSV\" disableable disabled\n    uid=7_66 button \"⬇ Завантажити Excel\" disableable disabled\n    uid=7_67 button \"⬇ Завантажити PDF\" disableable disabled\n  uid=7_68 generic\n  uid=7_69 dialog \"dialog\" modal\n    uid=7_70 StaticText \"Connection error\"\n    uid=7_71 StaticText \"Is Streamlit still running? If you accidentally stopped Streamlit, just restart it in your terminal:\"\n    uid=7_72 StaticText \"streamlit run yourscript.py\"\n    uid=7_73 button \"Copy to clipboard\" description=\"Copy to clipboard\" focusable focused\n    uid=7_74 button \"Close\"\n  uid=7_75 generic\n"}]

--- Output 31 ---
Exit code: 0
Wall time: 0.3 seconds
Output:
2026-03-25 23:08:59.182 Did not auto detect external IP.
Please go to https://docs.streamlit.io/ for debugging hints.


--- Output 32 ---
Exit code: 0
Wall time: 0.3 seconds
Output:

  You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.1.27:8501



--- Output 33 ---
[{"type":"text","text":"Successfully reloaded the page.\n## Pages\n1: chrome-error://chromewebdata/\n2: chrome-error://chromewebdata/ [selected]"}]

--- Output 34 ---
Exit code: 1
Wall time: 0.4 seconds
Output:


--- Output 35 ---
Exit code: 1
Wall time: 0.4 seconds
Output:


--- Output 36 ---
Exit code: 0
Wall time: 4.5 seconds
Output:
HTTP_FAIL: Unable to connect to the remote server


--- Output 37 ---
Exit code: 1
Wall time: 0.3 seconds
Output:
ERROR: Access denied


--- Output 38 ---
Exit code: 124
Wall time: 22.1 seconds
Output:
command timed out after 22118 milliseconds

  You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.1.27:8501

2026-03-25 23:13:12.009 Did not auto detect external IP.
Please go to https://docs.streamlit.io/ for debugging hints.


--- Output 39 ---
Exit code: 0
Wall time: 0.4 seconds
Output:

FullName                                       Length
--------                                       ------
E:\IDS_ML_Analyzer\scripts\simulate_mega.py      6070
E:\IDS_ML_Analyzer\scripts\simulate_pcap.py      4982
E:\IDS_ML_Analyzer\scripts\simulate_testing.py   7031




--- Output 40 ---
Exit code: 0
Wall time: 0.4 seconds
Output:

Name                                         Length
----                                         ------
CIC-IDS2017_DDoS_50pct_anomaly.csv           363498
CIC-IDS2017_PortScan_30pct_anomaly.csv       322070
CIC-IDS2017_WebAttack_10pct_anomaly.csv      336100
CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv 372211
CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv 401371
NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv     69915
NSL-KDD_Neptune_DoS_20pct_anomaly.csv        138378
NSL-KDD_Satan_Probe_15pct_anomaly.csv        137895
NSL-KDD_Smurf_DoS_10pct_anomaly.csv           69490
Public_ARP_Storm_Anomaly.pcap                 47296
Public_Benign_HTTP.pcap                         247
Public_DNS_Anomaly.pcap                        4338
Public_Teardrop_DoS_Anomaly.pcap               1828
UNSW_NB15_DoS_20pct_anomaly.csv              200810
UNSW_NB15_Exploits_30pct_anomaly.csv         208766
UNSW_NB15_Fuzzers_10pct_anomaly.csv          207224
UNSW_NB15_Generic_50pct_anomaly.csv          183721
Тест_Сканування_PortScan(Probe).pcap          54904
Тест_Сканування_SynFlood(DoS).pcap            56024
Тест_Сканування_Нормальний_трафік.pcap         6044




--- Output 41 ---
Exit code: 1
Wall time: 0.4 seconds
Output:
scripts\simulate_testing.py:21:    test_dir = root_dir / 'datasets' / 'TEST_DATA'
scripts\simulate_testing.py:27:        'NSL-KDD': training_dir / 'NSL_KDD_train.csv',
scripts\simulate_testing.py:28:        'UNSW_NB15': training_dir / 'UNSW_NB15_train.csv',
scripts\simulate_testing.py:29:        'CIC-IDS2017': training_dir / 'CIC-IDS2017_[DDoS].csv', 
scripts\simulate_testing.py:30:        'CIC-IDS2018': training_dir / 'CIC-IDS2018_[BruteForce-FTP_SSH].csv'
scripts\simulate_testing.py:47:            df_train = loader.load_file(str(train_file), max_rows=500000, align_to_schema=(family.startswith('CIC')), multiclass=True)
scripts\simulate_testing.py:87:                df_test = loader.load_file(str(tf), align_to_schema=(family.startswith('CIC')), multiclass=False)
scripts\simulate_pcap.py:15:    test_dir = root_dir / 'datasets' / 'TEST_DATA'
scripts\simulate_mega.py:14:from src.core.two_stage_model import TwoStageModel
scripts\simulate_mega.py:21:    test_dir = root_dir / 'datasets' / 'TEST_DATA'
scripts\simulate_mega.py:68:    mega_model = TwoStageModel(binary_model=binary_base, multiclass_model=multi_base)
scripts\simulate_mega.py:91:            # Predict using two-stage threshold=0.5
scripts\simulate_mega.py:92:            preds = mega_model.predict(X_test, threshold=0.5)
src\ui\app.py:26:from src.core.two_stage_model import TwoStageModel
src\ui\utils\training_helpers.py:9:from src.core.two_stage_model import TwoStageModel
src\ui\utils\training_helpers.py:20:    _clamp_two_stage_threshold,
src\ui\utils\training_helpers.py:38:    Load additional labeled attack samples for IF threshold calibration.
src\ui\utils\training_helpers.py:105:def _calibrate_two_stage_threshold(
src\ui\utils\training_helpers.py:106:    model: TwoStageModel,
src\ui\utils\training_helpers.py:112:    Auto-select the best Two-Stage binary threshold on validation data.
src\ui\utils\training_helpers.py:117:    fallback_threshold = _clamp_two_stage_threshold(
src\ui\utils\training_helpers.py:118:        float(getattr(model, 'binary_threshold', DEFAULT_SENSITIVITY_THRESHOLD))
src\ui\utils\training_helpers.py:121:        'threshold': fallback_threshold,
src\ui\utils\training_helpers.py:126:        'evaluated_points': 0,
src\ui\utils\training_helpers.py:131:        model.binary_threshold = fallback_threshold
src\ui\utils\training_helpers.py:143:        # Not enough class variance for stable threshold calibration.
src\ui\utils\training_helpers.py:144:        model.binary_threshold = fallback_threshold
src\ui\utils\training_helpers.py:181:        'threshold': fallback_threshold,
src\ui\utils\training_helpers.py:194:    result['evaluated_points'] = int(len(grid))
src\ui\utils\training_helpers.py:195:    threshold_stats: list[dict[str, float]] = []
src\ui\utils\training_helpers.py:198:        threshold = _clamp_two_stage_threshold(float(thr))
src\ui\utils\training_helpers.py:199:        y_pred = model.predict(X_val, threshold=threshold)
src\ui\utils\training_helpers.py:224:        threshold_stats.append(
src\ui\utils\training_helpers.py:226:                'threshold': threshold,
src\ui\utils\training_helpers.py:246:            and abs(threshold - DEFAULT_SENSITIVITY_THRESHOLD) < abs(best['threshold'] - DEFAULT_SENSITIVITY_THRESHOLD)
src\ui\utils\training_helpers.py:252:                'threshold': threshold,
src\ui\utils\training_helpers.py:263:    if threshold_stats:
src\ui\utils\training_helpers.py:267:            item for item in threshold_stats
src\ui\utils\training_helpers.py:275:                    abs(item['threshold'] - DEFAULT_SENSITIVITY_THRESHOLD),
src\ui\utils\training_helpers.py:291:            fp_threshold = float(np.quantile(attack_probs[benign_mask], 1.0 - target_fp_rate))
src\ui\utils\training_helpers.py:292:            fp_threshold = _clamp_two_stage_threshold(fp_threshold)
src\ui\utils\training_helpers.py:293:            if fp_threshold > best['threshold']:
src\ui\utils\training_helpers.py:294:                best['threshold'] = fp_threshold
src\ui\utils\training_helpers.py:296:        rate_threshold = float(np.quantile(attack_probs, 1.0 - max_attack_rate))
src\ui\utils\training_helpers.py:297:        rate_threshold = _clamp_two_stage_threshold(rate_threshold)
src\ui\utils\training_helpers.py:298:        if rate_threshold > best['threshold']:
src\ui\utils\training_helpers.py:299:            best['threshold'] = rate_threshold
src\ui\utils\training_helpers.py:303:    model.binary_threshold = float(best['threshold'])
src\ui\utils\training_helpers.py:304:    # Refresh metrics for the final threshold.
src\ui\utils\training_helpers.py:305:    y_pred_final = model.predict(X_val, threshold=model.binary_threshold)
src\ui\utils\training_helpers.py:308:        'threshold': float(model.binary_threshold),
src\ui\utils\training_helpers.py:313:        'evaluated_points': int(result.get('evaluated_points', 0)),
src\ui\utils\training_helpers.py:322:def _evaluate_training_quality_gate(
src\ui\utils\training_helpers.py:361:        thresholds = {
src\ui\utils\training_helpers.py:369:            thresholds = {
src\ui\utils\training_helpers.py:376:            thresholds = {
src\ui\utils\training_helpers.py:383:            thresholds = {
src\ui\utils\training_helpers.py:391:            thresholds['min_attack_recall'] = min(0.95, thresholds['min_attack_recall'] + 0.05)
src\ui\utils\training_helpers.py:393:            thresholds['min_attack_precision'] = max(0.20, thresholds['min_attack_precision'] - 0.10)
src\ui\utils\training_helpers.py:394:            thresholds['min_attack_recall'] = max(0.35, thresholds['min_attack_recall'] - 0.10)
src\ui\utils\training_helpers.py:395:            thresholds['min_attack_f1'] = max(0.40, thresholds['min_attack_f1'] - 0.10)
src\ui\utils\training_helpers.py:412:    if observed['accuracy'] < thresholds['min_accuracy']:
src\ui\utils\training_helpers.py:414:            f"Точність (accuracy) {observed['accuracy']:.3f} < {thresholds['min_accuracy']:.3f}"
src\ui\utils\training_helpers.py:416:    if observed['attack_precision'] < thresholds['min_attack_precision']:
src\ui\utils\training_helpers.py:418:            f"Точність по атаках {observed['attack_precision']:.3f} < {thresholds['min_attack_precision']:.3f}"
src\ui\utils\training_helpers.py:420:    if observed['attack_recall'] < thresholds['min_attack_recall']:
src\ui\utils\training_helpers.py:422:            f"Повнота по атаках {observed['attack_recall']:.3f} < {thresholds['min_attack_recall']:.3f}"
src\ui\utils\training_helpers.py:424:    if observed['attack_f1'] < thresholds['min_attack_f1']:
src\ui\utils\training_helpers.py:426:            f"F1 по атаках {observed['attack_f1']:.3f} < {thresholds['min_attack_f1']:.3f}"
src\ui\utils\training_helpers.py:461:        gap = thresholds[key] - observed[observed_key]
src\ui\utils\training_helpers.py:492:        'thresholds': thresholds,
src\ui\utils\model_helpers.py:39:def _clamp_two_stage_threshold(threshold: float) -> float:
src\ui\utils\model_helpers.py:40:    return float(np.clip(float(threshold), TWO_STAGE_THRESHOLD_MIN, TWO_STAGE_THRESHOLD_MAX))
src\ui\utils\model_helpers.py:42:def _threshold_to_sensitivity_level(threshold: float) -> int:
src\ui\utils\model_helpers.py:43:    threshold = _clamp_two_stage_threshold(threshold)
src\ui\utils\model_helpers.py:44:    level = int(round((1.0 - threshold) * 100.0))
src\ui\utils\model_helpers.py:47:def _sensitivity_level_to_threshold(level: int) -> float:
src\ui\utils\model_helpers.py:49:    threshold = 1.0 - (level / 100.0)
src\ui\utils\model_helpers.py:50:    return _clamp_two_stage_threshold(threshold)
src\ui\utils\model_helpers.py:58:def _resolve_two_stage_profile_threshold(default_threshold: float, profile: str) -> float:
src\ui\utils\model_helpers.py:59:    base_threshold = _clamp_two_stage_threshold(default_threshold)
src\ui\utils\model_helpers.py:63:        return base_threshold
src\ui\utils\model_helpers.py:65:    strict_threshold = min(0.90, max(base_threshold + 0.20, 0.65))
src\ui\utils\model_helpers.py:66:    return _clamp_two_stage_threshold(strict_threshold)
src\ui\utils\model_helpers.py:71:        return "UNSW-NB15"
src\ui\utils\model_helpers.py:73:        return "NSL-KDD"
src\ui\utils\model_helpers.py:75:        return "CIC-IDS"
src\ui\utils\model_helpers.py:104:        'two_stage_threshold_default': float(DEFAULT_SENSITIVITY_THRESHOLD),
src\ui\utils\model_helpers.py:107:        'two_stage_threshold_strict': float(np.clip(DEFAULT_SENSITIVITY_THRESHOLD, TWO_STAGE_THRESHOLD_MIN, TWO_STAGE_THRESHOLD_MAX)),
src\ui\utils\model_helpers.py:135:    threshold_default = float(
src\ui\utils\model_helpers.py:137:            'two_stage_threshold_default',
src\ui\utils\model_helpers.py:138:            getattr(loaded.get('model'), 'binary_threshold', DEFAULT_SENSITIVITY_THRESHOLD)
src\ui\utils\model_helpers.py:141:    threshold_default = _clamp_two_stage_threshold(threshold_default)
src\ui\utils\model_helpers.py:142:    sensitivity_default = _threshold_to_sensitivity_level(threshold_default)
src\ui\utils\model_helpers.py:146:    strict_threshold = _clamp_two_stage_threshold(
src\ui\utils\model_helpers.py:149:                'two_stage_threshold_strict',
src\ui\utils\model_helpers.py:150:                _resolve_two_stage_profile_threshold(threshold_default, "strict")
src\ui\utils\model_helpers.py:172:        'two_stage_threshold_default': threshold_default,
src\ui\utils\model_helpers.py:175:        'two_stage_threshold_strict': strict_threshold,
src\ui\utils\model_helpers.py:249:        'two_stage_threshold_default': float(DEFAULT_SENSITIVITY_THRESHOLD),
src\ui\utils\model_helpers.py:252:        'two_stage_threshold_strict': _resolve_two_stage_profile_threshold(
src\ui\core_state.py:210:    "CIC-IDS2017": {
src\ui\core_state.py:211:        "name": "CIC-IDS2017",
src\ui\core_state.py:214:        "originals_dir": "CIC-IDS2017_Originals"
src\ui\core_state.py:216:    "CIC-IDS2018": {
src\ui\core_state.py:217:        "name": "CIC-IDS2018",
src\ui\core_state.py:220:        "originals_dir": "CIC-IDS2018_Originals"
src\ui\core_state.py:222:    "NSL-KDD": {
src\ui\core_state.py:223:        "name": "NSL-KDD",
src\ui\core_state.py:226:        "originals_dir": "NSL-KDD_Originals"
src\services\threat_catalog.py:5:з CIC-IDS2017, CIC-IDS2018, NSL-KDD, UNSW-NB15 датасетів.
src\services\threat_catalog.py:35:    # ── CIC-IDS 2017/2018: DDoS ──
src\services\threat_catalog.py:206:        'description': 'Розвідувальне зондування мережі для виявлення активних хостів та сервісів (NSL-KDD).',
src\services\threat_catalog.py:217:        'description': 'Збір інформації про цільову мережу або систему перед основною атакою (UNSW-NB15).',
src\services\threat_catalog.py:268:    # ── NSL-KDD specific ──
src\services\threat_catalog.py:272:        'description': 'Атака з віддаленого хоста для отримання локального доступу до машини (NSL-KDD).',
src\services\threat_catalog.py:283:        'description': 'Ескалація привілеїв — звичайний користувач отримує root-доступ (NSL-KDD).',
src\services\threat_catalog.py:293:    # ── UNSW-NB15 specific ──
src\services\threat_catalog.py:319:        'description': 'Загальний клас атак, що не підпадають під конкретну категорію (UNSW-NB15).',
src\services\threat_catalog.py:353:        'description': 'Аналітичні атаки для збору інформації про конфігурацію мережі та сервісів (UNSW-NB15).',
src\services\threat_catalog.py:401:    # ── CIC-IDS 2018 specific ──
src\services\visualizer.py:220:        self.threshold_line_color = "#ffffff" if dark_mode else "#111111"
src\services\visualizer.py:473:                    threshold=dict(
src\services\visualizer.py:474:                        line=dict(color=self.threshold_line_color, width=2),
src\core\dataset_detector.py:18:        "CIC-IDS": [
src\core\dataset_detector.py:27:        "NSL-KDD": [
src\core\dataset_detector.py:37:        "UNSW-NB15": [
src\core\dataset_detector.py:51:        "CIC-IDS": {"flowduration", "totfwdpkts", "totlenfwdpkts", "flowiatmean"},
src\core\dataset_detector.py:52:        "NSL-KDD": {"protocol_type", "src_bytes", "dst_bytes", "serror_rate", "dst_host_srv_count"},
src\core\dataset_detector.py:53:        "UNSW-NB15": {"ct_srv_src", "ct_state_ttl", "ct_dst_ltm", "attack_cat", "sttl", "dttl"},
src\core\dataset_detector.py:83:            if dataset == "NSL-KDD" and ("class" in raw_cols or "label" in raw_cols):
src\core\dataset_detector.py:85:            elif dataset == "UNSW-NB15" and "attack_cat" in raw_cols:
src\ui\utils\scan_diagnostics.py:9:from src.core.two_stage_model import TwoStageModel
src\ui\utils\scan_diagnostics.py:17:    _clamp_two_stage_threshold,
src\ui\utils\scan_diagnostics.py:19:    _resolve_two_stage_profile_threshold,
src\ui\utils\scan_diagnostics.py:127:def _evaluate_distribution_drift(
src\ui\utils\scan_diagnostics.py:221:        is_two_stage = isinstance(model, TwoStageModel)
src\ui\utils\scan_diagnostics.py:342:        drift_report = _evaluate_distribution_drift(
src\ui\utils\scan_diagnostics.py:367:                two_stage_threshold = _clamp_two_stage_threshold(
src\ui\utils\scan_diagnostics.py:370:                            'two_stage_threshold_default',
src\ui\utils\scan_diagnostics.py:371:                            getattr(model, 'binary_threshold', DEFAULT_SENSITIVITY_THRESHOLD)
src\ui\utils\scan_diagnostics.py:375:                preds = model.predict(X_preview, threshold=two_stage_threshold)
src\ui\utils\scan_diagnostics.py:376:                report['checks']['two_stage_threshold'] = two_stage_threshold
src\ui\utils\scan_diagnostics.py:380:                report['checks']['two_stage_threshold_strict'] = _clamp_two_stage_threshold(
src\ui\utils\scan_diagnostics.py:383:                            'two_stage_threshold_strict',
src\ui\utils\scan_diagnostics.py:384:                            _resolve_two_stage_profile_threshold(two_stage_threshold, "strict")
src\core\data_loader.py:112:        # Force family-specific mode for NSL-KDD/UNSW to preserve rich features.
src\core\data_loader.py:113:        # Unified CIC schema drops most NSL/UNSW columns and hurts detection quality.
src\core\data_loader.py:114:        if align_to_schema and dataset_type in {"NSL-KDD", "UNSW-NB15"}:
src\core\data_loader.py:134:        # 2a. UNSW-NB15 attack_cat merge (MUST run before Aligner drops non-schema cols)
src\core\data_loader.py:135:        # The 'label' column in UNSW is binary 0/1; real attack names are in 'attack_cat'.
src\core\data_loader.py:215:            # Dataset-aware thresholds: CIC should keep richer coverage than NSL/UNSW.
src\core\data_loader.py:217:                "CIC-IDS": 0.60,
src\core\data_loader.py:218:                "NSL-KDD": 0.10,
src\core\data_loader.py:219:                "UNSW-NB15": 0.18,
src\core\data_loader.py:256:            # CRITICAL FIX: CIC-IDS2018 contains repeating header rows throughout the CSV.
src\core\data_loader.py:340:        Computes all CIC-IDS-like features including:
src\core\data_loader.py:462:            # Convert to DataFrame with ALL CIC-IDS-like columns
src\core\data_loader.py:466:                duration_us = duration * 1e6  # Convert to microseconds for CIC compatibility
src\core\feature_adapter.py:46:    drop_threshold: float = 0.5   # Відсоток відсутніх для DROP стратегії
src\core\feature_adapter.py:50:        if not 0 <= self.drop_threshold <= 1:
src\core\feature_adapter.py:51:            raise ValueError(f"drop_threshold must be in [0, 1], got {self.drop_threshold}")
src\core\feature_adapter.py:178:            if missing_ratio > self.config.drop_threshold:
src\ui\tabs\training.py:14:from src.core.two_stage_model import TwoStageModel
src\ui\tabs\training.py:25:    _calibrate_two_stage_threshold,
src\ui\tabs\training.py:28:    _evaluate_training_quality_gate
src\ui\tabs\training.py:34:    _resolve_two_stage_profile_threshold,
src\ui\tabs\training.py:297:            tab_threshold_info: dict[str, Any] = {
src\ui\tabs\training.py:298:                'threshold': float(DEFAULT_SENSITIVITY_THRESHOLD),
src\ui\tabs\training.py:303:                'evaluated_points': 0,
src\ui\tabs\training.py:316:                best_model: TwoStageModel | None = None
src\ui\tabs\training.py:322:                    local_model = TwoStageModel(binary_model=binary_base, multiclass_model=multiclass_base)
src\ui\tabs\training.py:327:                    local_threshold_info = _calibrate_two_stage_threshold(
src\ui\tabs\training.py:333:                    y_pred_tab = local_model.predict(X_test_tab, threshold=float(local_threshold_info['threshold']))
src\ui\tabs\training.py:345:                        f"Threshold={float(local_threshold_info['threshold']):.2f}"
src\ui\tabs\training.py:352:                        tab_threshold_info = local_threshold_info
src\ui\tabs\training.py:359:                        f"{float(tab_threshold_info['threshold']):.2f} "
src\ui\tabs\training.py:360:                        f"(F1_attack={float(tab_threshold_info['f1_attack']):.3f}, "
src\ui\tabs\training.py:361:                        f"F2_attack={float(tab_threshold_info.get('f2_attack', 0.0)):.3f}, "
src\ui\tabs\training.py:362:                        f"Recall_attack={float(tab_threshold_info['recall_attack']):.3f})"
src\ui\tabs\training.py:384:                        'two_stage_threshold_default': float(tab_threshold_info['threshold']),
src\ui\tabs\training.py:386:                        'two_stage_threshold_strict': _resolve_two_stage_profile_threshold(
src\ui\tabs\training.py:387:                            float(tab_threshold_info['threshold']),
src\ui\tabs\training.py:390:                        'two_stage_threshold_calibration': {
src\ui\tabs\training.py:391:                            'f1_attack': float(tab_threshold_info.get('f1_attack', 0.0)),
src\ui\tabs\training.py:392:                            'f2_attack': float(tab_threshold_info.get('f2_attack', 0.0)),
src\ui\tabs\training.py:393:                            'precision_attack': float(tab_threshold_info.get('precision_attack', 0.0)),
src\ui\tabs\training.py:394:                            'recall_attack': float(tab_threshold_info.get('recall_attack', 0.0)),
src\ui\tabs\training.py:395:                            'evaluated_points': int(tab_threshold_info.get('evaluated_points', 0)),
src\ui\tabs\training.py:396:                            'objective': float(tab_threshold_info.get('objective', 0.0)),
src\ui\tabs\training.py:471:                calib_info = engine_if.auto_calibrate_isolation_threshold(
src\ui\tabs\training.py:478:                    f"threshold={float(calib_info.get('threshold', 0.0)):.4f}"
src\ui\tabs\training.py:481:                calib_info = engine_if.auto_calibrate_isolation_threshold(
src\ui\tabs\training.py:502:                    'if_threshold_mode': getattr(engine_if, 'if_threshold_mode_', 'decision_zero'),
src\ui\tabs\training.py:711:                <li>CIC-IDS2017 (Основна база)</li>
src\ui\tabs\training.py:712:                <li>UNSW-NB15 (Складні патерни)</li>
src\ui\tabs\training.py:713:                <li>CIC-IDS2018 (Сучасні атаки)</li>
src\ui\tabs\training.py:719:        required_patterns = ["CIC-IDS2017", "UNSW-NB15", "CIC-IDS2018"]
src\ui\tabs\training.py:733:                    if pattern == "UNSW-NB15":
src\ui\tabs\training.py:743:                st.info("UNSW-NB15 знайдено у Training_Ready: Mega навчатиметься з урахуванням UNSW.")


--- Output 42 ---
Exit code: 0
Wall time: 0.3 seconds
Output:
                algorithm=config.algorithm,
                fast=True
            )
        else:
            model.fit(X, y_fit)
        
        # РњРµС‚СЂРёРєРё
        metrics = self._compute_metrics(model, X, y_fit)
        
        # Р—Р±РµСЂС–РіР°С”РјРѕ РјРѕРґРµР»СЊ
        self.model = model
        self.algorithm_name = config.algorithm
        
        return TrainingResult(
            model=model,
            metrics=metrics,
            config=config,
            feature_names=list(X.columns),
            class_mapping={i: str(c) for i, c in enumerate(np.unique(y_fit))}
        )
    
    def _compute_metrics(
        self,
        model: Any,
        X: pd.DataFrame,
        y: pd.Series
    ) -> Dict[str, float]:
        """РћР±С‡РёСЃР»РµРЅРЅСЏ РјРµС‚СЂРёРє РЅР° С‚СЂРµРЅСѓРІР°Р»СЊРЅРёС… РґР°РЅРёС…."""
        y_pred = model.predict(X)
        
        return {
            'accuracy': float(accuracy_score(y, y_pred)),
            'precision': float(precision_score(y, y_pred, average='weighted', zero_division=0)),
            'recall': float(recall_score(y, y_pred, average='weighted', zero_division=0)),
            'f1': float(f1_score(y, y_pred, average='weighted', zero_division=0))
        }

    def _create_base_model(
        self, 
        algorithm: str, 
        extra_params: Optional[dict] = None,
        y: Optional[pd.Series] = None
    ) -> Any:
        """РЎС‚РІРѕСЂРµРЅРЅСЏ Р±Р°Р·РѕРІРѕС— РјРѕРґРµР»С– Р· РґРµС„РѕР»С‚РЅРёРјРё РїР°СЂР°РјРµС‚СЂР°РјРё."""
        model_class = self.ALGORITHMS[algorithm]
        
        defaults = {
            'Random Forest': {
                'random_state': 42, 
                'n_jobs': 1,
                'class_weight': 'balanced_subsample',
                'max_features': 'sqrt'
            },
            'XGBoost': {
                'random_state': 42, 
                'n_jobs': 1,
                'eval_metric': 'mlogloss',
                'verbosity': 0
            },
            'Logistic Regression': {
                'random_state': 42, 
                'max_iter': 3000,
                'solver': 'lbfgs',
                'class_weight': 'balanced'
            },
            'Isolation Forest': {
                'random_state': 42,
                'n_jobs': 1,
                'n_estimators': 100,
                'contamination': 0.05,
                'max_features': 1.0
            }
        }
        
        params = defaults.get(algorithm, {})
        
        # Р”Р»СЏ XGBoost СЂРѕР·СЂР°С…РѕРІСѓС”РјРѕ scale_pos_weight СЏРєС‰Рѕ С” РґР°РЅС–
        if algorithm == 'XGBoost' and y is not None:
            unique, counts = np.unique(y, return_counts=True)
            if len(unique) == 2:
                majority = counts.max()
                minority = counts.min()
                params['scale_pos_weight'] = majority / minority
                logger.info(f"[ModelEngine] XGBoost scale_pos_weight set to {params['scale_pos_weight']:.2f}")
        
        if extra_params:
            params.update(extra_params)
        
        return model_class(**params)

    def evaluate(
        self, 
        X_test: pd.DataFrame, 
        y_test: pd.Series
    ) -> dict[str, Any]:
        """
        РћС†С–РЅРєР° РјРѕРґРµР»С– РЅР° С‚РµСЃС‚РѕРІРёС… РґР°РЅРёС….
        
        Args:
            X_test: РўРµСЃС‚РѕРІС– РѕР·РЅР°РєРё
            y_test: РўРµСЃС‚РѕРІС– РјС–С‚РєРё
            
        Returns:
            Dict Р· РјРµС‚СЂРёРєР°РјРё: accuracy, precision, recall, f1, confusion_matrix
            
        Raises:
            RuntimeError: РњРѕРґРµР»СЊ РЅРµ РЅР°РІС‡РµРЅР°
        """
        if self.model is None:
            raise RuntimeError("РњРѕРґРµР»СЊ РЅРµ РЅР°РІС‡РµРЅР° Р°Р±Рѕ РЅРµ Р·Р°РІР°РЅС‚Р°Р¶РµРЅР°")
        
        y_pred = self.model.predict(X_test)
        
        return {
            'accuracy': accuracy_score(y_test, y_pred),
            'precision': precision_score(y_test, y_pred, average='weighted', zero_division=0),
            'recall': recall_score(y_test, y_pred, average='weighted', zero_division=0),
            'f1': f1_score(y_test, y_pred, average='weighted', zero_division=0),
            'confusion_matrix': confusion_matrix(y_test, y_pred).tolist()
        }

    def _is_isolation_forest(self) -> bool:
        """Check if current model is Isolation Forest (handles name variants)."""
        if self.algorithm_name is None:
            return False
        return 'Isolation Forest' in self.algorithm_name

    def predict(self, X: pd.DataFrame) -> np.ndarray:
        """
        РџСЂРѕРіРЅРѕР·СѓРІР°РЅРЅСЏ РєР»Р°СЃС–РІ.
        
        Args:
            X: Р”Р°РЅС– РґР»СЏ РїСЂРѕРіРЅРѕР·СѓРІР°РЅРЅСЏ
            
        Returns:
            РњР°СЃРёРІ РїРµСЂРµРґР±Р°С‡РµРЅРёС… РєР»Р°СЃС–РІ
        """
        if self.model is None:
            raise RuntimeError("РњРѕРґРµР»СЊ РЅРµ РЅР°РІС‡РµРЅР° Р°Р±Рѕ РЅРµ Р·Р°РІР°РЅС‚Р°Р¶РµРЅР°")
        
        # Isolation Forest handling - use decision_function for scoring


--- Output 43 ---
Exit code: 0
Wall time: 0.3 seconds
Output:
    if len(y_full) == 0:
        return None, None

    return X_full, y_full

def _resolve_normal_label_ids(label_map: dict[int, Any]) -> list[int]:
    normal_ids = [
        class_id
        for class_id, class_name in label_map.items()
        if str(class_name).strip().lower() in BENIGN_LABEL_TOKENS
    ]
    return normal_ids or [0]


def _calibrate_two_stage_threshold(
    model: TwoStageModel,
    X_val: pd.DataFrame,
    y_val: pd.Series,
    benign_code: int | None = None
) -> dict[str, Any]:
    """
    Auto-select the best Two-Stage binary threshold on validation data.
    Objective: maximize attack F2 (recall-oriented) with precision guard.
    """
    from sklearn.metrics import f1_score, precision_score, recall_score

    fallback_threshold = _clamp_two_stage_threshold(
        float(getattr(model, 'binary_threshold', DEFAULT_SENSITIVITY_THRESHOLD))
    )
    result: dict[str, Any] = {
        'threshold': fallback_threshold,
        'f1_attack': 0.0,
        'f2_attack': 0.0,
        'precision_attack': 0.0,
        'recall_attack': 0.0,
        'evaluated_points': 0,
        'objective': 0.0,
    }

    if X_val is None or y_val is None or len(X_val) == 0 or len(y_val) == 0:
        model.binary_threshold = fallback_threshold
        result['reason'] = 'empty_validation'
        return result

    benign_code_resolved = benign_code
    if benign_code_resolved is None:
        benign_code_resolved = getattr(model, 'benign_code_', None)
    if benign_code_resolved is None:
        benign_code_resolved = 0

    y_true_attack = (np.asarray(y_val) != benign_code_resolved).astype(int)
    if len(np.unique(y_true_attack)) < 2:
        # Not enough class variance for stable threshold calibration.
        model.binary_threshold = fallback_threshold
        result['reason'] = 'single_class_validation'
        return result

    attack_rate = float(np.mean(y_true_attack))
    # Adaptive FP target and guards for low-prevalence datasets.
    if attack_rate < 0.02:
        target_fp_rate = 0.005
        precision_floor = 0.75
        max_attack_rate = min(0.08, (attack_rate * 2.5) + 0.02)
        fp_penalty_weight = 1.6
        over_pred_weight = 0.7
        precision_weight = 0.45
    elif attack_rate < 0.05:
        target_fp_rate = 0.01
        precision_floor = 0.70
        max_attack_rate = min(0.12, (attack_rate * 2.0) + 0.03)
        fp_penalty_weight = 1.2
        over_pred_weight = 0.55
        precision_weight = 0.40
    elif attack_rate < 0.10:
        target_fp_rate = 0.02
        precision_floor = 0.65
        max_attack_rate = min(0.20, (attack_rate * 1.8) + 0.04)
        fp_penalty_weight = 1.0
        over_pred_weight = 0.40
        precision_weight = 0.38
    else:
        target_fp_rate = 0.03 if attack_rate < 0.25 else 0.04
        precision_floor = 0.60
        max_attack_rate = min(0.60, (attack_rate * 1.6) + 0.05)
        fp_penalty_weight = 0.9
        over_pred_weight = 0.25
        precision_weight = 0.35
    max_attack_rate = float(np.clip(max_attack_rate, 0.01, 0.90))

    best = {
        'threshold': fallback_threshold,
        'f1_attack': -1.0,
        'f2_attack': -1.0,
        'precision_attack': 0.0,
        'recall_attack': 0.0,
        'objective': -1e9,
    }

    grid = np.arange(
        TWO_STAGE_THRESHOLD_MIN,
        TWO_STAGE_THRESHOLD_MAX + (TWO_STAGE_THRESHOLD_STEP / 2.0),
        TWO_STAGE_THRESHOLD_STEP
    )
    result['evaluated_points'] = int(len(grid))
    threshold_stats: list[dict[str, float]] = []

    for thr in grid:
        threshold = _clamp_two_stage_threshold(float(thr))
        y_pred = model.predict(X_val, threshold=threshold)
        y_pred_attack = (np.asarray(y_pred) != benign_code_resolved).astype(int)

        f1 = float(f1_score(y_true_attack, y_pred_attack, zero_division=0))
        precision = float(precision_score(y_true_attack, y_pred_attack, zero_division=0))
        recall = float(recall_score(y_true_attack, y_pred_attack, zero_division=0))
        fp_rate = 0.0
        pred_attack_rate = float(np.mean(y_pred_attack)) if len(y_pred_attack) else 0.0
        if len(y_true_attack):
            tn = int(np.sum((y_true_attack == 0) & (y_pred_attack == 0)))
            fp = int(np.sum((y_true_attack == 0) & (y_pred_attack == 1)))
            fp_rate = fp / (fp + tn) if (fp + tn) else 0.0
        denom = (4.0 * precision) + recall
        f2 = float((5.0 * precision * recall) / denom) if denom > 0 else 0.0

        # Soft guard against extremely low precision.
        precision_penalty = max(0.0, precision_floor - precision)
        fp_penalty = max(0.0, fp_rate - target_fp_rate)
        over_pred_penalty = max(0.0, pred_attack_rate - max_attack_rate)
        objective = (
            f2
            - (precision_penalty * precision_penalty * precision_weight)
            - (fp_penalty * fp_penalty_weight)
            - (over_pred_penalty * over_pred_weight)
        )
        threshold_stats.append(
            {
                'threshold': threshold,
                'f1_attack': f1,
                'f2_attack': f2,
                'precision_attack': precision,
                'recall_attack': recall,
                'fp_rate': fp_rate,
                'pred_attack_rate': pred_attack_rate,
                'objective': objective,
            }
        )

        better = False
        if objective > best['objective'] + 1e-12:
            better = True
        elif abs(objective - best['objective']) <= 1e-12 and recall > best['recall_attack'] + 1e-12:
            better = True
        elif (
            abs(objective - best['objective']) <= 1e-12
            and abs(recall - best['recall_attack']) <= 1e-12
            and abs(f1 - best['f1_attack']) <= 1e-12
            and abs(threshold - DEFAULT_SENSITIVITY_THRESHOLD) < abs(best['threshold'] - DEFAULT_SENSITIVITY_THRESHOLD)
        ):
            better = True

        if better:
            best = {
                'threshold': threshold,
                'f1_attack': f1,
                'f2_attack': f2,
                'precision_attack': precision,
                'recall_attack': recall,
                'objective': objective,
            }

    # РЎС‚Р°Р±С–Р»С–Р·Р°С†С–СЏ РїРѕСЂРѕРіСѓ:
    # СЏРєС‰Рѕ РєС–Р»СЊРєР° РїРѕСЂРѕРіС–РІ РјР°Р№Р¶Рµ СЂС–РІРЅРѕС†С–РЅРЅС– Р·Р° F2, РѕР±РёСЂР°С”РјРѕ С‚РѕР№, С‰Рѕ Р±Р»РёР¶С‡Рµ
    # РґРѕ РґРµС„РѕР»С‚РЅРѕРіРѕ С– Р·Р°Р·РІРёС‡Р°Р№ РґР°С” СЃС‚Р°Р±С–Р»СЊРЅС–С€РёР№ Р±Р°Р»Р°РЅСЃ FP/FN.
    if threshold_stats:
        f2_tolerance = 0.002
        precision_tolerance = 0.03
        stable_pool = [
            item for item in threshold_stats
            if item['f2_attack'] >= (best['f2_attack'] - f2_tolerance)
            and item['precision_attack'] >= max(0.0, best['precision_attack'] - precision_tolerance)
        ]
        if stable_pool:
            stable_choice = min(
                stable_pool,
                key=lambda item: (
                    abs(item['threshold'] - DEFAULT_SENSITIVITY_THRESHOLD),
                    -item['recall_attack'],
                    -item['f2_attack']
                )
            )
            best = stable_choice

    # Enforce FP guard using benign-only quantile if needed.
    try:
        probas = model.binary_model.predict_proba(X_val)
        attack_idx = getattr(model, 'attack_idx_', None)
        if attack_idx is None:
            attack_idx = int(np.where(model.binary_model.classes_ == 1)[0][0]) if hasattr(model.binary_model, 'classes_') else 1
        attack_probs = probas[:, attack_idx]
        benign_mask = (y_true_attack == 0)
        if benign_mask.any():
            fp_threshold = float(np.quantile(attack_probs[benign_mask], 1.0 - target_fp_rate))
            fp_threshold = _clamp_two_stage_threshold(fp_threshold)
            if fp_threshold > best['threshold']:
                best['threshold'] = fp_threshold
        # Guard predicted attack rate on validation set (use all samples).
        rate_threshold = float(np.quantile(attack_probs, 1.0 - max_attack_rate))
        rate_threshold = _clamp_two_stage_threshold(rate_threshold)
        if rate_threshold > best['threshold']:
            best['threshold'] = rate_threshold
    except Exception:
        pass

    model.binary_threshold = float(best['threshold'])
    # Refresh metrics for the final threshold.
    y_pred_final = model.predict(X_val, threshold=model.binary_threshold)
    y_pred_attack_final = (np.asarray(y_pred_final) != benign_code_resolved).astype(int)
    result.update({
        'threshold': float(model.binary_threshold),
        'f1_attack': float(f1_score(y_true_attack, y_pred_attack_final, zero_division=0)),
        'f2_attack': float(best.get('f2_attack', 0.0)),
        'precision_attack': float(precision_score(y_true_attack, y_pred_attack_final, zero_division=0)),
        'recall_attack': float(recall_score(y_true_attack, y_pred_attack_final, zero_division=0)),
        'evaluated_points': int(result.get('evaluated_points', 0)),
        'objective': float(best.get('objective', 0.0)),
        'target_fp_rate': float(target_fp_rate),
        'max_attack_rate': float(max_attack_rate),
        'precision_floor': float(precision_floor),
    })
    return result


def _evaluate_training_quality_gate(
    metrics: dict[str, Any],
    *,
    is_isolation_algorithm: bool,
    two_stage_mode: bool,
    is_mega_model: bool,
    trained_family_count: int = 0,
    training_file_count: int = 1
) -> dict[str, Any]:
    """
    РЇРєС–СЃРЅРёР№ С„С–Р»СЊС‚СЂ РїРµСЂРµРґ Р·Р±РµСЂРµР¶РµРЅРЅСЏРј РјРѕРґРµР»С–.
    РћС†С–РЅСЋС” Р·РґР°С‚РЅС–СЃС‚СЊ Р·РЅР°С…РѕРґРёС‚Рё Р°С‚Р°РєРё, Р° РЅРµ Р»РёС€Рµ Р·Р°РіР°Р»СЊРЅСѓ С‚РѕС‡РЅС–СЃС‚СЊ.
    """
    def _safe(value: Any) -> float:
        try:
            v = float(value)
            if not np.isfinite(v):
                return 0.0
            return v
        except Exception:


--- Output 44 ---
Exit code: 0
Wall time: 0.3 seconds
Output:
IDS ML Analyzer - Two-Stage Model

Реалізація дворівневої системи виявлення атак:
Stage 1: Binary Classifier (Attack vs Normal) - оптимізовано на Recall.
Stage 2: Multiclass Classifier (Attack Type) - визначає тип атаки.

Цей клас обгортає дві ML моделі і надає інтерфейс, сумісний з sklearn (fit/predict).
"""

import numpy as np
import pandas as pd
from sklearn.base import BaseEstimator, ClassifierMixin, clone
from sklearn.utils.validation import check_X_y, check_array, check_is_fitted
from sklearn.ensemble import RandomForestClassifier
import logging

logger = logging.getLogger(__name__)

class TwoStageModel(BaseEstimator, ClassifierMixin):
    """
    Двоетапна модель для IDS.
    
    Parameters:
        binary_model: Модель для першого етапу (за замовчуванням RandomForest)
        multiclass_model: Модель для другого етапу (за замовчуванням RandomForest)
        binary_threshold: Поріг для бінарного класифікатора (default=0.3 для High Recall)
    """
    
    def __init__(
        self,
        binary_model=None,
        multiclass_model=None,
        binary_threshold=0.3,
        binary_target_attack_rate=0.20,
        binary_downsample=True,
        stage2_balance=True,
        stage2_balance_min_samples=800,
        stage2_balance_max_multiplier=3.0,
        random_state=42
    ):
        # NOTE: Do not use truthiness for sklearn estimators.
        # Unfitted estimators may define __len__ and raise AttributeError.
        # Stability-first defaults: n_jobs=1 avoids process storms / RAM spikes in Streamlit runtime.
        self.binary_model = binary_model if binary_model is not None else RandomForestClassifier(n_jobs=1, random_state=42)
        self.multiclass_model = multiclass_model if multiclass_model is not None else RandomForestClassifier(n_jobs=1, random_state=42)
        self.binary_threshold = binary_threshold
        self.binary_target_attack_rate = binary_target_attack_rate
        self.binary_downsample = binary_downsample
        self.stage2_balance = stage2_balance
        self.stage2_balance_min_samples = stage2_balance_min_samples
        self.stage2_balance_max_multiplier = stage2_balance_max_multiplier
        self.random_state = random_state
        self.classes_ = None
        self.binary_classes_ = None
        self.singleton_attack_label_ = None # Якщо тільки один тип атаки
        self.stage2_label_to_index_ = None
        self.stage2_index_to_label_ = None
        self.is_fitted_ = False
        self.binary_sampling_info_ = {}
        self.stage2_sampling_info_ = {}

    @staticmethod
    def _force_single_thread(model) -> None:
        """
        У predict/predict_proba працюємо в single-thread режимі для стабільності.
        Це прибирає пікові навантаження та WinError у деяких середовищах.
        """
        if model is None:
            return
        try:
            if hasattr(model, "n_jobs"):
                try:
                    model.n_jobs = 1
                except Exception:
                    pass
            if hasattr(model, "set_params"):
                try:
                    model.set_params(n_jobs=1)
                except Exception:
                    pass
        except Exception:
            pass

    def fit(self, X, y, benign_label='BENIGN', benign_code=None):
        """
        Тренування двоетапної моделі.
        
        Args:
            X: Ознаки
            y: Мітки (можуть бути числовими або рядковими)
            benign_label: Назва нормального класу (для рядкових міток)
            benign_code: Числовий код нормального класу (для закодованих міток)
        """
        X = pd.DataFrame(X) if not isinstance(X, pd.DataFrame) else X
        y = pd.Series(y) if not isinstance(y, pd.Series) else y
        
        logger.info(f"[DIAGNOSTIC] TwoStageModel.fit() called")
        logger.info(f"[DIAGNOSTIC] Training data shape: {X.shape}")
        logger.info(f"[DIAGNOSTIC] Unique labels in y: {np.unique(y)}")
        
        # Клонуємо базові оцінювачі перед кожним fit, щоб уникати застарілого стану
        # (критично для стабільної індексації класів у XGBoost при повторному навчанні).
        self.binary_model = clone(self.binary_model)
        if self.multiclass_model is not None:
            self.multiclass_model = clone(self.multiclass_model)

        # DIAGNOSTIC: warn only on re-fit, not on first fit.
        if getattr(self, 'is_fitted_', False):
            logger.warning(f"[DIAGNOSTIC] WARNING: Overwriting existing binary_model!")
            logger.warning(f"[DIAGNOSTIC] Catastrophic Forgetting Risk: YES")
            logger.warning(f"[DIAGNOSTIC] Previous knowledge will be lost!")
        
        logger.info(f"TwoStageModel: Training Stage 1 (Binary) on {len(X)} samples...")
        
        # --- STAGE 1 PREPARATION ---
        # Створюємо бінарні мітки: 0 = Benign, 1 = Attack
        # Важливо обробити випадок, коли y може бути вже закодованим (числа) або рядками
        # Припускаємо, що benign_label - це мітка нормального трафіку.
        # Якщо y - числа, треба знати, яке число відповідає Benign.
        # Але зазвичай в нашому pipeline сюди приходять вже закодовані числа (0=BENIGN).
        # Тому зробимо припущення: 0 - це завжди нормальний трафік (стандарт LabelEncoder).
        
        # Перевірка: якщо benign_label це рядок, а y - числа, то треба бути обережним.
        # Наш Preprocessor кодує мітки. Зазвичай найпопулярніший клас (BENIGN) отримує код 0 або інший.
        # Але ми не можемо гарантувати це.
        # Тому надійніше тренувати на тому, що є, але для чистоти експерименту
        # краще передавати сюди НЕЗАКОДОВАНІ y, або знати мапінг.
        
        # Aле ModelEngine отримує вже закодовані X, y від Preprocessor.
        # Тому ми будемо вважати: 0 = BENIGN (якщо це так працює в нашому Preprocessor).
        # Давайте перевіримо: Preprocessor використовує LabelEncoder.
        # LabelEncoder сортує алфавітно. "BENIGN" < "DDoS". Тобто BENIGN швидше за все не 0?
        # "BENIGN" vs "Bot". BENIGN йде першим. 
        # "BENIGN" vs "BruteForce". BENIGN йде першим.
        # Але краще не гадати. 
        
        # --- ВИЗНАЧЕННЯ BENIGN КЛАСУ ---
        # Явне визначення пріоритетніше за евристику
        if benign_code is not None:
            # Explicit benign class/code provided by caller.
            pass
        elif isinstance(y.iloc[0] if hasattr(y, 'iloc') else y[0], str):
            # String labels: try exact match first, then safe case-insensitive fallbacks.
            if benign_label in y.values:
                benign_code = benign_label
            else:
                lower_map = {str(v).strip().lower(): v for v in np.unique(y)}
                fallback = lower_map.get(str(benign_label).strip().lower())
                if fallback is not None:
                    benign_code = fallback
                elif 'benign' in lower_map:
                    benign_code = lower_map['benign']
                elif 'normal' in lower_map:
                    benign_code = lower_map['normal']
                else:
                    raise ValueError(
                        f"Benign label '{benign_label}' not found in y. Available: {np.unique(y)}"
                    )
        else:
            # Numeric labels: only safe implicit rule is class 0 as BENIGN.
            # Otherwise require explicit benign_code to avoid silent mislabeling.
            unique_vals = np.unique(y)
            if 0 in set(unique_vals.tolist()):
                benign_code = 0
            else:
                raise ValueError(
                    "Cannot infer BENIGN class for numeric labels without class 0. "
                    "Pass benign_code explicitly from label mapping."
                )
        
        logger.info(f"[TwoStageModel] Benign code set to: '{benign_code}'")
        
        # --- STAGE 1: Тренування бінарної моделі ---
        # Бінарні мітки: 0 = Benign, 1 = Attack
        y_binary = (y != benign_code).astype(int)

        X_stage1 = X
        y_stage1 = y_binary
        self.binary_sampling_info_ = {
            'enabled': bool(self.binary_downsample),
            'attack_rate_raw': float(np.mean(y_binary)) if len(y_binary) else 0.0,
            'target_rate': float(np.clip(self.binary_target_attack_rate, 0.05, 0.50)),
            'used_attack_samples': int(np.sum(y_binary == 1)) if len(y_binary) else 0,
            'used_benign_samples': int(np.sum(y_binary == 0)) if len(y_binary) else 0,
        }

        # Optional downsampling of dominant attack class to reduce base-rate bias.
        if self.binary_downsample and len(y_binary) > 0:
            attack_idx = np.flatnonzero(y_binary == 1)
            benign_idx = np.flatnonzero(y_binary == 0)
            if len(attack_idx) > 0 and len(benign_idx) > 0:
                attack_rate = float(len(attack_idx) / len(y_binary))
                target_rate = self.binary_sampling_info_['target_rate']
                if attack_rate > (target_rate + 0.02):
                    desired_attack = int(round(len(benign_idx) * target_rate / max(1e-9, (1.0 - target_rate))))
                    desired_attack = max(1, min(desired_attack, len(attack_idx)))
                    rng = np.random.default_rng(self.random_state)
                    attack_sample = rng.choice(attack_idx, size=desired_attack, replace=False)
                    keep_idx = np.concatenate([benign_idx, attack_sample])
                    rng.shuffle(keep_idx)
                    X_stage1 = X.iloc[keep_idx]
                    y_stage1 = y_binary.iloc[keep_idx]
                    self.binary_sampling_info_.update({
                        'attack_rate_raw': float(attack_rate),
                        'used_attack_samples': int(desired_attack),
                        'used_benign_samples': int(len(benign_idx)),
                        'downsampled': True
                    })
                else:
                    self.binary_sampling_info_.update({'downsampled': False})
            else:
                self.binary_sampling_info_.update({'downsampled': False})
        else:
            self.binary_sampling_info_.update({'downsampled': False})

        self.binary_model.fit(X_stage1, y_stage1)
        self.binary_classes_ = self.binary_model.classes_
        
        # Зберігаємо індекс attack класу для predict_proba
        if len(self.binary_classes_) == 2:
            self.attack_idx_ = np.where(self.binary_classes_ == 1)[0]
            if len(self.attack_idx_) == 0:
                self.attack_idx_ = 1 if self.binary_classes_[1] != 0 else 0
            else:
                self.attack_idx_ = self.attack_idx_[0]
        else:
            self.attack_idx_ = 0
        
        logger.info(f"[TwoStageModel] Binary classes: {self.binary_classes_}, attack_idx: {self.attack_idx_}")
        
        # --- STAGE 2 PREPARATION ---
        # Беремо тільки атаки
        attack_mask = (y != benign_code)
        X_attack = X[attack_mask]
        y_attack = y[attack_mask]
        
        unique_attacks = np.unique(y_attack)
        
        if len(unique_attacks) == 0:
            logger.warning("TwoStageModel: No attack samples found! Stage 2 will not be trained.")
            self.multiclass_model = None
            self.stage2_label_to_index_ = None
            self.stage2_index_to_label_ = None
        elif len(unique_attacks) == 1:
            logger.info(f"TwoStageModel: Only one type of attack found ({unique_attacks[0]}). Stage 2 training skipped.")
            self.singleton_attack_label_ = unique_attacks[0]
            self.multiclass_model = None # Don't need model for 1 class
            self.stage2_label_to_index_ = None
            self.stage2_index_to_label_ = None
        else:
            logger.info(f"TwoStageModel: Training Stage 2 (Multiclass) on {len(X_attack)} attack samples ({len(unique_attacks)} types)...")
            # Stage-2 тренується на власному просторі класів 0..N-1.
            # Це обов'язково для XGBoost (і безпечно для інших класифікаторів).
            self.stage2_label_to_index_ = {label: idx for idx, label in enumerate(unique_attacks)}
            self.stage2_index_to_label_ = {idx: label for label, idx in self.stage2_label_to_index_.items()}
            y_attack_encoded = pd.Series(y_attack).map(self.stage2_label_to_index_)
            if y_attack_encoded.isnull().any():
                raise ValueError("TwoStageModel: Failed to encode Stage-2 attack labels.")

            # Балансування рідкісних типів атак для Stage-2.
            # Мета: зменшити washout-ефект у Mega/змішаних датасетах,
            # не роздуваючи вибірку агресивним oversampling.
            X_stage2 = X_attack
            y_stage2 = y_attack_encoded.to_numpy(dtype=int)
            self.stage2_sampling_info_ = {
                'enabled': bool(self.stage2_balance),
                'samples_raw': int(len(y_stage2)),
                'classes_raw': int(len(unique_attacks)),
                'min_samples_target': int(self.stage2_balance_min_samples),
                'max_multiplier': float(self.stage2_balance_max_multiplier),
                'samples_after': int(len(y_stage2)),
                'oversampled_classes': 0,
            }

            if self.stage2_balance and len(y_stage2) > 0:
                class_counts = pd.Series(y_stage2).value_counts().to_dict()
                rng = np.random.default_rng(self.random_state)
                sampled_indices = []
                oversampled_classes = 0



--- Output 45 ---
Exit code: 0
Wall time: 0.4 seconds
Output:

                for cls_id, cls_count in class_counts.items():
                    cls_idx = np.flatnonzero(y_stage2 == int(cls_id))
                    if len(cls_idx) == 0:
                        continue

                    max_for_class = int(max(1, round(cls_count * float(self.stage2_balance_max_multiplier))))
                    target_for_class = max(
                        cls_count,
                        min(int(self.stage2_balance_min_samples), max_for_class)
                    )

                    if target_for_class > cls_count:
                        extra = rng.choice(cls_idx, size=(target_for_class - cls_count), replace=True)
                        cls_idx = np.concatenate([cls_idx, extra])
                        oversampled_classes += 1

                    sampled_indices.append(cls_idx)

                if sampled_indices:
                    keep_idx = np.concatenate(sampled_indices)
                    rng.shuffle(keep_idx)
                    X_stage2 = X_attack.iloc[keep_idx]
                    y_stage2 = y_stage2[keep_idx]
                    self.stage2_sampling_info_.update({
                        'samples_after': int(len(y_stage2)),
                        'oversampled_classes': int(oversampled_classes),
                    })
                    logger.info(
                        "[TwoStageModel] Stage-2 balancing applied: "
                        f"{self.stage2_sampling_info_['samples_raw']} -> {self.stage2_sampling_info_['samples_after']} samples, "
                        f"oversampled_classes={oversampled_classes}/{len(class_counts)}"
                    )

            self.multiclass_model.fit(X_stage2, y_stage2)
            self.singleton_attack_label_ = None
        
        self.classes_ = np.unique(y) # Всі можливі класи
        self.benign_code_ = benign_code
        self.is_fitted_ = True
        
        return self

    def predict(self, X, threshold=None):
        """
        Прогнозування класів.
        
        Логіка:
        1. Stage 1 get proba(Attack).
        2. If proba > threshold -> Stage 2 predict.
        3. Else -> Benign.
        """
        check_is_fitted(self, 'is_fitted_')
        thresh = threshold if threshold is not None else self.binary_threshold

        self._force_single_thread(self.binary_model)
        self._force_single_thread(self.multiclass_model)
        
        # Stage 1
        # Proba для класу 1 (Attack)
        try:
            binary_probas = self.binary_model.predict_proba(X)
            
            # Використовуємо збережений attack_idx_ з fit
            if hasattr(self, 'attack_idx_'):
                attack_idx = self.attack_idx_
            else:
                # Fallback: знаходимо індекс класу 1 (Attack)
                attack_idx = np.where(self.binary_model.classes_ == 1)[0]
                attack_idx = attack_idx[0] if len(attack_idx) > 0 else 1
            
            # Перевірка меж
            if binary_probas.shape[1] <= attack_idx:
                logger.warning(f"[TwoStageModel] attack_idx {attack_idx} out of bounds, using 0")
                attack_idx = 0
                
            attack_probs = binary_probas[:, attack_idx]
            
        except Exception as e:
            logger.error(f"[TwoStageModel] Binary predict error: {e}")
            # Fallback: використовуємо predict замість predict_proba
            binary_preds = self.binary_model.predict(X)
            attack_probs = (binary_preds != 0).astype(float)
            
        # Маска атак
        is_attack = attack_probs > thresh
        
        # Результати (спочатку всі Benign)
        dtype = self.classes_.dtype if hasattr(self, 'classes_') and self.classes_ is not None else object
        final_preds = np.full(len(X), self.benign_code_, dtype=dtype)
        
        # ҳ, що пройшли поріг, відправляємо на Stage 2
        if np.any(is_attack):
            X_attacks = X[is_attack]
            if self.multiclass_model is not None:
                attack_preds = self.multiclass_model.predict(X_attacks)
                if self.stage2_index_to_label_:
                    decoded_attack_preds = np.asarray([
                        self.stage2_index_to_label_.get(int(pred), pred) for pred in attack_preds
                    ], dtype=dtype)
                    final_preds[is_attack] = decoded_attack_preds
                else:
                    final_preds[is_attack] = attack_preds
            elif self.singleton_attack_label_ is not None:
                final_preds[is_attack] = self.singleton_attack_label_
            else:
                # Якщо атак не було в тренуванні, але детектуємо в Stage 1
                logger.warning("[TwoStageModel] Attacks detected but no Stage 2 model available")
                
        return final_preds

    def predict_proba(self, X):
        """
        Slightly hacky implementation for compatibility.
        Returns proba from Stage 2 for attacks, and 1.0 for Benign for normals.
        """
        # Це складно для Hybrid model.
        # Поки повернемо заглушку або реалізуємо пізніше, якщо треба для метрик.
        # Для ROC-AUC треба.
        self._force_single_thread(self.binary_model)
        return self.binary_model.predict_proba(X) # Повертаємо бінарні ймовірності поки що


--- Output 46 ---
Exit code: 0
Wall time: 0.3 seconds
Output:
import os
import time
import pandas as pd
from pathlib import Path
from collections import defaultdict
import numpy as np

# Adjust imports to use src
sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from src.core.data_loader import DataLoader
from src.core.preprocessor import Preprocessor
from src.core.model_engine import ModelEngine
from sklearn.metrics import confusion_matrix

def run_simulation():
    # Setup
    root_dir = Path(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
    training_dir = root_dir / 'datasets' / 'Training_Ready'
    test_dir = root_dir / 'datasets' / 'TEST_DATA'
    
    loader = DataLoader(verbose_diagnostics=False)
    
    # Map dataset family to its training file available in Training_Ready
    training_files = {
        'NSL-KDD': training_dir / 'NSL_KDD_train.csv',
        'UNSW_NB15': training_dir / 'UNSW_NB15_train.csv',
        'CIC-IDS2017': training_dir / 'CIC-IDS2017_[DDoS].csv', 
        'CIC-IDS2018': training_dir / 'CIC-IDS2018_[BruteForce-FTP_SSH].csv'
    }

    report = ["# РЎРёРјСѓР»СЏС†С–СЏ: РќР°РІС‡Р°РЅРЅСЏ РјРѕРґРµР»РµР№ С‚Р° С‚РµСЃС‚СѓРІР°РЅРЅСЏ РЅР° Р·РіРµРЅРµСЂРѕРІР°РЅРёС… РґР°С‚Р°СЃРµС‚Р°С…\n"]
    
    for family, train_file in training_files.items():
        if not train_file.exists():
            report.append(f"вљ пёЏ **{family}**: Р¤Р°Р№Р» {train_file.name} РЅРµ Р·РЅР°Р№РґРµРЅРѕ, РїСЂРѕРїСѓСЃРєР°С”РјРѕ.\n")
            continue
            
        print(f"\\n--- РЎРёРјСѓР»СЏС†С–СЏ РґР»СЏ {family} ---")
        report.append(f"## {family}")
        
        # 1. РўСЂРµРЅСѓРІР°РЅРЅСЏ
        print(f"РќР°РІС‡Р°РЅРЅСЏ РЅР° {train_file.name} (РґРѕ 500,000 СЂСЏРґРєС–РІ)...")
        try:
            # РћР±РјРµР¶РёРјРѕ РєС–Р»СЊРєС–СЃС‚СЊ СЂСЏРґРєС–РІ РґР»СЏ РїРѕРІРЅРѕС†С–РЅРЅРѕС— СЃРёРјСѓР»СЏС†С–С—
            df_train = loader.load_file(str(train_file), max_rows=500000, align_to_schema=(family.startswith('CIC')), multiclass=True)
            if 'label' not in df_train.columns:
                report.append(f"вљ пёЏ `{train_file.name}` РЅРµ РјР°С” РєРѕР»РѕРЅРєРё 'label'.\n")
                continue
                
            preprocessor = Preprocessor(enable_scaling=False)
            X_train, y_train = preprocessor.fit_transform(df_train, target_col='label')
            
            engine = ModelEngine(models_dir=str(root_dir / 'models'))
            
            # Р’РёРєРѕСЂРёСЃС‚РѕРІСѓС”РјРѕ СЂРµР°Р»СЊРЅСѓ РѕРїС‚РёРјС–Р·Р°С†С–СЋ РіС–РїРµСЂРїР°СЂР°РјРµС‚СЂС–РІ (GridSearch / CV)
            print("Р’РёРєРѕРЅСѓС”С‚СЊСЃСЏ РѕРїС‚РёРјС–Р·Р°С†С–СЏ РіС–РїРµСЂРїР°СЂР°РјРµС‚СЂС–РІ (Cross-Validation)...")
            model, search_info = engine.optimize_hyperparameters(
                X_train, 
                y_train, 
                algorithm='Random Forest', 
                search_type='grid', 
                fast=True
            )
            
            # Extract Normal Code mapping (Assuming 0 is normal, 1 is attack, but checking safely)
            label_map = preprocessor.get_label_map()
            
            report.append("### 1. РќР°РІС‡Р°РЅРЅСЏ")
            report.append(f"- **Р”Р°С‚Р°СЃРµС‚**: `{train_file.name}`")
            report.append(f"- **РљРѕСЂРёРіСѓРІР°РЅРЅСЏ**: {len(X_train)} Р·СЂР°Р·РєС–РІ")
            report.append(f"- **РђР»РіРѕСЂРёС‚Рј**: Random Forest")
            report.append(f"- **РљР»Р°СЃРё (РµРЅРєРѕРґРёРЅРі)**: {label_map}\n")

            # 2. РўРµСЃС‚СѓРІР°РЅРЅСЏ РґР»СЏ РєРѕР¶РЅРѕРіРѕ С„Р°Р№Р»Сѓ 
            test_files = [f for f in test_dir.glob(f'{family}*.csv')]
            if not test_files:
                report.append(f"вљ пёЏ РќРµ Р·РЅР°Р№РґРµРЅРѕ С‚РµСЃС‚РѕРІРёС… С„Р°Р№Р»С–РІ РґР»СЏ {family}.\n")
                continue
            
            report.append("### 2. РўРµСЃС‚СѓРІР°РЅРЅСЏ")
            report.append("| Р¤Р°Р№Р» | Р’СЃСЊРѕРіРѕ СЂСЏРґРєС–РІ | Р—РЅР°Р№РґРµРЅРѕ РђРЅРѕРјР°Р»С–Р№ | True РџРѕР·РёС‚РёРІ (%) | False Positives (%) |")
            report.append("| :--- | :--- | :--- | :--- | :--- |")
            
            for tf in test_files:
                df_test = loader.load_file(str(tf), align_to_schema=(family.startswith('CIC')), multiclass=False)
                if 'label' not in df_test.columns:
                    continue
                
                # We need to map labels manually according to the model's preprocessor if possible
                # But test dataset labels are usually 'BENIGN' / 'ATTACK' after loader multiclass=False
                # DataLoader sets 'label' to string 'BENIGN' / 'ATTACK' (often)
                
                y_test_raw = df_test['label'].astype(str).str.strip().str.upper()
                df_test_clean = df_test.drop(columns=['label'])
                
                X_test = preprocessor.transform(df_test_clean)
                preds = engine.predict(X_test)
                
                # Inverse transform to read predictions
                try:
                    pred_labels_text = preprocessor.target_encoder.inverse_transform(preds)
                except Exception:
                    pred_labels_text = np.array(['BENIGN' if p == 0 else 'ATTACK' for p in preds])
                
                pred_labels_text_upper = np.array([str(p).strip().upper() for p in pred_labels_text])
                
                # Find how many anomalies detected
                # Usually anything not BENIGN / NORMAL is an anomaly
                normal_keywords = {'BENIGN', 'NORMAL', '0'}
                actual_anomalies = ~y_test_raw.isin(normal_keywords)
                pred_anomalies = ~np.isin(pred_labels_text_upper, list(normal_keywords))
                
                total_rows = len(df_test)
                detected = pred_anomalies.sum()
                
                # Metrics
                actual_A = actual_anomalies.sum()
                pred_A = pred_anomalies.sum()

                # Correctly identified attacks
                TP = ((actual_anomalies == True) & (pred_anomalies == True)).sum()
                # False identified attacks
                FP = ((actual_anomalies == False) & (pred_anomalies == True)).sum()
                
                tp_pct = (TP / actual_A * 100) if actual_A > 0 else 0
                fp_pct = (FP / (total_rows - actual_A) * 100) if (total_rows - actual_A) > 0 else 0
                
                report.append(f"| `{tf.name}` | {total_rows:,} | {detected:,} | {tp_pct:.1f}% | {fp_pct:.1f}% |")
                
        except Exception as e:
            report.append(f"вќЊ РџРѕРјРёР»РєР° РїС–Рґ С‡Р°СЃ СЃРёРјСѓР»СЏС†С–С— {family}: {str(e)}\n")
            print(f"Error {family}: {e}")
            import traceback
            traceback.print_exc()

    # Save logic
    with open(root_dir / "simulation_report.md", "w", encoding="utf-8") as f:
        f.write("\\n".join(report))
        
    print("РЎРёРјСѓР»СЏС†С–СЏ Р·Р°РІРµСЂС€РµРЅР°. Р—РІС–С‚ Р·Р±РµСЂРµР¶РµРЅРѕ Сѓ simulation_report.md")

if __name__ == '__main__':
    run_simulation()


--- Output 47 ---
Exit code: 1
Wall time: 2.8 seconds
Output:
Traceback (most recent call last):
  File "<stdin>", line 17, in <module>
TypeError: tuple indices must be integers or slices, not str


--- Output 48 ---
Exit code: 1
Wall time: 2.8 seconds
Output:
<class 'tuple'>
Traceback (most recent call last):
  File "<stdin>", line 5, in <module>
  File "E:\IDS_ML_Analyzer\.venv\Lib\site-packages\sklearn\base.py", line 402, in __repr__
    repr_ = pp.pformat(self)
  File "D:\Program Files\Python\Lib\pprint.py", line 161, in pformat
    self._format(object, sio, 0, 0, {}, 0)
    ~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "D:\Program Files\Python\Lib\pprint.py", line 178, in _format
    rep = self._repr(object, context, level)
  File "D:\Program Files\Python\Lib\pprint.py", line 458, in _repr
    repr, readable, recursive = self.format(object, context.copy(),
                                ~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^
                                            self._depth, level)
                                            ^^^^^^^^^^^^^^^^^^^
  File "E:\IDS_ML_Analyzer\.venv\Lib\site-packages\sklearn\utils\_pprint.py", line 191, in format
    return _safe_repr(
        object, context, maxlevels, level, changed_only=self._changed_only
    )
  File "E:\IDS_ML_Analyzer\.venv\Lib\site-packages\sklearn\utils\_pprint.py", line 440, in _safe_repr
    params = _changed_params(object)
  File "E:\IDS_ML_Analyzer\.venv\Lib\site-packages\sklearn\utils\_pprint.py", line 95, in _changed_params
    params = estimator.get_params(deep=False)
  File "E:\IDS_ML_Analyzer\.venv\Lib\site-packages\sklearn\base.py", line 257, in get_params
    value = getattr(self, key)
AttributeError: 'TwoStageModel' object has no attribute 'stage2_balance'


--- Output 49 ---
Exit code: 0
Wall time: 0.3 seconds
Output:
        """
        Р—Р±РµСЂРµР¶РµРЅРЅСЏ РјРѕРґРµР»С– С‚Р° РїСЂРµРїСЂРѕС†РµСЃРѕСЂР° Сѓ С„Р°Р№Р».
        
        Args:
            filename: Р†Рј'СЏ С„Р°Р№Р»Сѓ (Р· СЂРѕР·С€РёСЂРµРЅРЅСЏРј .joblib)
            preprocessor: РџСЂРµРїСЂРѕС†РµСЃРѕСЂ РґР»СЏ Р·Р±РµСЂРµР¶РµРЅРЅСЏ СЂР°Р·РѕРј Р· РјРѕРґРµР»Р»СЋ
            metadata: Р”РѕРґР°С‚РєРѕРІС– РјРµС‚Р°РґР°РЅС– РїСЂРѕ РјРѕРґРµР»СЊ (С‚РёРї, СЃСѓРјС–СЃРЅС–СЃС‚СЊ, С‚РѕС‰Рѕ)
            
        Returns:
            РЁР»СЏС… РґРѕ Р·Р±РµСЂРµР¶РµРЅРѕРіРѕ С„Р°Р№Р»Сѓ
        """
        if self.model is None:
            raise RuntimeError("РќРµРјР°С” РјРѕРґРµР»С– РґР»СЏ Р·Р±РµСЂРµР¶РµРЅРЅСЏ")
        
        # Р—Р±РµСЂС–РіР°С”РјРѕ СЏРє Р±Р°РЅРґР»
        bundle = {
            'model': self.model,
            'algorithm_name': self.algorithm_name,
            'preprocessor': preprocessor,
            'metadata': metadata or {}
        }
        
        # Add Isolation Forest specific data
        if self._is_isolation_forest():
            if hasattr(self, 'if_threshold_') and self.if_threshold_ is not None:
                bundle['if_threshold'] = self.if_threshold_
                logger.info(f"[ModelEngine] Saving IF threshold: {self.if_threshold_:.4f}")
                bundle['if_threshold_mode'] = getattr(self, 'if_threshold_mode_', 'decision_zero')
            if hasattr(self, 'if_score_stats_'):
                bundle['if_score_stats'] = self.if_score_stats_
        
        path = self.models_dir / filename
        joblib.dump(bundle, path)
        
        logger.info(f"РњРѕРґРµР»СЊ Р·Р±РµСЂРµР¶РµРЅРѕ: {path}")
        return path

    def load_model(self, filename: str) -> tuple[Any, Optional[Any], Optional[dict]]:
        """
        Р—Р°РІР°РЅС‚Р°Р¶РµРЅРЅСЏ РјРѕРґРµР»С– Р· С„Р°Р№Р»Сѓ.
        
        Args:
            filename: Р†Рј'СЏ С„Р°Р№Р»Сѓ
            
        Returns:
            Tuple (РјРѕРґРµР»СЊ, РїСЂРµРїСЂРѕС†РµСЃРѕСЂ, metadata) Р°Р±Рѕ (РјРѕРґРµР»СЊ, None, None) РґР»СЏ СЃС‚Р°СЂРёС… С„Р°Р№Р»С–РІ
            
        Raises:
            FileNotFoundError: Р¤Р°Р№Р» РЅРµ Р·РЅР°Р№РґРµРЅРѕ
        """
        path = self.models_dir / filename
        
        if not path.exists():
            raise FileNotFoundError(f"Р¤Р°Р№Р» РјРѕРґРµР»С– РЅРµ Р·РЅР°Р№РґРµРЅРѕ: {path}")
        
        loaded = joblib.load(path)
        
        # РџС–РґС‚СЂРёРјРєР° СЃС‚Р°СЂРѕРіРѕ С„РѕСЂРјР°С‚Сѓ (С‚С–Р»СЊРєРё РјРѕРґРµР»СЊ)
        if isinstance(loaded, dict) and 'model' in loaded:
            self.model = loaded['model']
            self.algorithm_name = loaded.get('algorithm_name')
            preprocessor = loaded.get('preprocessor')
            metadata = loaded.get('metadata', {})
            if self.algorithm_name is None and isinstance(metadata, dict):
                self.algorithm_name = metadata.get('algorithm')
            
            # Restore Isolation Forest specific data
            if self._is_isolation_forest():
                if 'if_threshold' in loaded:
                    loaded_mode = loaded.get('if_threshold_mode')
                    loaded_threshold = float(loaded['if_threshold'])

                    # Backward compatibility:
                    # legacy models stored percentile-based threshold for decision_function.
                    # That threshold was unstable, so we normalize to canonical 0.0 boundary.
                    if loaded_mode is None:
                        self.if_threshold_ = 0.0
                        self.if_threshold_mode_ = "decision_zero_legacy_normalized"
                        logger.warning(
                            "[ModelEngine] Legacy IF threshold detected and normalized to 0.0 "
                            f"(legacy value={loaded_threshold:.4f})"
                        )
                    else:
                        self.if_threshold_ = float(loaded_threshold)
                        self.if_threshold_mode_ = loaded_mode
                        logger.info(
                            f"[ModelEngine] Restored IF threshold: {self.if_threshold_:.4f} "
                            f"(mode={self.if_threshold_mode_})"
                        )
                else:
                    self.if_threshold_ = 0.0
                    self.if_threshold_mode_ = "decision_zero_default"
                    logger.warning("[ModelEngine] IF model loaded without threshold, using default 0.0")
                if 'if_score_stats' in loaded:
                    self.if_score_stats_ = loaded['if_score_stats']
        else:
            # РЎС‚Р°СЂРёР№ С„РѕСЂРјР°С‚ вЂ” С‚С–Р»СЊРєРё РјРѕРґРµР»СЊ
            self.model = loaded
            preprocessor = None
            metadata = None
        
        logger.info(f"РњРѕРґРµР»СЊ Р·Р°РІР°РЅС‚Р°Р¶РµРЅР°: {path}")
        
        return self.model, preprocessor, metadata

    def delete_model(self, filename: str) -> bool:
        """Р’РёРґР°Р»РµРЅРЅСЏ С„Р°Р№Р»Сѓ РјРѕРґРµР»С–."""
        path = self.models_dir / filename
        if path.exists():
            try:
                os.remove(path)
                logger.info(f"РњРѕРґРµР»СЊ РІРёРґР°Р»РµРЅРѕ: {filename}")
                return True
            except Exception as e:
                logger.error(f"РџРѕРјРёР»РєР° РІРёРґР°Р»РµРЅРЅСЏ: {e}")
                return False
        return False

    def rename_model(self, old_filename: str, new_filename: str) -> bool:
        """РџРµСЂРµР№РјРµРЅСѓРІР°РЅРЅСЏ С„Р°Р№Р»Сѓ РјРѕРґРµР»С–."""
        if not new_filename.endswith('.joblib'):
            new_filename += '.joblib'
            
        old_path = self.models_dir / old_filename
        new_path = self.models_dir / new_filename
        
        if not old_path.exists():
            return False
            
        if new_path.exists():
            raise FileExistsError(f"Р¤Р°Р№Р» {new_filename} РІР¶Рµ С–СЃРЅСѓС”")


--- Output 50 ---
Exit code: 0
Wall time: 3.4 seconds
Output:
MODEL ids_model_random_forest_194247_25032026.joblib
MODEL_CLASS TwoStageModel
ALGO Random Forest
TRAINED_FAMILIES ['CIC-IDS']
DEFAULT_THR 0.3
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_DDoS_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 88.4% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_PortScan_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 93.0% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_WebAttack_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 88.4% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 95.3% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 95.3% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Neptune_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Satan_Probe_15pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Smurf_DoS_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Exploits_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Fuzzers_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Generic_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Pipeline Complete. Output shape: (1000, 45)
                                        file    family    n  actual_attacks  pred_attacks   TPR      FPR
          CIC-IDS2017_DDoS_50pct_anomaly.csv   CIC-IDS 1000             500             0 0.000 0.000000
      CIC-IDS2017_PortScan_30pct_anomaly.csv   CIC-IDS 1000             300             1 0.000 0.001429
     CIC-IDS2017_WebAttack_10pct_anomaly.csv   CIC-IDS 1000             100             0 0.000 0.000000
CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv   CIC-IDS 1000             200             0 0.000 0.000000
CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv   CIC-IDS 1000             300             0 0.000 0.000000
   NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv   NSL-KDD  500             100             0 0.000 0.000000
       NSL-KDD_Neptune_DoS_20pct_anomaly.csv   NSL-KDD 1000             200             0 0.000 0.000000
       NSL-KDD_Satan_Probe_15pct_anomaly.csv   NSL-KDD 1000             150             0 0.000 0.000000
         NSL-KDD_Smurf_DoS_10pct_anomaly.csv   NSL-KDD  500              50             0 0.000 0.000000
             UNSW_NB15_DoS_20pct_anomaly.csv UNSW-NB15 1000             200             1 0.005 0.000000
        UNSW_NB15_Exploits_30pct_anomaly.csv UNSW-NB15 1000             300             0 0.000 0.000000
         UNSW_NB15_Fuzzers_10pct_anomaly.csv UNSW-NB15 1000             100             0 0.000 0.000000
         UNSW_NB15_Generic_50pct_anomaly.csv UNSW-NB15 1000             500             0 0.000 0.000000
weighted_TPR 0.0004166666666666667
weighted_FPR 0.00011904761904761905
[Preprocessor] 56 columns filled with 0: ['Fwd Byts/b Avg', 'Pkt Len Mean', 'Bwd Byts/b Avg', 'active_mean', 'Fwd Blk Rate Avg']...
[Preprocessor] 56 columns filled with 0: ['Fwd Byts/b Avg', 'Pkt Len Mean', 'Bwd Byts/b Avg', 'active_mean', 'Fwd Blk Rate Avg']...
[Preprocessor] 56 columns filled with 0: ['Fwd Byts/b Avg', 'Pkt Len Mean', 'Bwd Byts/b Avg', 'active_mean', 'Fwd Blk Rate Avg']...
[Preprocessor] 56 columns filled with 0: ['Fwd Byts/b Avg', 'Pkt Len Mean', 'Bwd Byts/b Avg', 'active_mean', 'Fwd Blk Rate Avg']...
[Preprocessor] 56 columns filled with 0: ['Fwd Byts/b Avg', 'Pkt Len Mean', 'Bwd Byts/b Avg', 'active_mean', 'Fwd Blk Rate Avg']...
[Preprocessor] 92 columns filled with 0: ['Fwd Byts/b Avg', 'Src Port', 'idle_min', 'Subflow Fwd Pkts', 'Fwd Header Length']...
[Preprocessor] 92 columns filled with 0: ['Fwd Byts/b Avg', 'Src Port', 'idle_min', 'Subflow Fwd Pkts', 'Fwd Header Length']...
[Preprocessor] 92 columns filled with 0: ['Fwd Byts/b Avg', 'Src Port', 'idle_min', 'Subflow Fwd Pkts', 'Fwd Header Length']...
[Preprocessor] 92 columns filled with 0: ['Fwd Byts/b Avg', 'Src Port', 'idle_min', 'Subflow Fwd Pkts', 'Fwd Header Length']...
[Preprocessor] 82 columns filled with 0: ['Fwd Byts/b Avg', 'Src Port', 'idle_min', 'Subflow Fwd Pkts', 'Fwd Header Length']...
[Preprocessor] 82 columns filled with 0: ['Fwd Byts/b Avg', 'Src Port', 'idle_min', 'Subflow Fwd Pkts', 'Fwd Header Length']...
[Preprocessor] 82 columns filled with 0: ['Fwd Byts/b Avg', 'Src Port', 'idle_min', 'Subflow Fwd Pkts', 'Fwd Header Length']...
[Preprocessor] 82 columns filled with 0: ['Fwd Byts/b Avg', 'Src Port', 'idle_min', 'Subflow Fwd Pkts', 'Fwd Header Length']...


--- Output 51 ---
Exit code: 0
Wall time: 5.4 seconds
Output:
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_DDoS_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 90.7% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_PortScan_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 95.3% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2017_WebAttack_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 90.7% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_FTP-BruteForce_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 97.7% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\CIC-IDS2018_SSH-Bruteforce_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: CIC-IDS
[LOG] Feature coverage (CIC-IDS): 97.7% non-zero schema features
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_GuessPasswd_R2L_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Neptune_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Satan_Probe_15pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Pipeline Complete. Output shape: (1000, 44)
[LOG] Loading file: datasets\TEST_DATA\NSL-KDD_Smurf_DoS_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: NSL-KDD
[LOG] Pipeline Complete. Output shape: (500, 44)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_DoS_20pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Exploits_30pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Fuzzers_10pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Pipeline Complete. Output shape: (1000, 45)
[LOG] Loading file: datasets\TEST_DATA\UNSW_NB15_Generic_50pct_anomaly.csv
[LOG] Starting Unified Pipeline...
[LOG] Detected Dataset Type: UNSW-NB15
[LOG] Pipeline Complete. Output shape: (1000, 45)
thr=0.03 wTPR=0.9381 wFPR=0.9936
thr=0.05 wTPR=0.8717 wFPR=0.9706
thr=0.07 wTPR=0.7701 wFPR=0.9197
thr=0.10 wTPR=0.4724 wFPR=0.7567
thr=0.12 wTPR=0.3880 wFPR=0.6027
thr=0.15 wTPR=0.3297 wFPR=0.4382
thr=0.20 wTPR=0.1673 wFPR=0.2207
thr=0.25 wTPR=0.0116 wFPR=0.0204
thr=0.30 wTPR=0.0004 wFPR=0.0000
[Preprocessor] 56 columns filled with 0: ['Fwd Header Length.1', 'Subflow Bwd Byts', 'Bwd Header Length', 'Pkt Len Min', 'Fwd Pkts/b Avg']...
[Preprocessor] 56 columns filled with 0: ['Fwd Header Length.1', 'Subflow Bwd Byts', 'Bwd Header Length', 'Pkt Len Min', 'Fwd Pkts/b Avg']...
[Preprocessor] 56 columns filled with 0: ['Fwd Header Length.1', 'Subflow Bwd Byts', 'Bwd Header Length', 'Pkt Len Min', 'Fwd Pkts/b Avg']...
[Preprocessor] 56 columns filled with 0: ['Fwd Header Length.1', 'Subflow Bwd Byts', 'Bwd Header Length', 'Pkt Len Min', 'Fwd Pkts/b Avg']...
[Preprocessor] 56 columns filled with 0: ['Fwd Header Length.1', 'Subflow Bwd Byts', 'Bwd Header Length', 'Pkt Len Min', 'Fwd Pkts/b Avg']...
[Preprocessor] 92 columns filled with 0: ['Pkt Len Min', 'bwd_iat_min', 'fwd_packet_length_max', 'Init Bwd Win Byts', 'PSH Flag Cnt']...
[Preprocessor] 92 columns filled with 0: ['Pkt Len Min', 'bwd_iat_min', 'fwd_packet_length_max', 'Init Bwd Win Byts', 'PSH Flag Cnt']...
[Preprocessor] 92 columns filled with 0: ['Pkt Len Min', 'bwd_iat_min', 'fwd_packet_length_max', 'Init Bwd Win Byts', 'PSH Flag Cnt']...
[Preprocessor] 92 columns filled with 0: ['Pkt Len Min', 'bwd_iat_min', 'fwd_packet_length_max', 'Init Bwd Win Byts', 'PSH Flag Cnt']...
[Preprocessor] 82 columns filled with 0: ['Pkt Len Min', 'bwd_iat_min', 'Init Bwd Win Byts', 'PSH Flag Cnt', 'Pkt Len Std']...
[Preprocessor] 82 columns filled with 0: ['Pkt Len Min', 'bwd_iat_min', 'Init Bwd Win Byts', 'PSH Flag Cnt', 'Pkt Len Std']...
[Preprocessor] 82 columns filled with 0: ['Pkt Len Min', 'bwd_iat_min', 'Init Bwd Win Byts', 'PSH Flag Cnt', 'Pkt Len Std']...
[Preprocessor] 82 columns filled with 0: ['Pkt Len Min', 'bwd_iat_min', 'Init Bwd Win Byts', 'PSH Flag Cnt', 'Pkt Len Std']...


--- Output 52 ---
Exit code: 0
Wall time: 0.3 seconds
Output:
        stat = model_path.stat()
        manifest = load_model_manifest(str(model_path), stat.st_mtime, stat.st_size)

        # Heuristic fallback for older models/files without metadata.
        lowered_name = model_name.lower()
        if not manifest.get('algorithm'):
            if 'isolation' in lowered_name:
                manifest['algorithm'] = 'Isolation Forest'
                manifest['is_isolation_forest'] = True
                manifest['compatible_file_types'] = sorted(SUPPORTED_SCAN_EXTENSIONS)
            elif 'two_stage' in lowered_name:
                manifest['two_stage_mode'] = True

        return manifest

    def resolve_auto_model(file_target: str | Path) -> tuple[str | None, str]:
        file_path = Path(file_target)
        normalized_ext = file_path.suffix.lower() if file_path.suffix else str(file_target).lower()
        model_names = [f.name for f in model_files]
        if not model_names:
            return None, 'none'

        file_family = _infer_dataset_family_name(file_path.name)
        file_family_confidence = 0.55 if file_family else 0.0
        file_family_ambiguous = False
        if file_path.exists():
            file_stat = file_path.stat()
            family_info = detect_scan_file_family_info(
                str(file_path),
                file_stat.st_mtime,
                file_stat.st_size
            )
            detected_family = str(family_info.get('family', ''))
            if detected_family:
                file_family = detected_family
            file_family_confidence = float(family_info.get('confidence', file_family_confidence))
            file_family_ambiguous = bool(family_info.get('ambiguous', False))

        scored: list[tuple[float, str, str]] = []
        for recency_idx, candidate in enumerate(model_names):
            manifest = get_manifest_for_model(candidate)
            compatible_types = _normalize_compatible_types(manifest.get('compatible_file_types'))
            pcap_tabular_fallback = (
                normalized_ext in PCAP_EXTENSIONS
                and bool(set(compatible_types) & TABULAR_EXTENSIONS)
            )
            if normalized_ext not in compatible_types and not pcap_tabular_fallback:
                continue

            trained_families = set(manifest.get('trained_families', []))
            if not trained_families:
                family_from_name = _infer_dataset_family_name(candidate)
                if family_from_name:
                    trained_families.add(family_from_name)

            score = 0.0
            reason = 'compatible'
            is_if = bool(manifest.get('is_isolation_forest'))
            is_two_stage = bool(manifest.get('two_stage_mode'))

            if normalized_ext in PCAP_EXTENSIONS:
                if is_if:
                    score += 1000.0
                    reason = 'pcap_if'
                else:
                    # Не блокуємо табличні моделі для PCAP у ручному/авто-режимі:
                    # даємо значно нижчий пріоритет, але залишаємо можливість запуску.
                    score -= 80.0
                    reason = 'pcap_tabular_fallback'
            else:
                if is_two_stage:
                    score += 160.0
                    reason = 'tabular_two_stage'
                if is_if:
                    score -= 250.0

                algorithm_meta = str(manifest.get('algorithm', '')).lower()
                if 'random forest' in algorithm_meta:
                    score += 40.0
                elif 'xgboost' in algorithm_meta:
                    score += 35.0
                elif 'logistic' in algorithm_meta:
                    score += 15.0

                family_reliable = bool(file_family) and (not file_family_ambiguous) and file_family_confidence >= 0.60
                if family_reliable:
                    if file_family in trained_families:
                        score += 260.0 * max(0.35, min(1.0, file_family_confidence))
                        reason = 'family_match_two_stage' if is_two_stage else 'family_match'
                    elif trained_families:
                        score -= 90.0
                elif file_family and trained_families:
                    if file_family in trained_families:
                        score += 35.0
                    reason = 'family_ambiguous'

                score += min(len(trained_families), 3) * 20.0

            # Легкий бонус за свіжість (список вже відсортовано за mtime desc)
            score += max(0.0, (len(model_names) - recency_idx) * 0.01)
            scored.append((score, candidate, reason))

        if not scored:
            return None, 'none'

        scored.sort(key=lambda item: item[0], reverse=True)
        best_score, best_candidate, best_reason = scored[0]
        return best_candidate, best_reason

    if not model_files:
        st.markdown("""
        <div class="warning-box">
            <b>Спочатку створіть модель</b><br><br>
            Для сканування трафіку необхідна навчена модель.
            Перейдіть до розділу <b>Тренування</b> та створіть модель на основі датасету.
        </div>
        """, unsafe_allow_html=True)
    else:
        st.markdown("""
        <div class="section-card">
            <div class="section-title">Налаштування сканування</div>
        </div>
        """, unsafe_allow_html=True)

        col1, col2 = st.columns([2, 1])

        with col1:
            auto_select_model = st.checkbox(
                "Автовибір найкращої моделі",
                value=True,
                help="Система автоматично обере: IF для PCAP, RF/XGBoost/Two-Stage для CSV/NF"
            )

            if auto_select_model:
                st.info("Система обере оптимальну модель для вашого файлу")
                selected_model = None
            else:
                selected_model = st.selectbox(
                    "Модель для аналізу:",
                    options=[f.name for f in model_files],
                    help="Оберіть модель, яку ви створили раніше"
                )

        with col2:
            gemini_key = services['settings'].get("gemini_api_key", "")
            if gemini_key:
                st.success("Gemini API підключено")
            else:
                st.warning("Gemini API не налаштовано")
                st.caption("Додайте ключ на головній сторінці для AI-аналізу")

        st.markdown("<br>", unsafe_allow_html=True)

        st.markdown("""
        <div class="section-card">
            <div class="section-title">Вибір даних для аналізу</div>
            <p class="text-muted">
                Оберіть файл з бібліотеки або завантажте новий (CSV, NF, PCAP)
            </p>
        </div>
        """, unsafe_allow_html=True)

        user_dir = ROOT_DIR / 'datasets' / 'User_Uploads'
        scans_dir = ROOT_DIR / 'datasets' / 'Processed_Scans'
        # Гарантуємо існування директорій (glob() падає якщо директорії немає)
        user_dir.mkdir(parents=True, exist_ok=True)
        scans_dir.mkdir(parents=True, exist_ok=True)
        processed_has_files = any(
            f.suffix.lower() in SUPPORTED_SCAN_EXTENSIONS
            for f in scans_dir.glob('*.*')
        )


--- Output 53 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
            report['checks']['quality_score'] = int(max(0, min(100, quality_score)))
            report['score'] = int(max(0, min(100, (format_score * 0.55) + (quality_score * 0.45))))
            return report

        file_family_info = detect_scan_file_family_info(file_path, file_mtime, file_size)
        file_family = str(file_family_info.get('family', ''))
        family_confidence = float(file_family_info.get('confidence', 0.0))
        family_ambiguous = bool(file_family_info.get('ambiguous', False))
        report['checks']['file_family'] = file_family
        report['checks']['file_family_confidence'] = family_confidence
        report['checks']['file_family_ambiguous'] = family_ambiguous

        schema_mode = str(metadata.get('schema_mode', 'unified')).strip().lower() if metadata else 'unified'
        report['checks']['schema_mode'] = schema_mode

        training_files = metadata.get('training_files', []) if metadata else []
        if not isinstance(training_files, list):
            training_files = []
        trained_families_meta = metadata.get('trained_families', []) if isinstance(metadata, dict) else []
        if not isinstance(trained_families_meta, list):
            trained_families_meta = []
        trained_families = {
            fam for fam in (_infer_dataset_family_name(Path(p).name) for p in training_files) if fam
        }
        trained_families.update({str(f).strip() for f in trained_families_meta if str(f).strip()})
        report['checks']['trained_families'] = sorted(trained_families)

        family_reliable = bool(file_family) and (not family_ambiguous) and family_confidence >= 0.60
        if family_reliable and trained_families and file_family not in trained_families:
            report['issues'].append(
                f"Р¤Р°Р№Р» СЃС…РѕР¶РёР№ РЅР° СЃС–РјРµР№СЃС‚РІРѕ {file_family}, Р°Р»Рµ РјРѕРґРµР»СЊ С‚СЂРµРЅРѕРІР°РЅР° РЅР°: {', '.join(sorted(trained_families))}. "
                "РњРѕР¶Р»РёРІР° РІС‚СЂР°С‚Р° СЏРєРѕСЃС‚С– РґРµС‚РµРєС†С–С— (OOD)."
            )
            quality_score -= 25 if family_confidence >= 0.75 else 15
            report['checks']['ood_family_mismatch'] = True
            if schema_mode == 'family':
                report['issues'].append(
                    "РњРѕРґРµР»СЊ Р·Р±РµСЂС–РіР°С” СЃС–РјРµР№РЅС– РѕР·РЅР°РєРё, С‚РѕР¶ РґР»СЏ С–РЅС€РѕРіРѕ СЃС–РјРµР№СЃС‚РІР° РІРѕРЅР° РЅРµ РїС–РґС…РѕРґРёС‚СЊ. "
                    "РћР±РµСЂС–С‚СЊ РјРѕРґРµР»СЊ, РЅР°С‚СЂРµРЅРѕРІР°РЅСѓ РЅР° С†СЊРѕРјСѓ СЃС–РјРµР№СЃС‚РІС–."
                )
                report['blocking'] = True
                format_score -= 15

        if family_ambiguous:
            report['issues'].append("РЎС–РјРµР№СЃС‚РІРѕ С„Р°Р№Р»Сѓ РІРёР·РЅР°С‡РµРЅРѕ РЅРµРѕРґРЅРѕР·РЅР°С‡РЅРѕ. РђРІС‚РѕРІРёР±С–СЂ РјРѕРґРµР»С– РјРµРЅС€ РЅР°РґС–Р№РЅРёР№.")
            quality_score -= 8

        schema_mode = str(metadata.get('schema_mode', 'unified')).strip().lower() if isinstance(metadata, dict) else 'unified'
        align_to_schema = schema_mode != 'family'
        loader = DataLoader()
        df_preview = loader.load_file(file_path, max_rows=3000, align_to_schema=align_to_schema)
        report['checks']['preview_rows'] = int(len(df_preview))
        # в”Ђв”Ђ Check if file has attack labels (before dropping) в”Ђв”Ђ
        file_has_attack_labels = False
        file_attack_label_count = 0
        label_col = None
        for c in df_preview.columns:
            if c.lower() in ('label', 'attack_cat', 'class'):
                label_col = c
                break
        if label_col is not None:
            non_benign = df_preview[label_col].astype(str).str.strip().str.lower()
            benign_variants = {'benign', 'normal', 'none', '', 'nan', '0', '0.0'}
            attack_mask = ~non_benign.isin(benign_variants)
            file_attack_label_count = int(attack_mask.sum())
            file_has_attack_labels = file_attack_label_count > 0
            report['checks']['file_has_labels'] = True
            report['checks']['file_attack_label_count'] = file_attack_label_count
            df_preview = df_preview.drop(columns=[label_col])
        else:
            report['checks']['file_has_labels'] = False
            report['checks']['file_attack_label_count'] = 0

        required_features = set(preprocessor.feature_columns)
        available_features = set(df_preview.columns)
        missing_features = required_features - available_features
        coverage = 1.0 - (len(missing_features) / len(required_features)) if required_features else 0.0

        report['checks']['feature_coverage'] = float(coverage)
        report['checks']['required_features'] = len(required_features)
        report['checks']['available_features'] = len(available_features)
        report['checks']['missing_features'] = len(missing_features)

        if coverage < 0.8:
            report['issues'].append(f"РќРёР·СЊРєР° СЃСѓРјС–СЃРЅС–СЃС‚СЊ РѕР·РЅР°Рє: {coverage:.0%}.")
            format_score -= 35
        elif coverage < 0.95:
            report['issues'].append(f"РќРµРїРѕРІРЅР° СЃСѓРјС–СЃРЅС–СЃС‚СЊ РѕР·РЅР°Рє: {coverage:.0%}.")
            format_score -= 12

        X_preview = preprocessor.transform(df_preview)
        drift_report = _evaluate_distribution_drift(
            (metadata or {}).get('training_distribution_profile', {}),
            X_preview,
            list(getattr(preprocessor, 'feature_columns', [])),
        )
        if drift_report.get('available'):
            drift_score = float(drift_report.get('drift_score', 0.0))
            report['checks']['distribution_drift_score'] = drift_score
            report['checks']['distribution_profile_coverage'] = float(drift_report.get('feature_coverage', 0.0))
            if drift_report.get('feature_coverage', 0.0) < 0.35:
                report['issues'].append("OOD-РїРµСЂРµРІС–СЂРєР° РјР°С” РЅРёР·СЊРєРµ РїРѕРєСЂРёС‚С‚СЏ РѕР·РЅР°Рє. РћС†С–РЅРєР° РЅР°РґС–Р№РЅРѕСЃС‚С– РѕР±РјРµР¶РµРЅР°.")
                quality_score -= 6
            elif drift_score >= 8.0:
                report['issues'].append("РЎРёР»СЊРЅРёР№ РґСЂРµР№С„ СЂРѕР·РїРѕРґС–Р»Сѓ РѕР·РЅР°Рє РІС–РґРЅРѕСЃРЅРѕ train. Р’РёСЃРѕРєРёР№ СЂРёР·РёРє OOD.")
                quality_score -= 30
                if bool(report['checks'].get('ood_family_mismatch')) or coverage < 0.90:
                    report['blocking'] = True
            elif drift_score >= 5.0:
                report['issues'].append("РџРѕРјС–С‚РЅРёР№ РґСЂРµР№С„ СЂРѕР·РїРѕРґС–Р»Сѓ РѕР·РЅР°Рє. РњРѕР¶Р»РёРІРµ Р·РЅРёР¶РµРЅРЅСЏ СЏРєРѕСЃС‚С– РґРµС‚РµРєС†С–С—.")
                quality_score -= 15
            elif drift_score >= 3.0:
                report['issues'].append("Р›РµРіРєРёР№ РґСЂРµР№С„ СЂРѕР·РїРѕРґС–Р»Сѓ РѕР·РЅР°Рє. Р РµРєРѕРјРµРЅРґРѕРІР°РЅРѕ РїРµСЂРµРІС–СЂРёС‚Рё С–РЅС†РёРґРµРЅС‚Рё РІСЂСѓС‡РЅСѓ.")
                quality_score -= 8
        if len(X_preview) > 0:
            if is_two_stage:
                two_stage_threshold = _clamp_two_stage_threshold(
                    float(
                        (metadata or {}).get(
                            'two_stage_threshold_default',
                            getattr(model, 'binary_threshold', DEFAULT_SENSITIVITY_THRESHOLD)
                        )
                    )
                )
                preds = model.predict(X_preview, threshold=two_stage_threshold)
                report['checks']['two_stage_threshold'] = two_stage_threshold
                report['checks']['two_stage_profile_default'] = _normalize_two_stage_profile(
                    (metadata or {}).get('two_stage_profile_default', DEFAULT_TWO_STAGE_PROFILE)
                )
                report['checks']['two_stage_threshold_strict'] = _clamp_two_stage_threshold(
                    float(
                        (metadata or {}).get(
                            'two_stage_threshold_strict',
                            _resolve_two_stage_profile_threshold(two_stage_threshold, "strict")
                        )
                    )
                )
                pred_attack = (np.asarray(preds) != 0).astype(int)
            elif is_if:
                preds = engine.predict(X_preview)
                pred_attack = (np.asarray(preds).astype(int) == 1).astype(int)
            else:
                preds = engine.predict(X_preview)
                pred_attack = (np.asarray(preds) != 0).astype(int)

            anomaly_rate = float(np.mean(pred_attack)) if len(pred_attack) > 0 else 0.0
            report['checks']['preview_anomaly_rate'] = anomaly_rate

            if file_ext in TABULAR_EXTENSIONS and is_if:
                report['issues'].append(
                    "Р”Р»СЏ CSV/NF Isolation Forest РјРѕР¶Рµ РґР°РІР°С‚Рё Р±С–Р»СЊС€Рµ FP; Р·Р°Р·РІРёС‡Р°Р№ С‚РѕС‡РЅС–С€Рµ РїСЂР°С†СЋС” Two-Stage/RF."
                )
                quality_score -= 15

            if file_ext in PCAP_EXTENSIONS and not is_if:
                report['issues'].append("PCAP-СЃРєР°РЅСѓРІР°РЅРЅСЏ Р±РµР· Isolation Forest РјР°С” РІРёСЃРѕРєРёР№ СЂРёР·РёРє РїСЂРѕРїСѓСЃРєСѓ Р°РЅРѕРјР°Р»С–Р№.")
                quality_score -= 40
                report['blocking'] = True

            if file_ext in PCAP_EXTENSIONS and is_if and anomaly_rate < 0.005:
                report['issues'].append(
                    "Р”СѓР¶Рµ РЅРёР·СЊРєР° С‡Р°СЃС‚РєР° Р°РЅРѕРјР°Р»С–Р№ Сѓ preview PCAP: РјРѕР¶Р»РёРІРёР№ СЂРёР·РёРє РїСЂРѕРїСѓСЃРєСѓ Р°С‚Р°Рє."
                )
                quality_score -= 20

            if file_ext in TABULAR_EXTENSIONS and anomaly_rate > 0.35:
                report['issues'].append("РќР°РґС‚Рѕ РІРёСЃРѕРєР° С‡Р°СЃС‚РєР° Р°РЅРѕРјР°Р»С–Р№ Сѓ preview: РјРѕР¶Р»РёРІРёР№ СЂРёР·РёРє FP.")
                quality_score -= 15

            # в”Ђв”Ђ Warning: model detects 0 anomalies on file with attack labels в”Ђв”Ђ
            if file_ext in TABULAR_EXTENSIONS and anomaly_rate < 0.001:
                if file_has_attack_labels:
                    pct = (file_attack_label_count / max(1, len(df_preview))) * 100
                    report['issues'].append(
                        f"Р¤Р°Р№Р» РјС–СЃС‚РёС‚СЊ {file_attack_label_count} РїРѕР·РЅР°С‡РµРЅРёС… Р°С‚Р°Рє ({pct:.0f}%), "
                        "Р°Р»Рµ РјРѕРґРµР»СЊ РЅРµ РІРёСЏРІРёР»Р° Р¶РѕРґРЅРѕС— Р°РЅРѕРјР°Р»С–С—. "
                        "Р™РјРѕРІС–СЂРЅРѕ, РґР°РЅС– РјР°СЋС‚СЊ С–РЅС€РёР№ СЂРѕР·РїРѕРґС–Р», РЅС–Р¶ С‚СЂРµРЅСѓРІР°Р»СЊРЅС–. "
                        "Р РµРєРѕРјРµРЅРґСѓС”С‚СЊСЃСЏ РїРµСЂРµРЅР°РІС‡РёС‚Рё РјРѕРґРµР»СЊ РЅР° РїРѕРґС–Р±РЅРёС… РґР°РЅРёС…."
                    )
                    quality_score -= 55
                    report['checks']['label_vs_model_mismatch'] = True
                    report['blocking'] = True
                else:
                    report['issues'].append(
                        "РњРѕРґРµР»СЊ РЅРµ РІРёСЏРІРёР»Р° Р°РЅРѕРјР°Р»С–Р№ Сѓ preview-РІРёР±С–СЂС†С–. "
                        "РЇРєС‰Рѕ РІРё РѕС‡С–РєСѓС”С‚Рµ Р°С‚Р°РєРё вЂ” РїРµСЂРµРІС–СЂС‚Рµ СЃСѓРјС–СЃРЅС–СЃС‚СЊ РґР°РЅРёС… Р· РјРѕРґРµР»Р»СЋ."
                    )
                    quality_score -= 10

            if is_if and metadata:
                if not bool(metadata.get('if_auto_calibration', False)):
                    report['issues'].append("Р”Р»СЏ IF РІРёРјРєРЅРµРЅРѕ Р°РІС‚Рѕ-РєР°Р»С–Р±СЂСѓРІР°РЅРЅСЏ РїРѕСЂРѕРіСѓ.")
                    quality_score -= 10

        if bool(report['checks'].get('ood_family_mismatch')) and coverage < 0.95:
            report['issues'].append("Р„ РѕР·РЅР°РєРё OOD: С– СЃС–РјРµР№СЃС‚РІРѕ, С– РѕР·РЅР°РєРё РІС–РґСЂС–Р·РЅСЏСЋС‚СЊСЃСЏ РІС–Рґ С‚СЂРµРЅСѓРІР°Р»СЊРЅРёС….")
            quality_score -= 10

        format_score = int(max(0, min(100, round(format_score))))
        quality_score = int(max(0, min(100, round(quality_score))))
        report['checks']['format_score'] = format_score
        report['checks']['quality_score'] = quality_score
        report['score'] = int(max(0, min(100, round((format_score * 0.55) + (quality_score * 0.45)))))

        if report['score'] >= 80:
            report['status'] = 'ready'
        elif report['score'] >= 60:
            report['status'] = 'caution'
        else:
            report['status'] = 'risk'



--- Output 54 ---
Exit code: 0
Wall time: 0.4 seconds
Output:

Name                                             Length LastWriteTime      
----                                             ------ -------------      
ids_model_random_forest_194247_25032026.joblib 44055557 25.03.2026 19:44:30
ids_model_random_forest_210257_19032026.joblib  2570051 19.03.2026 21:03:27
ids_model_random_forest_091303_19032026.joblib 43606927 19.03.2026 9:14:19 




--- Output 55 ---
Exit code: 0
Wall time: 0.4 seconds
Output:
                    _normalize_compatible_types(preview_manifest.get('compatible_file_types', sorted(TABULAR_EXTENSIONS)))
                )
                if file_ext_preview not in preview_compatible:
                    allow_preview_pcap_fallback = (
                        file_ext_preview in PCAP_EXTENSIONS
                        and not bool(preview_manifest.get('is_isolation_forest'))
                    )
                    if allow_preview_pcap_fallback:
                        scan_blocked = False
                        st.warning(
                            f"Модель `{preview_model_name}` запускається для `{file_ext_preview}` у fallback-режимі. "
                            "Рекомендується додатково перевірити результат еталонним IF-сканом."
                        )
                    else:
                        fallback_preview_model, _ = resolve_auto_model(dataset_path)
                        if fallback_preview_model and fallback_preview_model != preview_model_name:
                            st.warning(
                                f"Несумісна пара `{preview_model_name}` + `{file_ext_preview}`. "
                                f"Автоматично обрано сумісну модель `{fallback_preview_model}`."
                            )
                            preview_model_name = fallback_preview_model
                            preview_manifest = get_manifest_for_model(preview_model_name)
                            scan_blocked = False
                        else:
                            scan_blocked = True
                            st.error(
                                f"Для файлу `{file_ext_preview}` не знайдено жодної сумісної моделі. "
                                "Навчіть модель для цього типу даних у вкладці Тренування."
                            )

            if preview_model_name and dataset_path.exists() and not scan_blocked:
                preview_model_path = model_file_map.get(preview_model_name)
                if preview_model_path and preview_model_path.exists():
                    model_stat = preview_model_path.stat()
                    file_stat = dataset_path.stat()
                    readiness = compute_scan_readiness_diagnostics(
                        preview_model_name,
                        model_stat.st_mtime,
                        model_stat.st_size,
                        str(dataset_path),
                        file_stat.st_mtime,
                        file_stat.st_size
                    )

                    st.markdown("""
                    <div class="section-card">
                        <div class="section-title">Самодіагностика перед запуском</div>
                    </div>
                    """, unsafe_allow_html=True)

                    status_map = {
                        'ready': ("Готова", "Низький"),
                        'caution': ("З обмеженнями", "Середній"),
                        'risk': ("Ризик", "Високий"),
                    }
                    status_label, risk_label = status_map.get(readiness.get('status', 'risk'), ("Ризик", "Високий"))

                    checks = readiness.get('checks', {})
                    feature_coverage = float(checks.get('feature_coverage', 0.0))
                    d1, d2, d3, d4 = st.columns(4)
                    d1.metric("Стан", status_label)
                    d2.metric("Сумісність ознак", f"{feature_coverage:.0%}")
                    d3.metric("Сумісність формату", f"{int(checks.get('format_score', readiness.get('score', 0)))}/100")
                    d4.metric("Прогноз якості детекції", f"{int(checks.get('quality_score', readiness.get('score', 0)))}/100")

                    st.caption(
                        "Сумісність формату = чи модель технічно може обробити файл (формат, ознаки, препроцесор). "
                        "Прогноз якості детекції = оцінка ризику FP/FN та OOD на тестовій preview-вибірці. "
                        "Це орієнтир, а не 100% гарантія якості в кожному окремому файлі. "
                        f"Режим моделі: {str(readiness.get('model_mode', 'Unknown'))}. "
                        f"Оцінка ризику: {risk_label}."
                    )

                    if 'feature_coverage' in checks:
                        st.write(f"• Відсутніх ознак: **{checks.get('missing_features', 0)}**")
                    if 'preview_anomaly_rate' in checks:
                        preview_rate = float(checks.get('preview_anomaly_rate', 0.0))
                        preview_rows = int(checks.get('preview_rows', 0))
                        if preview_rate == 0.0:
                            preview_rate_text = "0.00%"
                        elif preview_rate < 0.001:
                            preview_rate_text = f"{preview_rate * 100:.3f}%"
                        else:
                            preview_rate_text = f"{preview_rate * 100:.2f}%"
                        rows_text = f" на вибірці {preview_rows:,} рядків" if preview_rows > 0 else ""
                        st.write(f"• Оціночна частка аномалій{rows_text}: **{preview_rate_text}**")
                    if 'distribution_drift_score' in checks:
                        drift_score = float(checks.get('distribution_drift_score', 0.0))
                        drift_cov = float(checks.get('distribution_profile_coverage', 0.0))
                        st.write(
                            f"• OOD-дрейф розподілу: **{drift_score:.2f}** "
                            f"(покриття профілю: {drift_cov:.0%})"
                        )
                    if 'two_stage_threshold' in checks:
                        profile_default_preview = _normalize_two_stage_profile(
                            checks.get('two_stage_profile_default', DEFAULT_TWO_STAGE_PROFILE)
                        )
                        profile_default_label = TWO_STAGE_PROFILE_RULES[profile_default_preview]['label']
                        st.write(
                            f"• Рекомендований профіль моделі: **{profile_default_label}**. "
                            "Поріг для цього профілю береться з навченої моделі."
                        )

                    issues = readiness.get('issues', [])
                    if issues:
                        for issue in issues[:4]:
                            st.warning(issue)

                    if readiness.get('blocking', False):
                        fallback_preview_model, _ = resolve_auto_model(dataset_path)
                        if fallback_preview_model and fallback_preview_model != preview_model_name:
                            st.warning(
                                f"Самодіагностика виявила ризик для `{preview_model_name}`. "
                                f"Для запуску буде використано `{fallback_preview_model}`."
                            )
                            preview_model_name = fallback_preview_model
                            preview_manifest = get_manifest_for_model(preview_model_name)
                            scan_blocked = False
                        else:
                            st.warning(
                                "Самодіагностика виявила високий ризик якості детекції. "
                                "Сканування буде виконано, але результат позначиться як низьконадійний."
                            )

            if st.button(
                "Розпочати аналіз",
                type="primary",
                width="stretch",
                disabled=scan_blocked or bool(st.session_state.get('scan_in_progress', False))
            ):
                # Очищення пам'яті від попереднього аналізу
                clear_session_memory()
                st.session_state['scan_in_progress'] = True

                progress = st.progress(0)
                status = st.empty()

                try:
                    st.info("Завантаження даних...")
                    progress.progress(15)

                    print(f"[LOG] Scanning: Loading file {dataset_path.name}...") 

                    st.info("Завантаження моделі...")
                    progress.progress(30)

                    # --- АВТОВБІР МОДЕЛІ ---
                    # --- AUTO MODEL RESOLUTION ---
                    if auto_select_model or selected_model is None:
                        file_ext = dataset_path.suffix.lower()
                        selected_model, auto_reason = resolve_auto_model(dataset_path)

                        if selected_model is None:
                            if file_ext in PCAP_EXTENSIONS:
                                st.error("Для PCAP/PCAPNG/CAP файлів не знайдено сумісної моделі. Створіть Isolation Forest у розділі Тренування.")
                            elif file_ext in TABULAR_EXTENSIONS:
                                st.error("Для CSV/NF файлів не знайдено сумісної моделі. Створіть або оберіть модель для табличних даних.")
                            else:
                                st.error("Непідтримуваний тип файлу. Використовуйте CSV/NF або PCAP.")
                            st.stop()

                        if auto_reason == 'pcap_if':
                            st.success(f"Автовибір для PCAP: **{selected_model}** (Isolation Forest)")
                        elif auto_reason == 'pcap_tabular_fallback':
                            st.warning(
                                f"Для PCAP не знайдено Isolation Forest. "
                                f"Обрано fallback-модель: **{selected_model}**. "
                                "Результат може мати підвищений ризик хибних спрацювань або пропусків."
                            )
                        elif auto_reason == 'family_match_two_stage':
                            st.success(f"Автовибір за сімейством датасету: **{selected_model}** (Two-Stage)")
                        elif auto_reason == 'family_match':
                            st.success(f"Автовибір за сімейством датасету: **{selected_model}**")
                        elif auto_reason == 'family_ambiguous':
                            st.info(f"Сімейство файлу визначено неоднозначно. Обрано найбезпечнішу сумісну модель: **{selected_model}**")
                        elif auto_reason == 'tabular_two_stage':
                            st.success(f"Автовибір для CSV/NF: **{selected_model}** (Two-Stage)")
                        else:
                            st.success(f"Автовибір: **{selected_model}**")
                    # ----------------------------

                    # Завантажуємо модель разом з препроцесором та метаданими
                    engine = ModelEngine(models_dir=str(ROOT_DIR / 'models'))
                    model, preprocessor, metadata = engine.load_model(selected_model)
                    loaded_is_isolation_forest = "Isolation Forest" in str(metadata.get('algorithm', '')) if metadata else False
                    loaded_is_isolation_forest = loaded_is_isolation_forest or (
                        "Isolation Forest" in str(getattr(engine, 'algorithm_name', ''))
                    )

                    # Діагностика моделі
                    print(f"[DIAGNOSTIC] Loaded model: {selected_model}")
                    print(f"[DIAGNOSTIC] Model type: {type(model)}")
                    print(f"[DIAGNOSTIC] Algorithm: {metadata.get('algorithm', 'Unknown') if metadata else 'No metadata'}")
                    print(f"[DIAGNOSTIC] Model attributes: {[attr for attr in dir(model) if not attr.startswith('_')][:10]}")

                    if hasattr(model, 'predict'):
                        print(f"[DIAGNOSTIC] Model has predict method: Yes")
                    else:
                        print(f"[DIAGNOSTIC] ERROR: Model missing predict method!")

                    # Перевірка сумісності моделі з типом файлу
                    file_ext = dataset_path.suffix.lower()
                    compatible_types = _normalize_compatible_types(
                        metadata.get('compatible_file_types', sorted(TABULAR_EXTENSIONS))
                        if metadata else sorted(TABULAR_EXTENSIONS)
                    )
                    if file_ext in TABULAR_EXTENSIONS and loaded_is_isolation_forest:
                        st.warning(
                            "Для CSV/NF зазвичай точніші моделі класифікації (Random Forest / Two-Stage). "
                            "Isolation Forest рекомендовано насамперед для PCAP-аналітики."
                        )
                    if file_ext not in compatible_types:
                        allow_pcap_with_warning = file_ext in PCAP_EXTENSIONS and not loaded_is_isolation_forest
                        if allow_pcap_with_warning:
                            st.warning(
                                f"Модель `{selected_model}` не позначена як сумісна з `{file_ext}`, "
                                "але запуск дозволено у fallback-режимі. "
                                "Рекомендовано перевірити результат додатково на еталонних файлах."
                            )
                        else:
                            fallback_model, fallback_reason = resolve_auto_model(dataset_path)
                            if fallback_model and fallback_model != selected_model:
                                st.warning(
                                    f"Обрана модель `{selected_model}` несумісна з `{file_ext}`. "
                                    f"Автоматично переключено на `{fallback_model}`."
                                )
                                selected_model = fallback_model
                                model, preprocessor, metadata = engine.load_model(selected_model)
                                loaded_is_isolation_forest = "Isolation Forest" in str(metadata.get('algorithm', '')) if metadata else False
                                loaded_is_isolation_forest = loaded_is_isolation_forest or (
                                    "Isolation Forest" in str(getattr(engine, 'algorithm_name', ''))
                                )
                                compatible_types = _normalize_compatible_types(
                                    metadata.get('compatible_file_types', sorted(TABULAR_EXTENSIONS))
                                    if metadata else sorted(TABULAR_EXTENSIONS)
                                )
                            elif fallback_model == selected_model:
                                st.info(
                                    f"Використовується найкраща доступна модель `{selected_model}`, "
                                    f"але результат може бути менш надійним для `{file_ext}`."
                                )
                            else:
                                st.error(
                                    f"Для файлу типу `{file_ext}` не знайдено жодної сумісної моделі. "
                                    "Навчіть відповідну модель у вкладці Тренування."
                                )
                                st.stop()

                    if preprocessor is None:
                        st.error("Ця модель була створена в старій версії без препроцесора. Перетренуйте модель.")
                        st.stop()

                    schema_mode = str(metadata.get('schema_mode', 'unified')).strip().lower() if metadata else 'unified'
                    align_to_schema = schema_mode != 'family'
                    loader = DataLoader()
                    file_stat = dataset_path.stat()
                    scan_row_cap, scan_cap_msg = _resolve_scan_row_cap(file_ext, int(file_stat.st_size))
                    # Hard safety cap for tabular scans even when file size is deceptively small.
                    # Prevents browser/UI freezes on million-row inputs.
                    if file_ext in TABULAR_EXTENSIONS and scan_row_cap is None:
                        scan_row_cap = 250000
                        scan_cap_msg = (
                            "Для стабільної роботи інтерфейсу застосовано безпечний ліміт 250,000 рядків "
                            "для інтерактивного сканування табличного файлу."
                        )
                    if scan_cap_msg:
                        st.warning(scan_cap_msg)

                    df = loader.load_file(
                        str(dataset_path),
                        max_rows=scan_row_cap,
                        multiclass=True,
                        align_to_schema=align_to_schema,
                        preserve_context=True
                    )
                    original_df = df

                    # Add family hint if the model was trained on multiple families.
                    if hasattr(preprocessor, 'feature_columns') and 'family_hint' in preprocessor.feature_columns:
                        file_family = _infer_dataset_family_name(dataset_path.name)
                        if dataset_path.exists():
                            try:
                                stat = dataset_path.stat()
                                fam_info = detect_scan_file_family_info(str(dataset_path), stat.st_mtime, stat.st_size)
                                detected_family = str(fam_info.get('family', '')).strip()
                                if detected_family:
                                    file_family = detected_family
                            except Exception:
                                pass
                        df = df.copy()
                        df['family_hint'] = file_family or "Unknown"

                    st.info("Обробка даних...")
                    progress.progress(45)

                    if 'label' in df.columns:
                        df = df.drop(columns=['label'])

                    # Перевірка сумісності ознак
                    required_features = set(preprocessor.feature_columns)
                    available_features = set(df.columns)
                    missing_features = required_features - available_features
                    coverage = 1.0 - (len(missing_features) / len(required_features)) if required_features else 0.0

                    if coverage < 1.0:
                        health_level = "low" if coverage < 0.7 else "medium" if coverage < 0.9 else "high"
                        coverage_pct = int(coverage * 100)
                        st.markdown(f"""
                        <div class="scan-compat-card {health_level}">
                            <h4 class="scan-compat-title">Сумісність даних: {coverage_pct}%</h4>
                            <div class="scan-compat-bar">
                                <div class="scan-compat-fill" style="width: {coverage_pct}%;"></div>
                            </div>
                            <p class="scan-compat-desc">
                                Модель очікує {len(required_features)} ознак, знайдено {len(available_features)}.
                                Відсутні: {len(missing_features)}
                            </p>
                        </div>
                        """, unsafe_allow_html=True)

                        if coverage < 0.8:
                            st.warning("Низька сумісність може призвести до неточних результатів!")
                            with st.expander("Переглянути відсутні ознаки"):
                                st.write(list(missing_features))
                    else:
                         st.success("Повна сумісність даних (100%)")

                    # --- FEATURE ALIGNMENT FIX for Unified Models ---
                    # Check if preprocessor expects different features than available
                    if hasattr(preprocessor, 'scaler') and hasattr(preprocessor.scaler, 'feature_names_in_'):
                        # Unified model with PipelineScaler - align features
                        expected_features = list(preprocessor.scaler.feature_names_in_)
                        available_features = set(df.columns)

                        # Build reverse synonym map for lookup
                        synonyms = FeatureRegistry.get_synonyms()
                        reverse_map = {}
                        for canonical, aliases in synonyms.items():
                            for alias in aliases:
                                reverse_map[alias] = canonical
                            reverse_map[canonical] = canonical

                        # Try to find synonyms before filling with zeros
                        aligned_count = 0
                        for feat in expected_features:
                            if feat not in available_features:
                                # Search for synonyms in available columns
                                found = False
                                for alias in synonyms.get(feat, []):
                                    if alias in available_features:
                                        df[feat] = df[alias]
                                        found = True
                                        aligned_count += 1
                                        break
                                if not found:
                                    df[feat] = 0

                        # Keep only expected features in correct order
                        df = df[expected_features]
                        print(f"[LOG] Aligned features for unified model: {len(expected_features)} features, {aligned_count} synonyms found")
                    # -------------------------------------------------

                    X = preprocessor.transform(df)
                    print(f"[LOG] Preprocessed features: {X.shape}")

                    st.info("Класифікація трафіку...")
                    progress.progress(60)

                    print("[LOG] Scanning: Predicting...") # Added log

                    # Determine model type for proper prediction handling
                    is_isolation_forest = "Isolation Forest" in str(metadata.get('algorithm', '')) if metadata else False
                    is_isolation_forest = is_isolation_forest or ("Isolation Forest" in str(getattr(engine, 'algorithm_name', '')))
                    is_two_stage = isinstance(model, TwoStageModel)

                    # PREDICTION - use appropriate method based on model type
                    aux_ood_mask = None
                    ood_from_benign_mask = None
                    if is_two_stage:
                        predictions = model.predict(X, threshold=selected_two_stage_threshold)
                        st.caption(
                            "Використано Two-Stage Detection "
                            f"(Профіль: {selected_two_stage_profile_label}, "
                            f"Threshold: {selected_two_stage_threshold:.2f}, "
                            f"еквівалент чутливості: {sensitivity_level}/99)"
                        )
                    elif is_isolation_forest:
                        # Use engine.predict() which handles IF correctly with threshold
                        predictions = engine.predict(X)

                        # Get anomaly scores for visualization
                        scores = model.decision_function(X)
                        if len(scores) <= 300000:
                            st.session_state['anomaly_scores'] = scores.tolist()
                        else:
                            st.session_state['anomaly_scores'] = None
                            st.caption(
                                "Для великого файлу детальні score-дані IF не збережено, "
                                "щоб не перевищувати ліміт памʼяті."
                            )

                        # Show IF statistics
                        if hasattr(engine, 'if_threshold_') and engine.if_threshold_ is not None:
                            threshold_mode = getattr(engine, 'if_threshold_mode_', 'decision_zero')
                            st.caption(
                                "Використано Anomaly Detection "
                                f"(IF threshold: {engine.if_threshold_:.4f}, mode: {threshold_mode})"
                            )
                        else:
                            st.caption("Використано Anomaly Detection (Isolation Forest)")

                        print(f"[LOG] IF scores: min={scores.min():.4f}, max={scores.max():.4f}, mean={scores.mean():.4f}")
                    else:
                        # Standard classification model
                        predictions = engine.predict(X)

                    predictions = np.asarray(predictions)

                    if is_isolation_forest:
                        unique_preds = set(np.unique(predictions).tolist())
                        if -1 in unique_preds:


